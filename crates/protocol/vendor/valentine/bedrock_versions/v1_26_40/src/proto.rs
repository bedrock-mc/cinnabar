// Generated by valentine_gen. Do not edit.

//!Generated protocol packet definitions.
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unused_parens)]
#![allow(unused_variables)]
#![allow(clippy::all)]
use crate::types::*;
use bytes::Buf;
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LoginPacket {
    pub client_network_version: i32,
    pub connection_request: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for LoginPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += {
            let _len = (&self.connection_request).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LoginPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.client_network_version.encode(buf)?;
        let bytes = (&self.connection_request).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let client_network_version = <i32 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let connection_request = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self {
            client_network_version,
            connection_request,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayStatusPacket {
    pub status: EnumsPlayStatus,
}
impl crate::bedrock::codec::BedrockSized for PlayStatusPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.status);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayStatusPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.status.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let status = <EnumsPlayStatus as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { status })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerToClientHandshakePacket {
    pub handshake_web_token: String,
}
impl crate::bedrock::codec::BedrockSized for ServerToClientHandshakePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.handshake_web_token).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerToClientHandshakePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.handshake_web_token).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let handshake_web_token = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            handshake_web_token,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientToServerHandshakePacket {}
impl crate::bedrock::codec::BedrockSized for ClientToServerHandshakePacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientToServerHandshakePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct DisconnectPacket {
    pub reason: EnumsConnectionDisconnectFailReason,
    pub messages: DisconnectPacketMessages,
}
impl crate::bedrock::codec::BedrockSized for DisconnectPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.reason);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.messages);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for DisconnectPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.reason.encode(buf)?;
        self.messages.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let reason =
            <EnumsConnectionDisconnectFailReason as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let messages =
            <DisconnectPacketMessages as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { reason, messages })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePacksInfoPacket {
    pub resource_pack_required: bool,
    pub has_addon_packs: bool,
    pub has_scripts: bool,
    pub force_disable_vibrant_visuals: bool,
    pub world_template_id_and_version: PackIdVersionjson,
    pub resource_packs: Vec<PackInfoData>,
}
impl crate::bedrock::codec::BedrockSized for ResourcePacksInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += 1usize;
        size += 1usize;
        size +=
            crate::bedrock::codec::BedrockSized::encoded_size(&self.world_template_id_and_version);
        size += {
            let _len = (&self.resource_packs).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.resource_packs)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePacksInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.resource_pack_required.encode(buf)?;
        self.has_addon_packs.encode(buf)?;
        self.has_scripts.encode(buf)?;
        self.force_disable_vibrant_visuals.encode(buf)?;
        self.world_template_id_and_version.encode(buf)?;
        let len = self.resource_packs.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.resource_packs {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let resource_pack_required =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let has_addon_packs = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let has_scripts = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let force_disable_vibrant_visuals =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let world_template_id_and_version =
            <PackIdVersionjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let resource_packs = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(<PackInfoData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?);
            }
            tmp_vec
        };
        Ok(Self {
            resource_pack_required,
            has_addon_packs,
            has_scripts,
            force_disable_vibrant_visuals,
            world_template_id_and_version,
            resource_packs,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePackStackPacket {
    pub texture_pack_required: bool,
    pub texture_pack_list: Vec<PackInstanceId>,
    pub base_game_version: String,
    pub experiments: Experiments,
    pub include_editor_packs: bool,
}
impl crate::bedrock::codec::BedrockSized for ResourcePackStackPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += {
            let _len = (&self.texture_pack_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.texture_pack_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.base_game_version).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.experiments);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePackStackPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.texture_pack_required.encode(buf)?;
        let len = self.texture_pack_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.texture_pack_list {
            item.encode(buf)?;
        }
        let bytes = (&self.base_game_version).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.experiments.encode(buf)?;
        self.include_editor_packs.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let texture_pack_required = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let texture_pack_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <PackInstanceId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let base_game_version = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let experiments = <Experiments as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let include_editor_packs = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            texture_pack_required,
            texture_pack_list,
            base_game_version,
            experiments,
            include_editor_packs,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePackClientResponsePacket {
    pub response: ResourcePackClientResponsePacketResponse,
}
impl crate::bedrock::codec::BedrockSized for ResourcePackClientResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.response);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePackClientResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.response.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let response = <ResourcePackClientResponsePacketResponse as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self { response })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct TextPacket {
    pub localize: bool,
    pub message_category: u8,
    pub body: TextPacketBody,
    pub senders_xuid: String,
    pub platform_id: String,
    pub filtered_message: Option<String>,
}
impl crate::bedrock::codec::BedrockSized for TextPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.body);
        size += {
            let _len = (&self.senders_xuid).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.platform_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            1usize
                + match &self.filtered_message {
                    Some(_v) => {
                        let _len = (_v).as_bytes().len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + _len
                    }
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for TextPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.localize.encode(buf)?;
        self.message_category.encode(buf)?;
        self.body.encode(buf)?;
        let bytes = (&self.senders_xuid).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.platform_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        match &self.filtered_message {
            Some(v) => {
                buf.put_u8(1);
                let bytes = (v).as_bytes();
                let len = bytes.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                buf.put_slice(bytes);
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let localize = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let message_category = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let body = <TextPacketBody as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let senders_xuid = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let platform_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let filtered_message = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    if buf.remaining() < len {
                        return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                            declared: len,
                            available: buf.remaining(),
                        });
                    }
                    let mut bytes = vec![0u8; len];
                    buf.copy_to_slice(&mut bytes);
                    crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                })
            } else {
                None
            }
        };
        Ok(Self {
            localize,
            message_category,
            body,
            senders_xuid,
            platform_id,
            filtered_message,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetTimePacket {
    pub time: i32,
}
impl crate::bedrock::codec::BedrockSized for SetTimePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.time),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetTimePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.time).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let time =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { time })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct StartGamePacket {
    pub entity_id: ActorUniqueId,
    pub runtime_id: ActorRuntimeId,
    pub game_type: EnumsGameType,
    pub position: Vec3,
    pub rotation: Vec2,
    pub settings: LevelSettings,
    pub level_id: String,
    pub level_name: String,
    pub template_content_identity: String,
    pub is_trial: bool,
    pub movement_settings: SyncedPlayerMovementSettings,
    pub level_current_time: u64,
    pub enchantment_seed: i32,
    pub block_properties: Vec<ServerBlockProperty>,
    pub multiplayer_correlation_id: String,
    pub enable_item_stack_net_manager: bool,
    pub server_version: String,
    pub player_property_data: crate::bedrock::codec::Nbt,
    pub server_block_type_registry_checksum: u64,
    pub world_template_id: MceUuiDjson,
    pub server_enabled_client_side_generation: bool,
    pub block_network_ids_are_hashes: bool,
    pub network_permissions: NetworkPermissions,
    pub server_configuration_join_info: Option<ServerConfigurationServerConfigurationJoinInfo>,
    pub server_telemetry_data: SocialEventsServerTelemetryData,
}
impl crate::bedrock::codec::BedrockSized for StartGamePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.game_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.settings);
        size += {
            let _len = (&self.level_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.level_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.template_content_identity).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.movement_settings);
        size += 8usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.enchantment_seed),
        );
        size += {
            let _len = (&self.block_properties).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.block_properties)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.multiplayer_correlation_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += {
            let _len = (&self.server_version).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_property_data);
        size += 8usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.world_template_id);
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.network_permissions);
        size += {
            1usize
                + match &self.server_configuration_join_info {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.server_telemetry_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for StartGamePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.entity_id.encode(buf)?;
        self.runtime_id.encode(buf)?;
        self.game_type.encode(buf)?;
        self.position.encode(buf)?;
        self.rotation.encode(buf)?;
        self.settings.encode(buf)?;
        let bytes = (&self.level_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.level_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.template_content_identity).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.is_trial.encode(buf)?;
        self.movement_settings.encode(buf)?;
        crate::bedrock::codec::U64LE(self.level_current_time).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.enchantment_seed).encode(buf)?;
        let len = self.block_properties.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.block_properties {
            item.encode(buf)?;
        }
        let bytes = (&self.multiplayer_correlation_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.enable_item_stack_net_manager.encode(buf)?;
        let bytes = (&self.server_version).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.player_property_data.encode(buf)?;
        crate::bedrock::codec::U64LE(self.server_block_type_registry_checksum).encode(buf)?;
        self.world_template_id.encode(buf)?;
        self.server_enabled_client_side_generation.encode(buf)?;
        self.block_network_ids_are_hashes.encode(buf)?;
        self.network_permissions.encode(buf)?;
        match &self.server_configuration_join_info {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        self.server_telemetry_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let entity_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let runtime_id = <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let game_type = <EnumsGameType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let settings = <LevelSettings as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let level_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let level_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let template_content_identity = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let is_trial = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let movement_settings =
            <SyncedPlayerMovementSettings as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let level_current_time =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let enchantment_seed =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let block_properties = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ServerBlockProperty as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let multiplayer_correlation_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let enable_item_stack_net_manager =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let server_version = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let player_property_data =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let server_block_type_registry_checksum =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let world_template_id =
            <MceUuiDjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let server_enabled_client_side_generation =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let block_network_ids_are_hashes =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let network_permissions =
            <NetworkPermissions as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let server_configuration_join_info = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ServerConfigurationServerConfigurationJoinInfo as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let server_telemetry_data =
            <SocialEventsServerTelemetryData as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self {
            entity_id,
            runtime_id,
            game_type,
            position,
            rotation,
            settings,
            level_id,
            level_name,
            template_content_identity,
            is_trial,
            movement_settings,
            level_current_time,
            enchantment_seed,
            block_properties,
            multiplayer_correlation_id,
            enable_item_stack_net_manager,
            server_version,
            player_property_data,
            server_block_type_registry_checksum,
            world_template_id,
            server_enabled_client_side_generation,
            block_network_ids_are_hashes,
            network_permissions,
            server_configuration_join_info,
            server_telemetry_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddPlayerPacket {
    pub uuid: MceUuiDjson,
    pub player_name: String,
    pub target_runtime_id: ActorRuntimeId,
    pub platform_chat_id: String,
    pub position: Vec3,
    pub velocity: Vec3,
    pub rotation: Vec2,
    pub y_head_rotation: f32,
    pub carried_item: CerealizerNetworkItemStackDescriptorSerializedData,
    pub player_game_type: EnumsGameType,
    pub entity_data: SynchedActorDataCopyableDataList,
    pub synched_properties: PropertySyncData,
    pub abilities_data: SerializedAbilitiesData,
    pub actor_links: Vec<ActorLink>,
    pub device_id: String,
    pub build_platform: EnumsBuildPlatform,
}
impl crate::bedrock::codec::BedrockSized for AddPlayerPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.uuid);
        size += {
            let _len = (&self.player_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += {
            let _len = (&self.platform_chat_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.velocity);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.carried_item);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_game_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_data);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.synched_properties);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.abilities_data);
        size += {
            let _len = (&self.actor_links).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.actor_links)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.device_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.build_platform);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddPlayerPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.uuid.encode(buf)?;
        let bytes = (&self.player_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.target_runtime_id.encode(buf)?;
        let bytes = (&self.platform_chat_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.position.encode(buf)?;
        self.velocity.encode(buf)?;
        self.rotation.encode(buf)?;
        crate::bedrock::codec::F32LE(self.y_head_rotation).encode(buf)?;
        self.carried_item.encode(buf)?;
        self.player_game_type.encode(buf)?;
        self.entity_data.encode(buf)?;
        self.synched_properties.encode(buf)?;
        self.abilities_data.encode(buf)?;
        let len = self.actor_links.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.actor_links {
            item.encode(buf)?;
        }
        let bytes = (&self.device_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.build_platform.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let uuid = <MceUuiDjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let player_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let platform_chat_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let velocity = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let y_head_rotation =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let carried_item = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let player_game_type =
            <EnumsGameType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let entity_data =
            <SynchedActorDataCopyableDataList as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let synched_properties =
            <PropertySyncData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let abilities_data =
            <SerializedAbilitiesData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_links = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(<ActorLink as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?);
            }
            tmp_vec
        };
        let device_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let build_platform =
            <EnumsBuildPlatform as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            uuid,
            player_name,
            target_runtime_id,
            platform_chat_id,
            position,
            velocity,
            rotation,
            y_head_rotation,
            carried_item,
            player_game_type,
            entity_data,
            synched_properties,
            abilities_data,
            actor_links,
            device_id,
            build_platform,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddActorPacket {
    pub target_actor_id: ActorUniqueId,
    pub target_runtime_id: ActorRuntimeId,
    pub actor_type: String,
    pub position: Vec3,
    pub velocity: Vec3,
    pub rotation: Vec2,
    pub y_head_rotation: f32,
    pub y_body_rotation: f32,
    pub attributes_list: Vec<SyncedAttribute>,
    pub actor_data: SynchedActorDataCopyableDataList,
    pub synched_properties: PropertySyncData,
    pub actor_links: Vec<ActorLink>,
}
impl crate::bedrock::codec::BedrockSized for AddActorPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += {
            let _len = (&self.actor_type).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.velocity);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += 4usize;
        size += 4usize;
        size += {
            let _len = (&self.attributes_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.attributes_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_data);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.synched_properties);
        size += {
            let _len = (&self.actor_links).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.actor_links)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddActorPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.target_runtime_id.encode(buf)?;
        let bytes = (&self.actor_type).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.position.encode(buf)?;
        self.velocity.encode(buf)?;
        self.rotation.encode(buf)?;
        crate::bedrock::codec::F32LE(self.y_head_rotation).encode(buf)?;
        crate::bedrock::codec::F32LE(self.y_body_rotation).encode(buf)?;
        let len = self.attributes_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.attributes_list {
            item.encode(buf)?;
        }
        self.actor_data.encode(buf)?;
        self.synched_properties.encode(buf)?;
        let len = self.actor_links.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.actor_links {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_type = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let velocity = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let y_head_rotation =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let y_body_rotation =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let attributes_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <SyncedAttribute as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let actor_data =
            <SynchedActorDataCopyableDataList as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let synched_properties =
            <PropertySyncData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_links = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(<ActorLink as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?);
            }
            tmp_vec
        };
        Ok(Self {
            target_actor_id,
            target_runtime_id,
            actor_type,
            position,
            velocity,
            rotation,
            y_head_rotation,
            y_body_rotation,
            attributes_list,
            actor_data,
            synched_properties,
            actor_links,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RemoveActorPacket {
    pub target_actor_id: ActorUniqueId,
}
impl crate::bedrock::codec::BedrockSized for RemoveActorPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RemoveActorPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { target_actor_id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddItemActorPacket {
    pub target_actor_id: ActorUniqueId,
    pub target_runtime_id: ActorRuntimeId,
    pub item: CerealizerNetworkItemStackDescriptorSerializedData,
    pub position: Vec3,
    pub velocity: Vec3,
    pub entity_data: SynchedActorDataCopyableDataList,
    pub is_from_fishing: bool,
}
impl crate::bedrock::codec::BedrockSized for AddItemActorPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.item);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.velocity);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_data);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddItemActorPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.target_runtime_id.encode(buf)?;
        self.item.encode(buf)?;
        self.position.encode(buf)?;
        self.velocity.encode(buf)?;
        self.entity_data.encode(buf)?;
        self.is_from_fishing.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let item = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let velocity = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let entity_data =
            <SynchedActorDataCopyableDataList as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let is_from_fishing = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_actor_id,
            target_runtime_id,
            item,
            position,
            velocity,
            entity_data,
            is_from_fishing,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerPlayerPostMovePositionPacket {
    pub pos: Vec3,
}
impl crate::bedrock::codec::BedrockSized for ServerPlayerPostMovePositionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pos);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerPlayerPostMovePositionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.pos.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let pos = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { pos })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct TakeItemActorPacket {
    pub item_runtime_id: ActorRuntimeId,
    pub actor_runtime_id: ActorRuntimeId,
}
impl crate::bedrock::codec::BedrockSized for TakeItemActorPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.item_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_runtime_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for TakeItemActorPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.item_runtime_id.encode(buf)?;
        self.actor_runtime_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let item_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            item_runtime_id,
            actor_runtime_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MoveActorAbsolutePacket {
    pub move_data: MoveActorAbsoluteData,
}
impl crate::bedrock::codec::BedrockSized for MoveActorAbsolutePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.move_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MoveActorAbsolutePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.move_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let move_data =
            <MoveActorAbsoluteData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { move_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MovePlayerPacket {
    pub player_runtime_id: ActorRuntimeId,
    pub position: Vec3,
    pub rotation: Vec2,
    pub y_head_rotation: f32,
    pub position_mode: EnumsPlayerPositionModeComponentPositionMode,
    pub on_ground: bool,
    pub riding_runtime_id: ActorRuntimeId,
    pub teleport_data: Option<MovePlayerTeleportData>,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for MovePlayerPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position_mode);
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.riding_runtime_id);
        size += {
            1usize
                + match &self.teleport_data {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MovePlayerPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_runtime_id.encode(buf)?;
        self.position.encode(buf)?;
        self.rotation.encode(buf)?;
        crate::bedrock::codec::F32LE(self.y_head_rotation).encode(buf)?;
        self.position_mode.encode(buf)?;
        self.on_ground.encode(buf)?;
        self.riding_runtime_id.encode(buf)?;
        match &self.teleport_data {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let y_head_rotation =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let position_mode = <EnumsPlayerPositionModeComponentPositionMode as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let on_ground = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let riding_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let teleport_data = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <MovePlayerTeleportData as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            player_runtime_id,
            position,
            rotation,
            y_head_rotation,
            position_mode,
            on_ground,
            riding_runtime_id,
            teleport_data,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateBlockPacket {
    pub block_position: BlockPos,
    pub block_runtime_id: u32,
    pub flags: u32,
    pub layer: u32,
}
impl crate::bedrock::codec::BedrockSized for UpdateBlockPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.block_runtime_id,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.flags,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.layer,
        ));
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateBlockPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.block_runtime_id).encode(buf)?;
        crate::bedrock::codec::VarUInt(self.flags).encode(buf)?;
        crate::bedrock::codec::VarUInt(self.layer).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let block_runtime_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let flags =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let layer =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            block_position,
            block_runtime_id,
            flags,
            layer,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddPaintingPacket {
    pub target_actor_id: ActorUniqueId,
    pub target_runtime_id: ActorRuntimeId,
    pub position: Vec3,
    pub direction: i32,
    pub motif: String,
}
impl crate::bedrock::codec::BedrockSized for AddPaintingPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.direction),
        );
        size += {
            let _len = (&self.motif).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddPaintingPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.target_runtime_id.encode(buf)?;
        self.position.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.direction).encode(buf)?;
        let bytes = (&self.motif).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let direction =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let motif = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            target_actor_id,
            target_runtime_id,
            position,
            direction,
            motif,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LevelEventPacket {
    pub event_id: i32,
    pub position: Vec3,
    pub data: i32,
}
impl crate::bedrock::codec::BedrockSized for LevelEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.event_id),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.data),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LevelEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.event_id).encode(buf)?;
        self.position.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.data).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let event_id =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            event_id,
            position,
            data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BlockEventPacket {
    pub block_position: BlockPos,
    pub event_type: i32,
    pub event_value: i32,
}
impl crate::bedrock::codec::BedrockSized for BlockEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.event_type),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.event_value),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BlockEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.event_type).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.event_value).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_type =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let event_value =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            block_position,
            event_type,
            event_value,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ActorEventPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub event_id: EnumsActorEvent,
    pub data: i32,
    pub fire_at_position: Option<Vec3>,
}
impl crate::bedrock::codec::BedrockSized for ActorEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.event_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.data),
        );
        size += {
            1usize
                + match &self.fire_at_position {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ActorEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.event_id.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.data).encode(buf)?;
        match &self.fire_at_position {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_id = <EnumsActorEvent as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let fire_at_position = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<Vec3 as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        Ok(Self {
            target_runtime_id,
            event_id,
            data,
            fire_at_position,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MobEffectPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub event_id: EnumsMobEffectPacketPayloadEvent,
    pub effect_id: i32,
    pub effect_amplifier: i32,
    pub show_particles: bool,
    pub effect_duration_ticks: i32,
    pub tick: PlayerInputTick,
    pub ambient: bool,
}
impl crate::bedrock::codec::BedrockSized for MobEffectPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.event_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.effect_id),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.effect_amplifier),
        );
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.effect_duration_ticks),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MobEffectPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.event_id.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.effect_id).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.effect_amplifier).encode(buf)?;
        self.show_particles.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.effect_duration_ticks).encode(buf)?;
        self.tick.encode(buf)?;
        self.ambient.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_id =
            <EnumsMobEffectPacketPayloadEvent as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let effect_id =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let effect_amplifier =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let show_particles = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let effect_duration_ticks =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let ambient = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            event_id,
            effect_id,
            effect_amplifier,
            show_particles,
            effect_duration_ticks,
            tick,
            ambient,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateAttributesPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub attribute_list: Vec<AttributeData>,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for UpdateAttributesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += {
            let _len = (&self.attribute_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.attribute_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateAttributesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        let len = self.attribute_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.attribute_list {
            item.encode(buf)?;
        }
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let attribute_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(<AttributeData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?);
            }
            tmp_vec
        };
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            attribute_list,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct InventoryTransactionPacket {
    pub legacy_request_id: TypedClientNetIdstructItemStackLegacyRequestIdTagint32T0,
    pub legacy_set_item_slots: Option<Vec<LegacySetSlot>>,
    pub transaction: Option<InventoryTransactionPacketTransaction>,
}
impl crate::bedrock::codec::BedrockSized for InventoryTransactionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.legacy_request_id);
        size += {
            1usize
                + match &self.legacy_set_item_slots {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v)
                            .iter()
                            .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                            .sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.transaction {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for InventoryTransactionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.legacy_request_id.encode(buf)?;
        match &self.legacy_set_item_slots {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    item.encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        match &self.transaction {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let legacy_request_id = <TypedClientNetIdstructItemStackLegacyRequestIdTagint32T0 as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let legacy_set_item_slots = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec.push(
                            <LegacySetSlot as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        let transaction = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <InventoryTransactionPacketTransaction as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            legacy_request_id,
            legacy_set_item_slots,
            transaction,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MobEquipmentPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub item: CerealizerNetworkItemStackDescriptorSerializedData,
    pub slot: u8,
    pub selected_slot: u8,
    pub container_id: u8,
}
impl crate::bedrock::codec::BedrockSized for MobEquipmentPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.item);
        size += 1usize;
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MobEquipmentPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.item.encode(buf)?;
        self.slot.encode(buf)?;
        self.selected_slot.encode(buf)?;
        self.container_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let item = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let slot = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let selected_slot = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            item,
            slot,
            selected_slot,
            container_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MobArmorEquipmentPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub head: CerealizerNetworkItemStackDescriptorSerializedData,
    pub torso: CerealizerNetworkItemStackDescriptorSerializedData,
    pub legs: CerealizerNetworkItemStackDescriptorSerializedData,
    pub feet: CerealizerNetworkItemStackDescriptorSerializedData,
    pub body: CerealizerNetworkItemStackDescriptorSerializedData,
}
impl crate::bedrock::codec::BedrockSized for MobArmorEquipmentPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.head);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.torso);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.legs);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.feet);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.body);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MobArmorEquipmentPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.head.encode(buf)?;
        self.torso.encode(buf)?;
        self.legs.encode(buf)?;
        self.feet.encode(buf)?;
        self.body.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let head = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let torso = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let legs = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let feet = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let body = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            target_runtime_id,
            head,
            torso,
            legs,
            feet,
            body,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct InteractPacket {
    pub action: EnumsInteractPacketPayloadAction,
    pub target_runtime_id: ActorRuntimeId,
    pub position: Option<Vec3>,
}
impl crate::bedrock::codec::BedrockSized for InteractPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += {
            1usize
                + match &self.position {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for InteractPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        self.target_runtime_id.encode(buf)?;
        match &self.position {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action =
            <EnumsInteractPacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<Vec3 as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        Ok(Self {
            action,
            target_runtime_id,
            position,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BlockPickRequestPacket {
    pub position: BlockPos,
    pub with_data: bool,
    pub max_slots: u8,
}
impl crate::bedrock::codec::BedrockSized for BlockPickRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BlockPickRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.position.encode(buf)?;
        self.with_data.encode(buf)?;
        self.max_slots.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let with_data = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let max_slots = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            position,
            with_data,
            max_slots,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ActorPickRequestPacket {
    pub actor_id: i64,
    pub max_slots: u8,
    pub with_data: bool,
}
impl crate::bedrock::codec::BedrockSized for ActorPickRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 8usize;
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ActorPickRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I64LE(self.actor_id).encode(buf)?;
        self.max_slots.encode(buf)?;
        self.with_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let actor_id =
            <crate::bedrock::codec::I64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let max_slots = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let with_data = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            actor_id,
            max_slots,
            with_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerActionPacket {
    pub player_runtime_id: ActorRuntimeId,
    pub action: EnumsPlayerActionType,
    pub block_position: BlockPos,
    pub result_pos: BlockPos,
    pub face: i32,
}
impl crate::bedrock::codec::BedrockSized for PlayerActionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.result_pos);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.face),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerActionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_runtime_id.encode(buf)?;
        self.action.encode(buf)?;
        self.block_position.encode(buf)?;
        self.result_pos.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.face).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let action =
            <EnumsPlayerActionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let result_pos = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let face =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            player_runtime_id,
            action,
            block_position,
            result_pos,
            face,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct HurtArmorPacket {
    pub cause: i32,
    pub damage: i32,
    pub armor_slots: u64,
}
impl crate::bedrock::codec::BedrockSized for HurtArmorPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.cause),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.damage),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::VarULong(self.armor_slots),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for HurtArmorPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.cause).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.damage).encode(buf)?;
        crate::bedrock::codec::VarULong(self.armor_slots).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let cause =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let damage =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let armor_slots =
            <crate::bedrock::codec::VarULong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            cause,
            damage,
            armor_slots,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetActorDataPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub actor_data: SynchedActorDataCopyableDataList,
    pub synched_properties: PropertySyncData,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for SetActorDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_data);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.synched_properties);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetActorDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.actor_data.encode(buf)?;
        self.synched_properties.encode(buf)?;
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_data =
            <SynchedActorDataCopyableDataList as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let synched_properties =
            <PropertySyncData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            actor_data,
            synched_properties,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetActorMotionPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub motion: Vec3,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for SetActorMotionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.motion);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetActorMotionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.motion.encode(buf)?;
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let motion = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            motion,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetActorLinkPacket {
    pub link: ActorLink,
}
impl crate::bedrock::codec::BedrockSized for SetActorLinkPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.link);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetActorLinkPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.link.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let link = <ActorLink as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { link })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetHealthPacket {
    pub health: i32,
}
impl crate::bedrock::codec::BedrockSized for SetHealthPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.health),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetHealthPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.health).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let health =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { health })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetSpawnPositionPacket {
    pub spawn_position_type: EnumsSpawnPositionType,
    pub block_position: BlockPos,
    pub dimensiontype: DimensionType,
    pub spawn_block_pos: BlockPos,
}
impl crate::bedrock::codec::BedrockSized for SetSpawnPositionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.spawn_position_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimensiontype);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.spawn_block_pos);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetSpawnPositionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.spawn_position_type.encode(buf)?;
        self.block_position.encode(buf)?;
        self.dimensiontype.encode(buf)?;
        self.spawn_block_pos.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let spawn_position_type =
            <EnumsSpawnPositionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimensiontype =
            <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let spawn_block_pos = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            spawn_position_type,
            block_position,
            dimensiontype,
            spawn_block_pos,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AnimatePacket {
    pub action: EnumsAnimatePacketPayloadAction,
    pub target_actor_runtime_id: ActorRuntimeId,
    pub data: f32,
    pub swing_source: Option<String>,
}
impl crate::bedrock::codec::BedrockSized for AnimatePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_runtime_id);
        size += 4usize;
        size += {
            1usize
                + match &self.swing_source {
                    Some(_v) => {
                        let _len = (_v).as_bytes().len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + _len
                    }
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AnimatePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        self.target_actor_runtime_id.encode(buf)?;
        crate::bedrock::codec::F32LE(self.data).encode(buf)?;
        match &self.swing_source {
            Some(v) => {
                buf.put_u8(1);
                let bytes = (v).as_bytes();
                let len = bytes.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                buf.put_slice(bytes);
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action =
            <EnumsAnimatePacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let target_actor_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let swing_source = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    if buf.remaining() < len {
                        return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                            declared: len,
                            available: buf.remaining(),
                        });
                    }
                    let mut bytes = vec![0u8; len];
                    buf.copy_to_slice(&mut bytes);
                    crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                })
            } else {
                None
            }
        };
        Ok(Self {
            action,
            target_actor_runtime_id,
            data,
            swing_source,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RespawnPacket {
    pub position: Vec3,
    pub state: EnumsPlayerRespawnState,
    pub player_runtime_id: ActorRuntimeId,
}
impl crate::bedrock::codec::BedrockSized for RespawnPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.state);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_runtime_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RespawnPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.position.encode(buf)?;
        self.state.encode(buf)?;
        self.player_runtime_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let state =
            <EnumsPlayerRespawnState as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let player_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            position,
            state,
            player_runtime_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ContainerOpenPacket {
    pub container_id: u8,
    pub container_type: u8,
    pub position: BlockPos,
    pub target_actor_id: ActorUniqueId,
}
impl crate::bedrock::codec::BedrockSized for ContainerOpenPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ContainerOpenPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        self.container_type.encode(buf)?;
        self.position.encode(buf)?;
        self.target_actor_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let container_type = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            container_id,
            container_type,
            position,
            target_actor_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ContainerClosePacket {
    pub container_id: u8,
    pub container_type: u8,
    pub server_initiated_close: bool,
}
impl crate::bedrock::codec::BedrockSized for ContainerClosePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ContainerClosePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        self.container_type.encode(buf)?;
        self.server_initiated_close.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let container_type = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let server_initiated_close =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            container_id,
            container_type,
            server_initiated_close,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerHotbarPacket {
    pub selected_slot: u32,
    pub container_id: u8,
    pub shouldselectslot: bool,
}
impl crate::bedrock::codec::BedrockSized for PlayerHotbarPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.selected_slot,
        ));
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerHotbarPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.selected_slot).encode(buf)?;
        self.container_id.encode(buf)?;
        self.shouldselectslot.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let selected_slot =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let shouldselectslot = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            selected_slot,
            container_id,
            shouldselectslot,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct InventoryContentPacket {
    pub container_id: u32,
    pub slots: Vec<CerealizerNetworkItemStackDescriptorSerializedData>,
    pub full_container_name: FullContainerName,
    pub storage_item: CerealizerNetworkItemStackDescriptorSerializedData,
}
impl crate::bedrock::codec::BedrockSized for InventoryContentPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.container_id,
        ));
        size += {
            let _len = (&self.slots).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.slots)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.full_container_name);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.storage_item);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for InventoryContentPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.container_id).encode(buf)?;
        let len = self.slots.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.slots {
            item.encode(buf)?;
        }
        self.full_container_name.encode(buf)?;
        self.storage_item.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let slots = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let full_container_name =
            <FullContainerName as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let storage_item = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            container_id,
            slots,
            full_container_name,
            storage_item,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct InventorySlotPacket {
    pub container_id: u8,
    pub slot: u32,
    pub full_container_name: Option<FullContainerName>,
    pub storage_item: Option<CerealizerNetworkItemStackDescriptorSerializedData>,
    pub item: CerealizerNetworkItemStackDescriptorSerializedData,
}
impl crate::bedrock::codec::BedrockSized for InventorySlotPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.slot,
        ));
        size += {
            1usize
                + match &self.full_container_name {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.storage_item {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.item);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for InventorySlotPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.slot).encode(buf)?;
        match &self.full_container_name {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.storage_item {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        self.item.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let slot = <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?
        .0;
        let full_container_name = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<FullContainerName as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?)
            } else {
                None
            }
        };
        let storage_item = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let item = <CerealizerNetworkItemStackDescriptorSerializedData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            container_id,
            slot,
            full_container_name,
            storage_item,
            item,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ContainerSetDataPacket {
    pub container_id: u8,
    pub id: i32,
    pub value: i32,
}
impl crate::bedrock::codec::BedrockSized for ContainerSetDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.id),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.value),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ContainerSetDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.id).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.value).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let id = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?
        .0;
        let value =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            container_id,
            id,
            value,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CraftingDataPacket {
    pub shaped_recipes: Vec<ShapedRecipePayload>,
    pub shapeless_recipes: Vec<ShapelessRecipePayload>,
    pub multi_recipes: Vec<MultiRecipePayload>,
    pub user_data_shapeless_recipes: Vec<ShapelessRecipePayload>,
    pub shapeless_chemistry_recipes: Vec<ShapelessRecipePayload>,
    pub shaped_chemistry_recipes: Vec<ShapedRecipePayload>,
    pub smithing_transform_recipes: Vec<SmithingTransformRecipePayload>,
    pub smithing_trim_recipes: Vec<SmithingTrimRecipePayload>,
    pub potion_mixes: Vec<PotionMixDataEntry>,
    pub container_mixes: Vec<ContainerMixDataEntry>,
    pub material_reducers: Vec<MaterialReducerDataEntry>,
    pub clear_recipes: bool,
}
impl crate::bedrock::codec::BedrockSized for CraftingDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.shaped_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.shaped_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.shapeless_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.shapeless_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.multi_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.multi_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.user_data_shapeless_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.user_data_shapeless_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.shapeless_chemistry_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.shapeless_chemistry_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.shaped_chemistry_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.shaped_chemistry_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.smithing_transform_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.smithing_transform_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.smithing_trim_recipes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.smithing_trim_recipes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.potion_mixes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.potion_mixes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.container_mixes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.container_mixes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.material_reducers).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.material_reducers)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CraftingDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.shaped_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.shaped_recipes {
            item.encode(buf)?;
        }
        let len = self.shapeless_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.shapeless_recipes {
            item.encode(buf)?;
        }
        let len = self.multi_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.multi_recipes {
            item.encode(buf)?;
        }
        let len = self.user_data_shapeless_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.user_data_shapeless_recipes {
            item.encode(buf)?;
        }
        let len = self.shapeless_chemistry_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.shapeless_chemistry_recipes {
            item.encode(buf)?;
        }
        let len = self.shaped_chemistry_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.shaped_chemistry_recipes {
            item.encode(buf)?;
        }
        let len = self.smithing_transform_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.smithing_transform_recipes {
            item.encode(buf)?;
        }
        let len = self.smithing_trim_recipes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.smithing_trim_recipes {
            item.encode(buf)?;
        }
        let len = self.potion_mixes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.potion_mixes {
            item.encode(buf)?;
        }
        let len = self.container_mixes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.container_mixes {
            item.encode(buf)?;
        }
        let len = self.material_reducers.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.material_reducers {
            item.encode(buf)?;
        }
        self.clear_recipes.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let shaped_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ShapedRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let shapeless_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ShapelessRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let multi_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <MultiRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let user_data_shapeless_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ShapelessRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let shapeless_chemistry_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ShapelessRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let shaped_chemistry_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ShapedRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let smithing_transform_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SmithingTransformRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let smithing_trim_recipes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <SmithingTrimRecipePayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let potion_mixes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <PotionMixDataEntry as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let container_mixes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ContainerMixDataEntry as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let material_reducers = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <MaterialReducerDataEntry as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let clear_recipes = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            shaped_recipes,
            shapeless_recipes,
            multi_recipes,
            user_data_shapeless_recipes,
            shapeless_chemistry_recipes,
            shaped_chemistry_recipes,
            smithing_transform_recipes,
            smithing_trim_recipes,
            potion_mixes,
            container_mixes,
            material_reducers,
            clear_recipes,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct GuiDataPickItemPacket {
    pub item_name: String,
    pub item_effect_name: String,
    pub slot: i32,
}
impl crate::bedrock::codec::BedrockSized for GuiDataPickItemPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.item_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.item_effect_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for GuiDataPickItemPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.item_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.item_effect_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::I32LE(self.slot).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let item_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let item_effect_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let slot =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            item_name,
            item_effect_name,
            slot,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BlockActorDataPacket {
    pub block_position: BlockPos,
    pub actor_data_tags: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for BlockActorDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_data_tags);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BlockActorDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        self.actor_data_tags.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_data_tags =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            block_position,
            actor_data_tags,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LevelChunkPacket {
    pub chunk_position: ChunkPos,
    pub dimension_id: DimensionType,
    pub subchunks_count: u32,
    pub client_request_sub_chunk_limit: Option<i32>,
    pub cache_enabled: bool,
    pub cache_metadata: Vec<LevelChunkPacketPayloadSubChunkMetadata>,
    pub serialized_chunk_data: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for LevelChunkPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.chunk_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.subchunks_count,
        ));
        size += {
            1usize
                + match &self.client_request_sub_chunk_limit {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::ZigZag32(*_v),
                    ),
                    None => 0usize,
                }
        };
        size += 1usize;
        size += {
            let _len = (&self.cache_metadata).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.cache_metadata)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.serialized_chunk_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LevelChunkPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.chunk_position.encode(buf)?;
        self.dimension_id.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.subchunks_count).encode(buf)?;
        match &self.client_request_sub_chunk_limit {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::ZigZag32(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        self.cache_enabled.encode(buf)?;
        let len = self.cache_metadata.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.cache_metadata {
            item.encode(buf)?;
        }
        let bytes = (&self.serialized_chunk_data).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let chunk_position = <ChunkPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimension_id = <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let subchunks_count =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let client_request_sub_chunk_limit = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0,
                )
            } else {
                None
            }
        };
        let cache_enabled = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let cache_metadata = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <LevelChunkPacketPayloadSubChunkMetadata as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let serialized_chunk_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self {
            chunk_position,
            dimension_id,
            subchunks_count,
            client_request_sub_chunk_limit,
            cache_enabled,
            cache_metadata,
            serialized_chunk_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetCommandsEnabledPacket {
    pub commands_enabled: bool,
}
impl crate::bedrock::codec::BedrockSized for SetCommandsEnabledPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetCommandsEnabledPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.commands_enabled.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let commands_enabled = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { commands_enabled })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetDifficultyPacket {
    pub difficulty: u32,
}
impl crate::bedrock::codec::BedrockSized for SetDifficultyPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.difficulty,
        ));
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetDifficultyPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.difficulty).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let difficulty =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { difficulty })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ChangeDimensionPacket {
    pub dimension_id: DimensionType,
    pub position: Vec3,
    pub respawn: bool,
    pub loading_screen_id: Option<u32>,
}
impl crate::bedrock::codec::BedrockSized for ChangeDimensionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += 1usize;
        size += {
            1usize
                + match &self.loading_screen_id {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ChangeDimensionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.dimension_id.encode(buf)?;
        self.position.encode(buf)?;
        self.respawn.encode(buf)?;
        match &self.loading_screen_id {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::U32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let dimension_id = <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let respawn = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let loading_screen_id = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        Ok(Self {
            dimension_id,
            position,
            respawn,
            loading_screen_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetPlayerGameTypePacket {
    pub player_game_type: EnumsGameType,
}
impl crate::bedrock::codec::BedrockSized for SetPlayerGameTypePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_game_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetPlayerGameTypePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_game_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_game_type =
            <EnumsGameType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { player_game_type })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerListPacket {
    pub entries: Vec<PlayerListPacketEntriesItem>,
}
impl crate::bedrock::codec::BedrockSized for PlayerListPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.entries).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.entries)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerListPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.entries.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.entries {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let entries = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <PlayerListPacketEntriesItem as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self { entries })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SimpleEventPacket {
    pub type_: EnumsSimpleEventPacketPayloadSubtype,
}
impl crate::bedrock::codec::BedrockSized for SimpleEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.type_);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SimpleEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.type_.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let type_ =
            <EnumsSimpleEventPacketPayloadSubtype as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self { type_ })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LegacyTelemetryEventPacket {
    pub target_actor_id: ActorUniqueId,
    pub event_type: EnumsLegacyTelemetryEventPacketPayloadType,
    pub use_player_id: bool,
    pub event_data: LegacyTelemetryEventPacketEventData,
}
impl crate::bedrock::codec::BedrockSized for LegacyTelemetryEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.event_type);
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.event_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LegacyTelemetryEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.event_type.encode(buf)?;
        self.use_player_id.encode(buf)?;
        self.event_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_type = <EnumsLegacyTelemetryEventPacketPayloadType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let use_player_id = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_data =
            <LegacyTelemetryEventPacketEventData as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self {
            target_actor_id,
            event_type,
            use_player_id,
            event_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SpawnExperienceOrbPacket {
    pub position: Vec3,
    pub xp_value: i32,
}
impl crate::bedrock::codec::BedrockSized for SpawnExperienceOrbPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.xp_value),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SpawnExperienceOrbPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.position.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.xp_value).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let xp_value =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { position, xp_value })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundMapItemDataPacket {
    pub map_id: ActorUniqueId,
    pub dimension: u8,
    pub is_locked: bool,
    pub map_origin: BlockPos,
    pub creation_map_i_ds: Option<Vec<ActorUniqueId>>,
    pub scale: Option<i8>,
    pub tracked_actor_i_ds: Option<Vec<MapItemTrackedActorUniqueId>>,
    pub decorations: Option<Vec<MapDecoration>>,
    pub width: Option<i32>,
    pub height: Option<i32>,
    pub start_x: Option<i32>,
    pub start_y: Option<i32>,
    pub pixels: Option<Vec<u32>>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundMapItemDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.map_id);
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.map_origin);
        size += {
            1usize
                + match &self.creation_map_i_ds {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v)
                            .iter()
                            .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                            .sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.scale {
                    Some(_v) => 1usize,
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.tracked_actor_i_ds {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v)
                            .iter()
                            .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                            .sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.decorations {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v)
                            .iter()
                            .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                            .sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.width {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::ZigZag32(*_v),
                    ),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.height {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::ZigZag32(*_v),
                    ),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.start_x {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::ZigZag32(*_v),
                    ),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.start_y {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::ZigZag32(*_v),
                    ),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.pixels {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v).iter().map(|_item| 4usize).sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundMapItemDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.map_id.encode(buf)?;
        self.dimension.encode(buf)?;
        self.is_locked.encode(buf)?;
        self.map_origin.encode(buf)?;
        match &self.creation_map_i_ds {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    item.encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        match &self.scale {
            Some(v) => {
                buf.put_u8(1);
                (*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.tracked_actor_i_ds {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    item.encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        match &self.decorations {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    item.encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        match &self.width {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::ZigZag32(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.height {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::ZigZag32(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.start_x {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::ZigZag32(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.start_y {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::ZigZag32(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.pixels {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    crate::bedrock::codec::U32LE(*item).encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let map_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimension = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_locked = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let map_origin = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let creation_map_i_ds = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec.push(
                            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        let scale = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<i8 as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        let tracked_actor_i_ds = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec
                            .push(
                                <MapItemTrackedActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(
                                    buf,
                                    (),
                                )?,
                            );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        let decorations = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec.push(
                            <MapDecoration as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        let width = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0,
                )
            } else {
                None
            }
        };
        let height = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0,
                )
            } else {
                None
            }
        };
        let start_x = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0,
                )
            } else {
                None
            }
        };
        let start_y = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0,
                )
            } else {
                None
            }
        };
        let pixels = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec
                            .push(
                                <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                                        buf,
                                        (),
                                    )?
                                    .0,
                            );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        Ok(Self {
            map_id,
            dimension,
            is_locked,
            map_origin,
            creation_map_i_ds,
            scale,
            tracked_actor_i_ds,
            decorations,
            width,
            height,
            start_x,
            start_y,
            pixels,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MapInfoRequestPacket {
    pub map_unique_id: ActorUniqueId,
    pub client_pixels_list: Vec<MapInfoRequestPacketAnonClientPixelsProxy>,
}
impl crate::bedrock::codec::BedrockSized for MapInfoRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.map_unique_id);
        size += {
            let _len = (&self.client_pixels_list).len();
            4usize
                + (&self.client_pixels_list)
                    .iter()
                    .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                    .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MapInfoRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.map_unique_id.encode(buf)?;
        let len = self.client_pixels_list.len();
        crate::bedrock::codec::U32LE(len as u32).encode(buf)?;
        for item in &self.client_pixels_list {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let map_unique_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let client_pixels_list = {
            let len =
                (<crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <MapInfoRequestPacketAnonClientPixelsProxy as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            map_unique_id,
            client_pixels_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RequestChunkRadiusPacket {
    pub chunk_radius: i32,
    pub max_chunk_radius: u8,
}
impl crate::bedrock::codec::BedrockSized for RequestChunkRadiusPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.chunk_radius),
        );
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RequestChunkRadiusPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.chunk_radius).encode(buf)?;
        self.max_chunk_radius.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let chunk_radius =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let max_chunk_radius = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            chunk_radius,
            max_chunk_radius,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ChunkRadiusUpdatedPacket {
    pub chunk_radius: i32,
}
impl crate::bedrock::codec::BedrockSized for ChunkRadiusUpdatedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.chunk_radius),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ChunkRadiusUpdatedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.chunk_radius).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let chunk_radius =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { chunk_radius })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct GameRulesChangedPacket {
    pub rule_data: GameRulesChangedPacketData,
}
impl crate::bedrock::codec::BedrockSized for GameRulesChangedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rule_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for GameRulesChangedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.rule_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let rule_data =
            <GameRulesChangedPacketData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { rule_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraPacket {
    pub camera_id: ActorUniqueId,
    pub target_player_id: ActorUniqueId,
}
impl crate::bedrock::codec::BedrockSized for CameraPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.camera_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_player_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.camera_id.encode(buf)?;
        self.target_player_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let target_player_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            camera_id,
            target_player_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BossEventPacket {
    pub target_actor_id: ActorUniqueId,
    pub player_id: ActorUniqueId,
    pub event_type: EnumsBossEventUpdateType,
    pub name: String,
    pub filtered_name: String,
    pub health_percent: f32,
    pub color: EnumsBossBarColor,
    pub overlay: EnumsBossBarOverlay,
}
impl crate::bedrock::codec::BedrockSized for BossEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.event_type);
        size += {
            let _len = (&self.name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.filtered_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.color);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.overlay);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BossEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.player_id.encode(buf)?;
        self.event_type.encode(buf)?;
        let bytes = (&self.name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.filtered_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::F32LE(self.health_percent).encode(buf)?;
        self.color.encode(buf)?;
        self.overlay.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let player_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let event_type =
            <EnumsBossEventUpdateType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let filtered_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let health_percent =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let color = <EnumsBossBarColor as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let overlay =
            <EnumsBossBarOverlay as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_actor_id,
            player_id,
            event_type,
            name,
            filtered_name,
            health_percent,
            color,
            overlay,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ShowCreditsPacket {
    pub player_runtime_id: ActorRuntimeId,
    pub credits_state: i32,
}
impl crate::bedrock::codec::BedrockSized for ShowCreditsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.credits_state),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ShowCreditsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_runtime_id.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.credits_state).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let credits_state =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            player_runtime_id,
            credits_state,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AvailableCommandsPacket {
    pub enum_values: Vec<String>,
    pub chained_subcommand_values: Vec<String>,
    pub post_fixes: Vec<String>,
    pub enum_data: Vec<AvailableCommandsPacketPayloadEnumData>,
    pub chained_subcommand_data: Vec<AvailableCommandsPacketPayloadChainedSubcommandData>,
    pub commands: Vec<AvailableCommandsPacketCommandDatajson>,
    pub soft_enums: Vec<AvailableCommandsPacketPayloadSoftEnumData>,
    pub constraints: Vec<AvailableCommandsPacketPayloadConstrainedValueData>,
}
impl crate::bedrock::codec::BedrockSized for AvailableCommandsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.enum_values).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.enum_values)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size += {
            let _len = (&self.chained_subcommand_values).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.chained_subcommand_values)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size += {
            let _len = (&self.post_fixes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.post_fixes)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size += {
            let _len = (&self.enum_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.enum_data)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.chained_subcommand_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.chained_subcommand_data)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.commands).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.commands)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.soft_enums).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.soft_enums)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.constraints).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.constraints)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AvailableCommandsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.enum_values.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.enum_values {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        let len = self.chained_subcommand_values.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.chained_subcommand_values {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        let len = self.post_fixes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.post_fixes {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        let len = self.enum_data.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.enum_data {
            item.encode(buf)?;
        }
        let len = self.chained_subcommand_data.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.chained_subcommand_data {
            item.encode(buf)?;
        }
        let len = self.commands.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.commands {
            item.encode(buf)?;
        }
        let len = self.soft_enums.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.soft_enums {
            item.encode(buf)?;
        }
        let len = self.constraints.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.constraints {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let enum_values = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        let chained_subcommand_values = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        let post_fixes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        let enum_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <AvailableCommandsPacketPayloadEnumData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let chained_subcommand_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <AvailableCommandsPacketPayloadChainedSubcommandData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let commands = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <AvailableCommandsPacketCommandDatajson as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let soft_enums = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <AvailableCommandsPacketPayloadSoftEnumData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let constraints = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <AvailableCommandsPacketPayloadConstrainedValueData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            enum_values,
            chained_subcommand_values,
            post_fixes,
            enum_data,
            chained_subcommand_data,
            commands,
            soft_enums,
            constraints,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CommandRequestPacket {
    pub command: String,
    pub origin: CommandOriginDatajson,
    pub is_internal: bool,
    pub version: String,
}
impl crate::bedrock::codec::BedrockSized for CommandRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.command).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.origin);
        size += 1usize;
        size += {
            let _len = (&self.version).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CommandRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.command).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.origin.encode(buf)?;
        self.is_internal.encode(buf)?;
        let bytes = (&self.version).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let command = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let origin =
            <CommandOriginDatajson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_internal = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let version = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            command,
            origin,
            is_internal,
            version,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CommandBlockUpdatePacket {
    pub target: CommandBlockUpdatePacketTarget,
    pub command: String,
    pub last_output: String,
    pub name: String,
    pub filtered_name: String,
    pub track_output: bool,
    pub tick_delay: i32,
    pub execute_on_first_tick: bool,
}
impl crate::bedrock::codec::BedrockSized for CommandBlockUpdatePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target);
        size += {
            let _len = (&self.command).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.last_output).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.filtered_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += 4usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CommandBlockUpdatePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target.encode(buf)?;
        let bytes = (&self.command).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.last_output).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.filtered_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.track_output.encode(buf)?;
        crate::bedrock::codec::I32LE(self.tick_delay).encode(buf)?;
        self.execute_on_first_tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target =
            <CommandBlockUpdatePacketTarget as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let command = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let last_output = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let filtered_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let track_output = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tick_delay =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let execute_on_first_tick = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target,
            command,
            last_output,
            name,
            filtered_name,
            track_output,
            tick_delay,
            execute_on_first_tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CommandOutputPacket {
    pub origin_data: CommandOriginDatajson,
    pub output: CommandOutput,
}
impl crate::bedrock::codec::BedrockSized for CommandOutputPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.origin_data);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.output);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CommandOutputPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.origin_data.encode(buf)?;
        self.output.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let origin_data =
            <CommandOriginDatajson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let output = <CommandOutput as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            origin_data,
            output,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateTradePacket {
    pub container_id: u8,
    pub type_: u8,
    pub size: i32,
    pub trader_tier: i32,
    pub entity_unique_id: ActorUniqueId,
    pub last_trading_player: ActorUniqueId,
    pub display_name: String,
    pub use_new_trade_screen: bool,
    pub using_economy_trade: bool,
    pub data: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for UpdateTradePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.size),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.trader_tier),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_unique_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.last_trading_player);
        size += {
            let _len = (&self.display_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateTradePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        self.type_.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.size).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.trader_tier).encode(buf)?;
        self.entity_unique_id.encode(buf)?;
        self.last_trading_player.encode(buf)?;
        let bytes = (&self.display_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.use_new_trade_screen.encode(buf)?;
        self.using_economy_trade.encode(buf)?;
        self.data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let type_ = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let size =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let trader_tier =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let entity_unique_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let last_trading_player =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let display_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let use_new_trade_screen = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let using_economy_trade = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            container_id,
            type_,
            size,
            trader_tier,
            entity_unique_id,
            last_trading_player,
            display_name,
            use_new_trade_screen,
            using_economy_trade,
            data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateEquipPacket {
    pub container_id: u8,
    pub type_: u8,
    pub size: i32,
    pub entity_unique_id: ActorUniqueId,
    pub data: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for UpdateEquipPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.size),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_unique_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateEquipPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.container_id.encode(buf)?;
        self.type_.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.size).encode(buf)?;
        self.entity_unique_id.encode(buf)?;
        self.data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let container_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let type_ = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let size =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let entity_unique_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            container_id,
            type_,
            size,
            entity_unique_id,
            data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePackDataInfoPacket {
    pub resource_name: String,
    pub chunk_size: u32,
    pub numberof_chunks: u32,
    pub file_size: u64,
    pub file_hash: Vec<u8>,
    pub is_premium_pack: bool,
    pub pack_type: u8,
}
impl crate::bedrock::codec::BedrockSized for ResourcePackDataInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.resource_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += 4usize;
        size += 8usize;
        size += {
            let _len = (&self.file_hash).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePackDataInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.resource_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::U32LE(self.chunk_size).encode(buf)?;
        crate::bedrock::codec::U32LE(self.numberof_chunks).encode(buf)?;
        crate::bedrock::codec::U64LE(self.file_size).encode(buf)?;
        let bytes = (&self.file_hash).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.is_premium_pack.encode(buf)?;
        self.pack_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let resource_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let chunk_size =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let numberof_chunks =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let file_size =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let file_hash = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        let is_premium_pack = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let pack_type = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            resource_name,
            chunk_size,
            numberof_chunks,
            file_size,
            file_hash,
            is_premium_pack,
            pack_type,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePackChunkDataPacket {
    pub resource_name: String,
    pub chunk_id: u32,
    pub byte_offset: u64,
    pub chunk_data: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for ResourcePackChunkDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.resource_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += 8usize;
        size += {
            let _len = (&self.chunk_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePackChunkDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.resource_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::U32LE(self.chunk_id).encode(buf)?;
        crate::bedrock::codec::U64LE(self.byte_offset).encode(buf)?;
        let bytes = (&self.chunk_data).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let resource_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let chunk_id =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let byte_offset =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let chunk_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self {
            resource_name,
            chunk_id,
            byte_offset,
            chunk_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePackChunkRequestPacket {
    pub resource_name: String,
    pub chunk: i32,
}
impl crate::bedrock::codec::BedrockSized for ResourcePackChunkRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.resource_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePackChunkRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.resource_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::I32LE(self.chunk).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let resource_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let chunk =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            resource_name,
            chunk,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct TransferPacket {
    pub server_address: String,
    pub server_port: u16,
    pub reload_world: bool,
    pub gatherings_configuration: Option<ServerConfigurationGatheringsConfigurationJoinInfo>,
}
impl crate::bedrock::codec::BedrockSized for TransferPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.server_address).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 2usize;
        size += 1usize;
        size += {
            1usize
                + match &self.gatherings_configuration {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for TransferPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.server_address).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::U16LE(self.server_port).encode(buf)?;
        self.reload_world.encode(buf)?;
        match &self.gatherings_configuration {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let server_address = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let server_port =
            <crate::bedrock::codec::U16LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let reload_world = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let gatherings_configuration = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ServerConfigurationGatheringsConfigurationJoinInfo as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            server_address,
            server_port,
            reload_world,
            gatherings_configuration,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlaySoundPacket {
    pub name: String,
    pub position: BlockPos,
    pub volume: f32,
    pub pitch: f32,
    pub loop_count: i32,
    pub server_sound_handle: Option<ServerSoundHandle>,
}
impl crate::bedrock::codec::BedrockSized for PlaySoundPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += 4usize;
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.loop_count),
        );
        size += {
            1usize
                + match &self.server_sound_handle {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlaySoundPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.position.encode(buf)?;
        crate::bedrock::codec::F32LE(self.volume).encode(buf)?;
        crate::bedrock::codec::F32LE(self.pitch).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.loop_count).encode(buf)?;
        match &self.server_sound_handle {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let volume =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let pitch =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let loop_count =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let server_sound_handle = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<ServerSoundHandle as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?)
            } else {
                None
            }
        };
        Ok(Self {
            name,
            position,
            volume,
            pitch,
            loop_count,
            server_sound_handle,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct StopSoundPacket {
    pub sound_name: String,
    pub stop_all_sounds: bool,
    pub stop_music_legacy: bool,
}
impl crate::bedrock::codec::BedrockSized for StopSoundPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.sound_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for StopSoundPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.sound_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.stop_all_sounds.encode(buf)?;
        self.stop_music_legacy.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let sound_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let stop_all_sounds = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let stop_music_legacy = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            sound_name,
            stop_all_sounds,
            stop_music_legacy,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetTitlePacket {
    pub title_type: EnumsSetTitlePacketPayloadTitleType,
    pub title_text: String,
    pub fade_in_time: i32,
    pub stay_time: i32,
    pub fade_out_time: i32,
    pub xuid: String,
    pub platform_online_id: String,
    pub filtered_title_message: String,
}
impl crate::bedrock::codec::BedrockSized for SetTitlePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.title_type);
        size += {
            let _len = (&self.title_text).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.fade_in_time),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.stay_time),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.fade_out_time),
        );
        size += {
            let _len = (&self.xuid).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.platform_online_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.filtered_title_message).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetTitlePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.title_type.encode(buf)?;
        let bytes = (&self.title_text).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::ZigZag32(self.fade_in_time).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.stay_time).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.fade_out_time).encode(buf)?;
        let bytes = (&self.xuid).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.platform_online_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.filtered_title_message).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let title_type =
            <EnumsSetTitlePacketPayloadTitleType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let title_text = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let fade_in_time =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let stay_time =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let fade_out_time =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let xuid = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let platform_online_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let filtered_title_message = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            title_type,
            title_text,
            fade_in_time,
            stay_time,
            fade_out_time,
            xuid,
            platform_online_id,
            filtered_title_message,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddBehaviorTreePacket {
    pub behavior_tree_structure_json: String,
}
impl crate::bedrock::codec::BedrockSized for AddBehaviorTreePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.behavior_tree_structure_json).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddBehaviorTreePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.behavior_tree_structure_json).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let behavior_tree_structure_json = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            behavior_tree_structure_json,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct StructureBlockUpdatePacket {
    pub block_position: BlockPos,
    pub structure_data: StructureEditorData,
    pub trigger: bool,
    pub is_waterlogged: bool,
}
impl crate::bedrock::codec::BedrockSized for StructureBlockUpdatePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.structure_data);
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for StructureBlockUpdatePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        self.structure_data.encode(buf)?;
        self.trigger.encode(buf)?;
        self.is_waterlogged.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let structure_data =
            <StructureEditorData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let trigger = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_waterlogged = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            block_position,
            structure_data,
            trigger,
            is_waterlogged,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ShowStoreOfferPacket {
    pub offer_id: MceUuiDjson,
    pub redirect_type: EnumsShowStoreOfferRedirectType,
}
impl crate::bedrock::codec::BedrockSized for ShowStoreOfferPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.offer_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.redirect_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ShowStoreOfferPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.offer_id.encode(buf)?;
        self.redirect_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let offer_id = <MceUuiDjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let redirect_type =
            <EnumsShowStoreOfferRedirectType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self {
            offer_id,
            redirect_type,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PurchaseReceiptPacket {
    pub purchase_receipts: Vec<String>,
}
impl crate::bedrock::codec::BedrockSized for PurchaseReceiptPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.purchase_receipts).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.purchase_receipts)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PurchaseReceiptPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.purchase_receipts.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.purchase_receipts {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let purchase_receipts = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        Ok(Self { purchase_receipts })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerSkinPacket {
    pub uuid: MceUuiDjson,
    pub serialized_skin: PlayerSkinPacketSerializedSkin,
    pub localized_new_skin_name: String,
    pub localized_old_skin_name: String,
}
impl crate::bedrock::codec::BedrockSized for PlayerSkinPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.uuid);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.serialized_skin);
        size += {
            let _len = (&self.localized_new_skin_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.localized_old_skin_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerSkinPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.uuid.encode(buf)?;
        self.serialized_skin.encode(buf)?;
        let bytes = (&self.localized_new_skin_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.localized_old_skin_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let uuid = <MceUuiDjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let serialized_skin =
            <PlayerSkinPacketSerializedSkin as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let localized_new_skin_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let localized_old_skin_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            uuid,
            serialized_skin,
            localized_new_skin_name,
            localized_old_skin_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SubClientLoginPacket {
    pub sub_client_connection_request: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for SubClientLoginPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.sub_client_connection_request).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SubClientLoginPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.sub_client_connection_request).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let sub_client_connection_request = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self {
            sub_client_connection_request,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AutomationClientConnectPacket {
    pub web_socket_data: WebSocketPacketData,
}
impl crate::bedrock::codec::BedrockSized for AutomationClientConnectPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.web_socket_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AutomationClientConnectPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.web_socket_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let web_socket_data =
            <WebSocketPacketData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { web_socket_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetLastHurtByPacket {
    pub last_hurt_by: EnumsActorType,
}
impl crate::bedrock::codec::BedrockSized for SetLastHurtByPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.last_hurt_by);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetLastHurtByPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.last_hurt_by.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let last_hurt_by =
            <EnumsActorType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { last_hurt_by })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BookEditPacket {
    pub book_slot: i32,
    pub operation: BookEditPacketOperation,
}
impl crate::bedrock::codec::BedrockSized for BookEditPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.book_slot),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.operation);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BookEditPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.book_slot).encode(buf)?;
        self.operation.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let book_slot =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let operation =
            <BookEditPacketOperation as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            book_slot,
            operation,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NpcRequestPacket {
    pub npc_runtime_id: ActorRuntimeId,
    pub request_type: EnumsNpcRequestPacketPayloadRequestType,
    pub actions: String,
    pub action_index: u8,
    pub scene_name: String,
}
impl crate::bedrock::codec::BedrockSized for NpcRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.npc_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.request_type);
        size += {
            let _len = (&self.actions).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += {
            let _len = (&self.scene_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for NpcRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.npc_runtime_id.encode(buf)?;
        self.request_type.encode(buf)?;
        let bytes = (&self.actions).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.action_index.encode(buf)?;
        let bytes = (&self.scene_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let npc_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let request_type = <EnumsNpcRequestPacketPayloadRequestType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let actions = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let action_index = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let scene_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            npc_runtime_id,
            request_type,
            actions,
            action_index,
            scene_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PhotoTransferPacket {
    pub photo_name: String,
    pub photo_data: Vec<u8>,
    pub book_id: String,
    pub type_: EnumsPhotoType,
    pub source_type: EnumsPhotoType,
    pub owner_id: i64,
    pub new_photo_name: String,
}
impl crate::bedrock::codec::BedrockSized for PhotoTransferPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.photo_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.photo_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.book_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.type_);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.source_type);
        size += 8usize;
        size += {
            let _len = (&self.new_photo_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PhotoTransferPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.photo_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.photo_data).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.book_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.type_.encode(buf)?;
        self.source_type.encode(buf)?;
        crate::bedrock::codec::I64LE(self.owner_id).encode(buf)?;
        let bytes = (&self.new_photo_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let photo_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let photo_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        let book_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let type_ = <EnumsPhotoType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let source_type = <EnumsPhotoType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let owner_id =
            <crate::bedrock::codec::I64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let new_photo_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            photo_name,
            photo_data,
            book_id,
            type_,
            source_type,
            owner_id,
            new_photo_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ModalFormRequestPacket {
    pub form_id: u32,
    pub form_uijson: String,
}
impl crate::bedrock::codec::BedrockSized for ModalFormRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.form_id,
        ));
        size += {
            let _len = (&self.form_uijson).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ModalFormRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.form_id).encode(buf)?;
        let bytes = (&self.form_uijson).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let form_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let form_uijson = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            form_id,
            form_uijson,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ModalFormResponsePacket {
    pub form_id: u32,
    pub json_response: Option<JsonValuejson>,
    pub form_cancel_reason: Option<EnumsModalFormCancelReason>,
}
impl crate::bedrock::codec::BedrockSized for ModalFormResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.form_id,
        ));
        size += {
            1usize
                + match &self.json_response {
                    Some(_v) => {
                        let _len = (_v).as_bytes().len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + _len
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.form_cancel_reason {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ModalFormResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.form_id).encode(buf)?;
        match &self.json_response {
            Some(v) => {
                buf.put_u8(1);
                let bytes = (v).as_bytes();
                let len = bytes.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                buf.put_slice(bytes);
            }
            None => buf.put_u8(0),
        }
        match &self.form_cancel_reason {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let form_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let json_response = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let res: JsonValuejson = {
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    };
                    res
                })
            } else {
                None
            }
        };
        let form_cancel_reason = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <EnumsModalFormCancelReason as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            form_id,
            json_response,
            form_cancel_reason,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerSettingsRequestPacket {}
impl crate::bedrock::codec::BedrockSized for ServerSettingsRequestPacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerSettingsRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerSettingsResponsePacket {
    pub form_id: u32,
    pub form_uijson: String,
}
impl crate::bedrock::codec::BedrockSized for ServerSettingsResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.form_id,
        ));
        size += {
            let _len = (&self.form_uijson).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerSettingsResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.form_id).encode(buf)?;
        let bytes = (&self.form_uijson).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let form_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let form_uijson = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            form_id,
            form_uijson,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ShowProfilePacket {
    pub player_xuid: String,
}
impl crate::bedrock::codec::BedrockSized for ShowProfilePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.player_xuid).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ShowProfilePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.player_xuid).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_xuid = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self { player_xuid })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetDefaultGameTypePacket {
    pub default_game_type: SetDefaultGameTypePacketDefaultGameType,
}
impl crate::bedrock::codec::BedrockSized for SetDefaultGameTypePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.default_game_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetDefaultGameTypePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.default_game_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let default_game_type = <SetDefaultGameTypePacketDefaultGameType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self { default_game_type })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RemoveObjectivePacket {
    pub objective_name: String,
}
impl crate::bedrock::codec::BedrockSized for RemoveObjectivePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.objective_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RemoveObjectivePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.objective_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let objective_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self { objective_name })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetDisplayObjectivePacket {
    pub display_slot_name: String,
    pub objective_name: String,
    pub objective_display_name: String,
    pub criteria_name: String,
    pub sort_order: i32,
}
impl crate::bedrock::codec::BedrockSized for SetDisplayObjectivePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.display_slot_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.objective_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.objective_display_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.criteria_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.sort_order),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetDisplayObjectivePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.display_slot_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.objective_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.objective_display_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.criteria_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::ZigZag32(self.sort_order).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let display_slot_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let objective_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let objective_display_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let criteria_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let sort_order =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            display_slot_name,
            objective_name,
            objective_display_name,
            criteria_name,
            sort_order,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetScorePacket {
    pub score_info: Vec<SetScorePacketScoreInfoItem>,
}
impl crate::bedrock::codec::BedrockSized for SetScorePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.score_info).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.score_info)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetScorePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.score_info.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.score_info {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let score_info = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <SetScorePacketScoreInfoItem as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self { score_info })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LabTablePacket {
    pub type_: EnumsLabTablePacketPayloadType,
    pub position: BlockPos,
    pub reaction: EnumsLabTableReactionType,
}
impl crate::bedrock::codec::BedrockSized for LabTablePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.type_);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.reaction);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LabTablePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.type_.encode(buf)?;
        self.position.encode(buf)?;
        self.reaction.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let type_ =
            <EnumsLabTablePacketPayloadType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let reaction =
            <EnumsLabTableReactionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            type_,
            position,
            reaction,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateBlockSyncedPacket {
    pub block_position: BlockPos,
    pub block_runtime_id: u32,
    pub flags: u32,
    pub layer: u32,
    pub unique_actor_id: u64,
    pub actor_sync_message: u64,
}
impl crate::bedrock::codec::BedrockSized for UpdateBlockSyncedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.block_runtime_id,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.flags,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.layer,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::VarULong(self.unique_actor_id),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::VarULong(self.actor_sync_message),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateBlockSyncedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.block_runtime_id).encode(buf)?;
        crate::bedrock::codec::VarUInt(self.flags).encode(buf)?;
        crate::bedrock::codec::VarUInt(self.layer).encode(buf)?;
        crate::bedrock::codec::VarULong(self.unique_actor_id).encode(buf)?;
        crate::bedrock::codec::VarULong(self.actor_sync_message).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let block_runtime_id =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let flags =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let layer =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let unique_actor_id =
            <crate::bedrock::codec::VarULong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let actor_sync_message =
            <crate::bedrock::codec::VarULong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            block_position,
            block_runtime_id,
            flags,
            layer,
            unique_actor_id,
            actor_sync_message,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MoveActorDeltaPacket {
    pub move_data: MoveActorDeltaData,
}
impl crate::bedrock::codec::BedrockSized for MoveActorDeltaPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.move_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MoveActorDeltaPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.move_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let move_data =
            <MoveActorDeltaData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { move_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetScoreboardIdentityPacket {
    pub scoreboard_identity_packet_type: EnumsScoreboardIdentityPacketType,
    pub scoreboard_identity_info: Vec<ScoreboardIdentityPacketInfo>,
}
impl crate::bedrock::codec::BedrockSized for SetScoreboardIdentityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &self.scoreboard_identity_packet_type,
        );
        size += {
            let _len = (&self.scoreboard_identity_info).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.scoreboard_identity_info)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetScoreboardIdentityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.scoreboard_identity_packet_type.encode(buf)?;
        let len = self.scoreboard_identity_info.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.scoreboard_identity_info {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let scoreboard_identity_packet_type =
            <EnumsScoreboardIdentityPacketType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let scoreboard_identity_info = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ScoreboardIdentityPacketInfo as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self {
            scoreboard_identity_packet_type,
            scoreboard_identity_info,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetLocalPlayerAsInitializedPacket {
    pub player_id: ActorRuntimeId,
}
impl crate::bedrock::codec::BedrockSized for SetLocalPlayerAsInitializedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetLocalPlayerAsInitializedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_id = <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { player_id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateSoftEnumPacket {
    pub enum_name: String,
    pub values: Vec<String>,
    pub update_type: EnumsSoftEnumUpdateType,
}
impl crate::bedrock::codec::BedrockSized for UpdateSoftEnumPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.enum_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.values).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.values)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.update_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateSoftEnumPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.enum_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let len = self.values.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.values {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        self.update_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let enum_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let values = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        let update_type =
            <EnumsSoftEnumUpdateType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            enum_name,
            values,
            update_type,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NetworkStackLatencyPacket {
    pub creation_time: u64,
    pub is_from_server: bool,
}
impl crate::bedrock::codec::BedrockSized for NetworkStackLatencyPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 8usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for NetworkStackLatencyPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U64LE(self.creation_time).encode(buf)?;
        self.is_from_server.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let creation_time =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let is_from_server = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            creation_time,
            is_from_server,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SpawnParticleEffectPacket {
    pub dimension_id: u8,
    pub actor_id: ActorUniqueId,
    pub position: Vec3,
    pub effect_name: String,
    pub molang_variables: Option<MolangVariableMapjson>,
}
impl crate::bedrock::codec::BedrockSized for SpawnParticleEffectPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += {
            let _len = (&self.effect_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            1usize
                + match &self.molang_variables {
                    Some(_v) => {
                        let _len = (_v).as_bytes().len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + _len
                    }
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SpawnParticleEffectPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.dimension_id.encode(buf)?;
        self.actor_id.encode(buf)?;
        self.position.encode(buf)?;
        let bytes = (&self.effect_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        match &self.molang_variables {
            Some(v) => {
                buf.put_u8(1);
                let bytes = (v).as_bytes();
                let len = bytes.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                buf.put_slice(bytes);
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let dimension_id = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let effect_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let molang_variables = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let res: MolangVariableMapjson = {
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    };
                    res
                })
            } else {
                None
            }
        };
        Ok(Self {
            dimension_id,
            actor_id,
            position,
            effect_name,
            molang_variables,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AvailableActorIdentifiersPacket {
    pub identifier_list: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for AvailableActorIdentifiersPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.identifier_list);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AvailableActorIdentifiersPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.identifier_list.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let identifier_list =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { identifier_list })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NetworkChunkPublisherUpdatePacket {
    pub newpositionforview: BlockPos,
    pub newradiusforview: u32,
    pub server_built_chunks_list: Vec<ChunkPos>,
}
impl crate::bedrock::codec::BedrockSized for NetworkChunkPublisherUpdatePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.newpositionforview);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.newradiusforview,
        ));
        size += {
            let _len = (&self.server_built_chunks_list).len();
            4usize
                + (&self.server_built_chunks_list)
                    .iter()
                    .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                    .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for NetworkChunkPublisherUpdatePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.newpositionforview.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.newradiusforview).encode(buf)?;
        let len = self.server_built_chunks_list.len();
        crate::bedrock::codec::U32LE(len as u32).encode(buf)?;
        for item in &self.server_built_chunks_list {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let newpositionforview =
            <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let newradiusforview =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let server_built_chunks_list = {
            let len =
                (<crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(<ChunkPos as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?);
            }
            tmp_vec
        };
        Ok(Self {
            newpositionforview,
            newradiusforview,
            server_built_chunks_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct BiomeDefinitionListPacket {
    pub mapof_biomenamestodata: Vec<BiomeDefinitionListPacketMapofBiomenamestodataItem>,
    pub stringlist: BiomeStringList,
}
impl crate::bedrock::codec::BedrockSized for BiomeDefinitionListPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.mapof_biomenamestodata).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.mapof_biomenamestodata)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.stringlist);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for BiomeDefinitionListPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.mapof_biomenamestodata.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.mapof_biomenamestodata {
            item.encode(buf)?;
        }
        self.stringlist.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let mapof_biomenamestodata = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <BiomeDefinitionListPacketMapofBiomenamestodataItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let stringlist = <BiomeStringList as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            mapof_biomenamestodata,
            stringlist,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LevelSoundEventPacket {
    pub sound_event: SoundEventIdentifierjson,
    pub position: Vec3,
    pub data: i32,
    pub actor_identifier: String,
    pub is_baby: bool,
    pub is_global: bool,
    pub actor_unique_id: i64,
    pub fire_at_position: Option<Vec3>,
}
impl crate::bedrock::codec::BedrockSized for LevelSoundEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.sound_event).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.data),
        );
        size += {
            let _len = (&self.actor_identifier).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += 1usize;
        size += 8usize;
        size += {
            1usize
                + match &self.fire_at_position {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LevelSoundEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.sound_event).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.position.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.data).encode(buf)?;
        let bytes = (&self.actor_identifier).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.is_baby.encode(buf)?;
        self.is_global.encode(buf)?;
        crate::bedrock::codec::I64LE(self.actor_unique_id).encode(buf)?;
        match &self.fire_at_position {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let sound_event = {
            let res: SoundEventIdentifierjson = {
                let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0) as usize;
                if buf.remaining() < len {
                    return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                        declared: len,
                        available: buf.remaining(),
                    });
                }
                let mut bytes = vec![0u8; len];
                buf.copy_to_slice(&mut bytes);
                crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
            };
            res
        };
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let actor_identifier = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let is_baby = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_global = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_unique_id =
            <crate::bedrock::codec::I64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let fire_at_position = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<Vec3 as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        Ok(Self {
            sound_event,
            position,
            data,
            actor_identifier,
            is_baby,
            is_global,
            actor_unique_id,
            fire_at_position,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LevelEventGenericPacket {
    pub event_id: i32,
    pub __ctd__: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for LevelEventGenericPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.event_id),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.__ctd__);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LevelEventGenericPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.event_id).encode(buf)?;
        self.__ctd__.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let event_id =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let __ctd__ =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { event_id, __ctd__ })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LecternUpdatePacket {
    pub newpagetoshow: u8,
    pub total_pages: u8,
    pub positionof_lecterntoupdate: BlockPos,
}
impl crate::bedrock::codec::BedrockSized for LecternUpdatePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.positionof_lecterntoupdate);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LecternUpdatePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.newpagetoshow.encode(buf)?;
        self.total_pages.encode(buf)?;
        self.positionof_lecterntoupdate.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let newpagetoshow = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let total_pages = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let positionof_lecterntoupdate =
            <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            newpagetoshow,
            total_pages,
            positionof_lecterntoupdate,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientCacheStatusPacket {
    pub iscachesupported: bool,
}
impl crate::bedrock::codec::BedrockSized for ClientCacheStatusPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientCacheStatusPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.iscachesupported.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let iscachesupported = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { iscachesupported })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct OnScreenTextureAnimationPacket {
    pub effect_id: u32,
}
impl crate::bedrock::codec::BedrockSized for OnScreenTextureAnimationPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for OnScreenTextureAnimationPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U32LE(self.effect_id).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let effect_id =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self { effect_id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MapCreateLockedCopyPacket {
    pub original_map_id: ActorUniqueId,
    pub new_map_id: ActorUniqueId,
}
impl crate::bedrock::codec::BedrockSized for MapCreateLockedCopyPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.original_map_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.new_map_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MapCreateLockedCopyPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.original_map_id.encode(buf)?;
        self.new_map_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let original_map_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let new_map_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            original_map_id,
            new_map_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct StructureTemplateDataRequestPacket {
    pub structure_name: String,
    pub structure_position: BlockPos,
    pub structure_settings: StructureSettings,
    pub requested_operation: EnumsStructureTemplateRequestOperation,
}
impl crate::bedrock::codec::BedrockSized for StructureTemplateDataRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.structure_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.structure_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.structure_settings);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.requested_operation);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for StructureTemplateDataRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.structure_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.structure_position.encode(buf)?;
        self.structure_settings.encode(buf)?;
        self.requested_operation.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let structure_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let structure_position =
            <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let structure_settings =
            <StructureSettings as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let requested_operation = <EnumsStructureTemplateRequestOperation as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            structure_name,
            structure_position,
            structure_settings,
            requested_operation,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct StructureTemplateDataResponsePacket {
    pub structure_name: String,
    pub structures_nbt: crate::bedrock::codec::Nbt,
    pub response_type: EnumsStructureTemplateResponseType,
}
impl crate::bedrock::codec::BedrockSized for StructureTemplateDataResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.structure_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.structures_nbt);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.response_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for StructureTemplateDataResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.structure_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.structures_nbt.encode(buf)?;
        self.response_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let structure_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let structures_nbt =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let response_type =
            <EnumsStructureTemplateResponseType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self {
            structure_name,
            structures_nbt,
            response_type,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientCacheBlobStatusPacket {
    pub missing_ids: Vec<u64>,
    pub found_ids: Vec<u64>,
}
impl crate::bedrock::codec::BedrockSized for ClientCacheBlobStatusPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.missing_ids).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.missing_ids)
                .iter()
                .map(|_item| 8usize)
                .sum::<usize>()
        };
        size += {
            let _len = (&self.found_ids).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.found_ids).iter().map(|_item| 8usize).sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientCacheBlobStatusPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.missing_ids.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.missing_ids {
            crate::bedrock::codec::U64LE(*item).encode(buf)?;
        }
        let len = self.found_ids.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.found_ids {
            crate::bedrock::codec::U64LE(*item).encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let missing_ids = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                );
            }
            tmp_vec
        };
        let found_ids = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                );
            }
            tmp_vec
        };
        Ok(Self {
            missing_ids,
            found_ids,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientCacheMissResponsePacket {
    pub missing_blobs: Vec<MissingBlobData>,
}
impl crate::bedrock::codec::BedrockSized for ClientCacheMissResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.missing_blobs).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.missing_blobs)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientCacheMissResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.missing_blobs.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.missing_blobs {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let missing_blobs = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <MissingBlobData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        Ok(Self { missing_blobs })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct EducationSettingsPacket {
    pub education_level_settings: EducationLevelSettings,
}
impl crate::bedrock::codec::BedrockSized for EducationSettingsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.education_level_settings);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for EducationSettingsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.education_level_settings.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let education_level_settings =
            <EducationLevelSettings as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            education_level_settings,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct EmotePacket {
    pub actor_runtime_id: ActorRuntimeId,
    pub emote_id: String,
    pub emote_length_ticks: u32,
    pub xuid: String,
    pub platform_id: String,
    pub flags: u8,
}
impl crate::bedrock::codec::BedrockSized for EmotePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_runtime_id);
        size += {
            let _len = (&self.emote_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.emote_length_ticks,
        ));
        size += {
            let _len = (&self.xuid).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.platform_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for EmotePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.actor_runtime_id.encode(buf)?;
        let bytes = (&self.emote_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::VarUInt(self.emote_length_ticks).encode(buf)?;
        let bytes = (&self.xuid).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.platform_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.flags.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let actor_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let emote_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let emote_length_ticks =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let xuid = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let platform_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let flags = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            actor_runtime_id,
            emote_id,
            emote_length_ticks,
            xuid,
            platform_id,
            flags,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MultiplayerSettingsPacket {
    pub packet_type: EnumsMultiplayerSettingsPacketType,
}
impl crate::bedrock::codec::BedrockSized for MultiplayerSettingsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.packet_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MultiplayerSettingsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.packet_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let packet_type =
            <EnumsMultiplayerSettingsPacketType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self { packet_type })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SettingsCommandPacket {
    pub command: String,
    pub suppress_output: bool,
}
impl crate::bedrock::codec::BedrockSized for SettingsCommandPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.command).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SettingsCommandPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.command).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.suppress_output.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let command = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let suppress_output = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            command,
            suppress_output,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AnvilDamagePacket {
    pub block_position: BlockPos,
}
impl crate::bedrock::codec::BedrockSized for AnvilDamagePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.block_position);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AnvilDamagePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.block_position.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let block_position = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { block_position })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CompletedUsingItemPacket {
    pub item_id: i16,
    pub item_use_method: i32,
}
impl crate::bedrock::codec::BedrockSized for CompletedUsingItemPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 2usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CompletedUsingItemPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I16LE(self.item_id).encode(buf)?;
        crate::bedrock::codec::I32LE(self.item_use_method).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let item_id =
            <crate::bedrock::codec::I16LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let item_use_method =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            item_id,
            item_use_method,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NetworkSettingsPacket {
    pub compression_threshold: u16,
    pub compression_algorithm: EnumsPacketCompressionAlgorithm,
    pub client_throttle_enabled: bool,
    pub client_throttle_threshold: u8,
    pub client_throttle_scalar: f32,
}
impl crate::bedrock::codec::BedrockSized for NetworkSettingsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 2usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.compression_algorithm);
        size += 1usize;
        size += 1usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for NetworkSettingsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U16LE(self.compression_threshold).encode(buf)?;
        self.compression_algorithm.encode(buf)?;
        self.client_throttle_enabled.encode(buf)?;
        self.client_throttle_threshold.encode(buf)?;
        crate::bedrock::codec::F32LE(self.client_throttle_scalar).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let compression_threshold =
            <crate::bedrock::codec::U16LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let compression_algorithm =
            <EnumsPacketCompressionAlgorithm as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let client_throttle_enabled =
            <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let client_throttle_threshold =
            <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let client_throttle_scalar =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            compression_threshold,
            compression_algorithm,
            client_throttle_enabled,
            client_throttle_threshold,
            client_throttle_scalar,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerAuthInputPacket {
    pub player_rotation: Vec2,
    pub position: Vec3,
    pub move_vector: Vec2,
    pub player_head_rotation: f32,
    pub input_data: Option<Vec<EnumsPlayerAuthInputPacketPayloadInputData>>,
    pub input_mode: EnumsInputMode,
    pub play_mode: EnumsClientPlayMode,
    pub new_interaction_model: EnumsNewInteractionModel,
    pub interact_rotation: Vec2,
    pub client_tick: PlayerInputTick,
    pub pos_delta: Vec3,
    pub item_use_transaction: Option<Option<PackedItemUseLegacyInventoryTransaction>>,
    pub item_stack_request: Option<Option<ItemStackRequestCerealRequestData>>,
    pub player_block_actions: Option<Option<Vec<PlayerBlockActionData>>>,
    pub vehicle_rotation: Option<Option<Vec2>>,
    pub client_predicted_vehicle: Option<Option<ActorUniqueId>>,
    pub analog_move_vector: Vec2,
    pub camera_orientation: Vec3,
    pub raw_move_vector: Vec2,
}
impl crate::bedrock::codec::BedrockSized for PlayerAuthInputPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_rotation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.move_vector);
        size += 4usize;
        size += {
            1usize
                + match &self.input_data {
                    Some(_v) => {
                        let _len = (_v).len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + (_v)
                            .iter()
                            .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                            .sum::<usize>()
                    }
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.input_mode);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.play_mode);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.new_interaction_model);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.interact_rotation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.client_tick);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pos_delta);
        size += {
            1usize
                + match &self.item_use_transaction {
                    Some(_v) => {
                        1usize
                            + match _v {
                                Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                                None => 0usize,
                            }
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.item_stack_request {
                    Some(_v) => {
                        1usize
                            + match _v {
                                Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                                None => 0usize,
                            }
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.player_block_actions {
                    Some(_v) => {
                        1usize
                            + match _v {
                                Some(_v) => {
                                    let _len = (_v).len();
                                    crate::bedrock::codec::BedrockSized::encoded_size(
                                        &crate::bedrock::codec::VarUInt(_len as u32),
                                    ) + (_v)
                                        .iter()
                                        .map(|_item| {
                                            crate::bedrock::codec::BedrockSized::encoded_size(_item)
                                        })
                                        .sum::<usize>()
                                }
                                None => 0usize,
                            }
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.vehicle_rotation {
                    Some(_v) => {
                        1usize
                            + match _v {
                                Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                                None => 0usize,
                            }
                    }
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.client_predicted_vehicle {
                    Some(_v) => {
                        1usize
                            + match _v {
                                Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                                None => 0usize,
                            }
                    }
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.analog_move_vector);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.camera_orientation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.raw_move_vector);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerAuthInputPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_rotation.encode(buf)?;
        self.position.encode(buf)?;
        self.move_vector.encode(buf)?;
        crate::bedrock::codec::F32LE(self.player_head_rotation).encode(buf)?;
        match &self.input_data {
            Some(v) => {
                buf.put_u8(1);
                let len = v.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                for item in v {
                    item.encode(buf)?;
                }
            }
            None => buf.put_u8(0),
        }
        self.input_mode.encode(buf)?;
        self.play_mode.encode(buf)?;
        self.new_interaction_model.encode(buf)?;
        self.interact_rotation.encode(buf)?;
        self.client_tick.encode(buf)?;
        self.pos_delta.encode(buf)?;
        match &self.item_use_transaction {
            Some(v) => {
                buf.put_u8(1);
                match v {
                    Some(v) => {
                        buf.put_u8(1);
                        v.encode(buf)?;
                    }
                    None => buf.put_u8(0),
                }
            }
            None => buf.put_u8(0),
        }
        match &self.item_stack_request {
            Some(v) => {
                buf.put_u8(1);
                match v {
                    Some(v) => {
                        buf.put_u8(1);
                        v.encode(buf)?;
                    }
                    None => buf.put_u8(0),
                }
            }
            None => buf.put_u8(0),
        }
        match &self.player_block_actions {
            Some(v) => {
                buf.put_u8(1);
                match v {
                    Some(v) => {
                        buf.put_u8(1);
                        let len = v.len();
                        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                        for item in v {
                            item.encode(buf)?;
                        }
                    }
                    None => buf.put_u8(0),
                }
            }
            None => buf.put_u8(0),
        }
        match &self.vehicle_rotation {
            Some(v) => {
                buf.put_u8(1);
                match v {
                    Some(v) => {
                        buf.put_u8(1);
                        v.encode(buf)?;
                    }
                    None => buf.put_u8(0),
                }
            }
            None => buf.put_u8(0),
        }
        match &self.client_predicted_vehicle {
            Some(v) => {
                buf.put_u8(1);
                match v {
                    Some(v) => {
                        buf.put_u8(1);
                        v.encode(buf)?;
                    }
                    None => buf.put_u8(0),
                }
            }
            None => buf.put_u8(0),
        }
        self.analog_move_vector.encode(buf)?;
        self.camera_orientation.encode(buf)?;
        self.raw_move_vector.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let position = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let move_vector = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let player_head_rotation =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let input_data = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    let mut tmp_vec =
                        crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
                    for _ in 0..len {
                        crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                        tmp_vec
                            .push(
                                <EnumsPlayerAuthInputPacketPayloadInputData as crate::bedrock::codec::BedrockCodec>::decode(
                                    buf,
                                    (),
                                )?,
                            );
                    }
                    tmp_vec
                })
            } else {
                None
            }
        };
        let input_mode = <EnumsInputMode as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let play_mode =
            <EnumsClientPlayMode as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let new_interaction_model =
            <EnumsNewInteractionModel as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let interact_rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let client_tick =
            <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let pos_delta = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let item_use_transaction = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let present = u8::decode(buf, ())?;
                    if present != 0 {
                        Some(
                            <PackedItemUseLegacyInventoryTransaction as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        )
                    } else {
                        None
                    }
                })
            } else {
                None
            }
        };
        let item_stack_request = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let present = u8::decode(buf, ())?;
                    if present != 0 {
                        Some(
                            <ItemStackRequestCerealRequestData as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        )
                    } else {
                        None
                    }
                })
            } else {
                None
            }
        };
        let player_block_actions = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let present = u8::decode(buf, ())?;
                    if present != 0 {
                        Some({
                            let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                    buf,
                                    (),
                                )?
                                .0) as usize;
                            let mut tmp_vec = crate::bedrock::codec::prepare_decode_vec(
                                len,
                                buf.remaining(),
                                None,
                            )?;
                            for _ in 0..len {
                                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                                tmp_vec
                                    .push(
                                        <PlayerBlockActionData as crate::bedrock::codec::BedrockCodec>::decode(
                                            buf,
                                            (),
                                        )?,
                                    );
                            }
                            tmp_vec
                        })
                    } else {
                        None
                    }
                })
            } else {
                None
            }
        };
        let vehicle_rotation = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let present = u8::decode(buf, ())?;
                    if present != 0 {
                        Some(<Vec2 as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?)
                    } else {
                        None
                    }
                })
            } else {
                None
            }
        };
        let client_predicted_vehicle = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let present = u8::decode(buf, ())?;
                    if present != 0 {
                        Some(
                            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?,
                        )
                    } else {
                        None
                    }
                })
            } else {
                None
            }
        };
        let analog_move_vector = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let camera_orientation = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let raw_move_vector = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            player_rotation,
            position,
            move_vector,
            player_head_rotation,
            input_data,
            input_mode,
            play_mode,
            new_interaction_model,
            interact_rotation,
            client_tick,
            pos_delta,
            item_use_transaction,
            item_stack_request,
            player_block_actions,
            vehicle_rotation,
            client_predicted_vehicle,
            analog_move_vector,
            camera_orientation,
            raw_move_vector,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CreativeContentPacket {
    pub groups: Vec<CreativeGroupInfoPayload>,
    pub entries: Vec<CreativeItemEntryPayload>,
}
impl crate::bedrock::codec::BedrockSized for CreativeContentPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.groups).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.groups)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.entries).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.entries)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CreativeContentPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.groups.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.groups {
            item.encode(buf)?;
        }
        let len = self.entries.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.entries {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let groups = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <CreativeGroupInfoPayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let entries = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <CreativeItemEntryPayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self { groups, entries })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerEnchantOptionsPacket {
    pub options: Vec<ItemEnchantOption>,
}
impl crate::bedrock::codec::BedrockSized for PlayerEnchantOptionsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.options).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.options)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerEnchantOptionsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.options.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.options {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let options = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ItemEnchantOption as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        Ok(Self { options })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ItemStackRequestPacket {
    pub requests: Vec<ItemStackRequestPacketDataRequestData>,
}
impl crate::bedrock::codec::BedrockSized for ItemStackRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.requests).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.requests)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ItemStackRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.requests.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.requests {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let requests = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <ItemStackRequestPacketDataRequestData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self { requests })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ItemStackResponsePacket {
    pub responses: Vec<ItemStackResponseInfo>,
}
impl crate::bedrock::codec::BedrockSized for ItemStackResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.responses).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.responses)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ItemStackResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.responses.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.responses {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let responses = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ItemStackResponseInfo as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self { responses })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerArmorDamagePacket {
    pub armor_slotand_damage_pairs: Vec<ArmorSlotAndDamagePair>,
}
impl crate::bedrock::codec::BedrockSized for PlayerArmorDamagePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.armor_slotand_damage_pairs).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.armor_slotand_damage_pairs)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerArmorDamagePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.armor_slotand_damage_pairs.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.armor_slotand_damage_pairs {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let armor_slotand_damage_pairs = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ArmorSlotAndDamagePair as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self {
            armor_slotand_damage_pairs,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CodeBuilderPacket {
    pub url: String,
    pub shouldopencodebuilder: bool,
}
impl crate::bedrock::codec::BedrockSized for CodeBuilderPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.url).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CodeBuilderPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.url).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.shouldopencodebuilder.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let url = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let shouldopencodebuilder = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            url,
            shouldopencodebuilder,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdatePlayerGameTypePacket {
    pub player_game_type: EnumsGameType,
    pub targetplayer: ActorUniqueId,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for UpdatePlayerGameTypePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.player_game_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.targetplayer);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdatePlayerGameTypePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.player_game_type.encode(buf)?;
        self.targetplayer.encode(buf)?;
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let player_game_type =
            <EnumsGameType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let targetplayer = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            player_game_type,
            targetplayer,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct EmoteListPacket {
    pub runtimeid: ActorRuntimeId,
    pub emotepieceids: Vec<uuid::Uuid>,
}
impl crate::bedrock::codec::BedrockSized for EmoteListPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.runtimeid);
        size += {
            let _len = (&self.emotepieceids).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.emotepieceids)
                .iter()
                .map(|_item| 16usize)
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for EmoteListPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.runtimeid.encode(buf)?;
        let len = self.emotepieceids.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.emotepieceids {
            (*item).encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let runtimeid = <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let emotepieceids = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(<uuid::Uuid as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?);
            }
            tmp_vec
        };
        Ok(Self {
            runtimeid,
            emotepieceids,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PositionTrackingDbServerBroadcastPacket {
    pub action: EnumsPositionTrackingDbServerBroadcastPacketPayloadAction,
    pub id: PositionTrackingId,
    pub positiontrackingdata: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for PositionTrackingDbServerBroadcastPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.positiontrackingdata);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PositionTrackingDbServerBroadcastPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        self.id.encode(buf)?;
        self.positiontrackingdata.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action = <EnumsPositionTrackingDbServerBroadcastPacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let id = <PositionTrackingId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let positiontrackingdata =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            action,
            id,
            positiontrackingdata,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PositionTrackingDbClientRequestPacket {
    pub action: EnumsPositionTrackingDbClientRequestPacketPayloadAction,
    pub id: PositionTrackingId,
}
impl crate::bedrock::codec::BedrockSized for PositionTrackingDbClientRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PositionTrackingDbClientRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        self.id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action = <EnumsPositionTrackingDbClientRequestPacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let id = <PositionTrackingId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { action, id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct DebugInfoPacket {
    pub actor_id: ActorUniqueId,
    pub data: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for DebugInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_id);
        size += {
            let _len = (&self.data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for DebugInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.actor_id.encode(buf)?;
        let bytes = (&self.data).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let actor_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self { actor_id, data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PacketViolationWarningPacket {
    pub violation_type: EnumsPacketViolationType,
    pub violation_severity: EnumsPacketViolationSeverity,
    pub violation_packet_id: i32,
    pub violation_context: String,
}
impl crate::bedrock::codec::BedrockSized for PacketViolationWarningPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.violation_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.violation_severity);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.violation_packet_id),
        );
        size += {
            let _len = (&self.violation_context).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PacketViolationWarningPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.violation_type.encode(buf)?;
        self.violation_severity.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.violation_packet_id).encode(buf)?;
        let bytes = (&self.violation_context).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let violation_type =
            <EnumsPacketViolationType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let violation_severity =
            <EnumsPacketViolationSeverity as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let violation_packet_id =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let violation_context = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            violation_type,
            violation_severity,
            violation_packet_id,
            violation_context,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MotionPredictionHintsPacket {
    pub m_runtime_id: ActorRuntimeId,
    pub m_motion: Vec3,
    pub m_on_ground: bool,
}
impl crate::bedrock::codec::BedrockSized for MotionPredictionHintsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.m_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.m_motion);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MotionPredictionHintsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.m_runtime_id.encode(buf)?;
        self.m_motion.encode(buf)?;
        self.m_on_ground.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let m_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let m_motion = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let m_on_ground = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            m_runtime_id,
            m_motion,
            m_on_ground,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AnimateEntityPacket {
    pub m_animation: String,
    pub m_next_state: String,
    pub m_stop_expression: String,
    pub m_stop_expression_version: i32,
    pub m_controller: String,
    pub m_blend_out_time: f32,
    pub m_runtime_ids: Vec<ActorRuntimeId>,
}
impl crate::bedrock::codec::BedrockSized for AnimateEntityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.m_animation).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.m_next_state).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.m_stop_expression).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += {
            let _len = (&self.m_controller).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += {
            let _len = (&self.m_runtime_ids).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.m_runtime_ids)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AnimateEntityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.m_animation).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.m_next_state).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.m_stop_expression).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::I32LE(self.m_stop_expression_version).encode(buf)?;
        let bytes = (&self.m_controller).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::F32LE(self.m_blend_out_time).encode(buf)?;
        let len = self.m_runtime_ids.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.m_runtime_ids {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let m_animation = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let m_next_state = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let m_stop_expression = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let m_stop_expression_version =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let m_controller = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let m_blend_out_time =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let m_runtime_ids = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        Ok(Self {
            m_animation,
            m_next_state,
            m_stop_expression,
            m_stop_expression_version,
            m_controller,
            m_blend_out_time,
            m_runtime_ids,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraShakePacket {
    pub intensity: f32,
    pub seconds: f32,
    pub shake_type: EnumsCameraShakeType,
    pub shake_action: EnumsCameraShakeAction,
}
impl crate::bedrock::codec::BedrockSized for CameraShakePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.shake_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.shake_action);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraShakePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::F32LE(self.intensity).encode(buf)?;
        crate::bedrock::codec::F32LE(self.seconds).encode(buf)?;
        self.shake_type.encode(buf)?;
        self.shake_action.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let intensity =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let seconds =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let shake_type =
            <EnumsCameraShakeType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let shake_action =
            <EnumsCameraShakeAction as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            intensity,
            seconds,
            shake_type,
            shake_action,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerFogPacket {
    pub fog_stack: Vec<String>,
}
impl crate::bedrock::codec::BedrockSized for PlayerFogPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.fog_stack).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.fog_stack)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerFogPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.fog_stack.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.fog_stack {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let fog_stack = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        Ok(Self { fog_stack })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CorrectPlayerMovePredictionPacket {
    pub prediction_type: EnumsRewindType,
    pub pos: Vec3,
    pub pos_delta: Vec3,
    pub rotation: Vec2,
    pub vehicle_angular_velocity: Option<f32>,
    pub on_ground: bool,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for CorrectPlayerMovePredictionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.prediction_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pos);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pos_delta);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += {
            1usize
                + match &self.vehicle_angular_velocity {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CorrectPlayerMovePredictionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.prediction_type.encode(buf)?;
        self.pos.encode(buf)?;
        self.pos_delta.encode(buf)?;
        self.rotation.encode(buf)?;
        match &self.vehicle_angular_velocity {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::F32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        self.on_ground.encode(buf)?;
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let prediction_type =
            <EnumsRewindType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let pos = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let pos_delta = <Vec3 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let rotation = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let vehicle_angular_velocity = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        let on_ground = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            prediction_type,
            pos,
            pos_delta,
            rotation,
            vehicle_angular_velocity,
            on_ground,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ItemRegistryPacket {
    pub item_data: Vec<ItemData>,
}
impl crate::bedrock::codec::BedrockSized for ItemRegistryPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.item_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.item_data)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ItemRegistryPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.item_data.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.item_data {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let item_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(<ItemData as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?);
            }
            tmp_vec
        };
        Ok(Self { item_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundDebugRendererPacket {
    pub type_: String,
    pub debug_marker_data: Option<ClientboundDebugRendererPacketPayloadDebugMarkerData>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundDebugRendererPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.type_).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            1usize
                + match &self.debug_marker_data {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundDebugRendererPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.type_).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        match &self.debug_marker_data {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let type_ = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let debug_marker_data = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundDebugRendererPacketPayloadDebugMarkerData as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            type_,
            debug_marker_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SyncActorPropertyPacket {
    pub property_data: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for SyncActorPropertyPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.property_data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SyncActorPropertyPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.property_data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let property_data =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { property_data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AddVolumeEntityPacket {
    pub entity_network_id: EntityNetId,
    pub components: crate::bedrock::codec::Nbt,
    pub json_identifier: String,
    pub instance_name: String,
    pub min_bounds: BlockPos,
    pub max_bounds: BlockPos,
    pub dimension_type: DimensionType,
    pub engine_version: String,
}
impl crate::bedrock::codec::BedrockSized for AddVolumeEntityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_network_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.components);
        size += {
            let _len = (&self.json_identifier).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.instance_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.min_bounds);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.max_bounds);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_type);
        size += {
            let _len = (&self.engine_version).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AddVolumeEntityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.entity_network_id.encode(buf)?;
        self.components.encode(buf)?;
        let bytes = (&self.json_identifier).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.instance_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.min_bounds.encode(buf)?;
        self.max_bounds.encode(buf)?;
        self.dimension_type.encode(buf)?;
        let bytes = (&self.engine_version).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let entity_network_id =
            <EntityNetId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let components =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let json_identifier = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let instance_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let min_bounds = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let max_bounds = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimension_type =
            <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let engine_version = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            entity_network_id,
            components,
            json_identifier,
            instance_name,
            min_bounds,
            max_bounds,
            dimension_type,
            engine_version,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RemoveVolumeEntityPacket {
    pub entity_network_id: EntityNetId,
    pub dimension_type: DimensionType,
}
impl crate::bedrock::codec::BedrockSized for RemoveVolumeEntityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.entity_network_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RemoveVolumeEntityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.entity_network_id.encode(buf)?;
        self.dimension_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let entity_network_id =
            <EntityNetId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimension_type =
            <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            entity_network_id,
            dimension_type,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SimulationTypePacket {
    pub sim_type: EnumsSimulationType,
}
impl crate::bedrock::codec::BedrockSized for SimulationTypePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.sim_type);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SimulationTypePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.sim_type.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let sim_type =
            <EnumsSimulationType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { sim_type })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct NpcDialoguePacket {
    pub npc_id_raw_id: u64,
    pub npc_dialogue_action_type: EnumsNpcDialoguePacketPayloadNpcDialogueActionType,
    pub dialogue: String,
    pub scene_name: String,
    pub npc_name: String,
    pub action_json: String,
}
impl crate::bedrock::codec::BedrockSized for NpcDialoguePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 8usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.npc_dialogue_action_type);
        size += {
            let _len = (&self.dialogue).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.scene_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.npc_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.action_json).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for NpcDialoguePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U64LE(self.npc_id_raw_id).encode(buf)?;
        self.npc_dialogue_action_type.encode(buf)?;
        let bytes = (&self.dialogue).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.scene_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.npc_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.action_json).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let npc_id_raw_id =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let npc_dialogue_action_type = <EnumsNpcDialoguePacketPayloadNpcDialogueActionType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let dialogue = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let scene_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let npc_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let action_json = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            npc_id_raw_id,
            npc_dialogue_action_type,
            dialogue,
            scene_name,
            npc_name,
            action_json,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct EduUriResourcePacket {
    pub edu_shared_uri_resource: EduSharedUriResource,
}
impl crate::bedrock::codec::BedrockSized for EduUriResourcePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.edu_shared_uri_resource);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for EduUriResourcePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.edu_shared_uri_resource.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let edu_shared_uri_resource =
            <EduSharedUriResource as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            edu_shared_uri_resource,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CreatePhotoPacket {
    pub raw_id: u64,
    pub photo_name: String,
    pub photo_item_name: String,
}
impl crate::bedrock::codec::BedrockSized for CreatePhotoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 8usize;
        size += {
            let _len = (&self.photo_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.photo_item_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CreatePhotoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U64LE(self.raw_id).encode(buf)?;
        let bytes = (&self.photo_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.photo_item_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let raw_id =
            <crate::bedrock::codec::U64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let photo_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let photo_item_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            raw_id,
            photo_name,
            photo_item_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateSubChunkBlocksPacket {
    pub sub_chunk_block_position: BlockPos,
    pub blocks_changed: UpdateSubChunkBlocksChangedInfo,
}
impl crate::bedrock::codec::BedrockSized for UpdateSubChunkBlocksPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.sub_chunk_block_position);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.blocks_changed);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateSubChunkBlocksPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.sub_chunk_block_position.encode(buf)?;
        self.blocks_changed.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let sub_chunk_block_position =
            <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let blocks_changed =
            <UpdateSubChunkBlocksChangedInfo as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self {
            sub_chunk_block_position,
            blocks_changed,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SubChunkPacket {
    pub cache_enabled: bool,
    pub dimension_type: DimensionType,
    pub center_pos: SubChunkPos,
    pub sub_chunk_data: Vec<SubChunkPacketPayloadSubChunkPacketData>,
}
impl crate::bedrock::codec::BedrockSized for SubChunkPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_type);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.center_pos);
        size += {
            let _len = (&self.sub_chunk_data).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.sub_chunk_data)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SubChunkPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.cache_enabled.encode(buf)?;
        self.dimension_type.encode(buf)?;
        self.center_pos.encode(buf)?;
        let len = self.sub_chunk_data.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.sub_chunk_data {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let cache_enabled = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let dimension_type =
            <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let center_pos = <SubChunkPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let sub_chunk_data = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SubChunkPacketPayloadSubChunkPacketData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            cache_enabled,
            dimension_type,
            center_pos,
            sub_chunk_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SubChunkRequestPacket {
    pub dimension_type: DimensionType,
    pub sub_chunk_position_offset_list: Vec<SubChunkPacketPayloadSubChunkPosOffset>,
    pub center_pos: SubChunkPos,
}
impl crate::bedrock::codec::BedrockSized for SubChunkRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.dimension_type);
        size += {
            let _len = (&self.sub_chunk_position_offset_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.sub_chunk_position_offset_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.center_pos);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SubChunkRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.dimension_type.encode(buf)?;
        let len = self.sub_chunk_position_offset_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.sub_chunk_position_offset_list {
            item.encode(buf)?;
        }
        self.center_pos.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let dimension_type =
            <DimensionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let sub_chunk_position_offset_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SubChunkPacketPayloadSubChunkPosOffset as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let center_pos = <SubChunkPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            dimension_type,
            sub_chunk_position_offset_list,
            center_pos,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerStartItemCooldownPacket {
    pub item_category: String,
    pub duration_ticks: i32,
}
impl crate::bedrock::codec::BedrockSized for PlayerStartItemCooldownPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.item_category).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.duration_ticks),
        );
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerStartItemCooldownPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.item_category).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::ZigZag32(self.duration_ticks).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let item_category = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let duration_ticks =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            item_category,
            duration_ticks,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ScriptMessagePacket {
    pub message_id: String,
    pub message_value: Vec<u8>,
}
impl crate::bedrock::codec::BedrockSized for ScriptMessagePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.message_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.message_value).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ScriptMessagePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.message_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.message_value).as_slice();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let message_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let message_value = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::ArrayLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut value = vec![0u8; len];
            buf.copy_to_slice(&mut value);
            value
        };
        Ok(Self {
            message_id,
            message_value,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CodeBuilderSourcePacket {
    pub operation: EnumsCodeBuilderStorageQueryOptionsOperation,
    pub category: EnumsCodeBuilderStorageQueryOptionsCategory,
    pub code_status: EnumsCodeBuilderExecutionStateCodeStatus,
}
impl crate::bedrock::codec::BedrockSized for CodeBuilderSourcePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.operation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.category);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.code_status);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CodeBuilderSourcePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.operation.encode(buf)?;
        self.category.encode(buf)?;
        self.code_status.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let operation = <EnumsCodeBuilderStorageQueryOptionsOperation as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let category = <EnumsCodeBuilderStorageQueryOptionsCategory as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let code_status = <EnumsCodeBuilderExecutionStateCodeStatus as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            operation,
            category,
            code_status,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct TickingAreasLoadStatusPacket {
    pub waiting_for_preload: bool,
}
impl crate::bedrock::codec::BedrockSized for TickingAreasLoadStatusPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for TickingAreasLoadStatusPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.waiting_for_preload.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let waiting_for_preload = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            waiting_for_preload,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct DimensionDataPacket {
    pub definitions: Vec<DimensionDataPacketDefinitionsItem>,
}
impl crate::bedrock::codec::BedrockSized for DimensionDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.definitions).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.definitions)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for DimensionDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.definitions.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.definitions {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let definitions = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <DimensionDataPacketDefinitionsItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self { definitions })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AgentActionEventPacket {
    pub request_id: String,
    pub action: EnumsAgentActionType,
    pub response: JsonValuejson,
}
impl crate::bedrock::codec::BedrockSized for AgentActionEventPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.request_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += {
            let _len = (&self.response).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AgentActionEventPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.request_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.action.encode(buf)?;
        let bytes = (&self.response).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let request_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let action =
            <EnumsAgentActionType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let response = {
            let res: JsonValuejson = {
                let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0) as usize;
                if buf.remaining() < len {
                    return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                        declared: len,
                        available: buf.remaining(),
                    });
                }
                let mut bytes = vec![0u8; len];
                buf.copy_to_slice(&mut bytes);
                crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
            };
            res
        };
        Ok(Self {
            request_id,
            action,
            response,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ChangeMobPropertyPacket {
    pub actor_id: ActorUniqueId,
    pub property_name: String,
    pub bool_component_value: bool,
    pub string_component_value: String,
    pub int_component_value: i32,
    pub float_component_value: f32,
}
impl crate::bedrock::codec::BedrockSized for ChangeMobPropertyPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_id);
        size += {
            let _len = (&self.property_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size += {
            let _len = (&self.string_component_value).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.int_component_value),
        );
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ChangeMobPropertyPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.actor_id.encode(buf)?;
        let bytes = (&self.property_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.bool_component_value.encode(buf)?;
        let bytes = (&self.string_component_value).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::ZigZag32(self.int_component_value).encode(buf)?;
        crate::bedrock::codec::F32LE(self.float_component_value).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let actor_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let property_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let bool_component_value = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let string_component_value = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let int_component_value =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let float_component_value =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            actor_id,
            property_name,
            bool_component_value,
            string_component_value,
            int_component_value,
            float_component_value,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LessonProgressPacket {
    pub lesson_action: i32,
    pub score: i32,
    pub activity_id: String,
}
impl crate::bedrock::codec::BedrockSized for LessonProgressPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.lesson_action),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.score),
        );
        size += {
            let _len = (&self.activity_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LessonProgressPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.lesson_action).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.score).encode(buf)?;
        let bytes = (&self.activity_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let lesson_action =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let score =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let activity_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            lesson_action,
            score,
            activity_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RequestAbilityPacket {
    pub ability: i32,
    pub value_type: EnumsRequestAbilityPacketPayloadType,
    pub bool: bool,
    pub float: f32,
}
impl crate::bedrock::codec::BedrockSized for RequestAbilityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.ability),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.value_type);
        size += 1usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RequestAbilityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.ability).encode(buf)?;
        self.value_type.encode(buf)?;
        self.bool.encode(buf)?;
        crate::bedrock::codec::F32LE(self.float).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let ability =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let value_type =
            <EnumsRequestAbilityPacketPayloadType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let bool = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let float =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            ability,
            value_type,
            bool,
            float,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RequestPermissionsPacket {
    pub target_player_ids_raw_id: i64,
    pub player_permission_level: i32,
    pub custom_permission_flags: u16,
}
impl crate::bedrock::codec::BedrockSized for RequestPermissionsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 8usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.player_permission_level),
        );
        size += 2usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RequestPermissionsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I64LE(self.target_player_ids_raw_id).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.player_permission_level).encode(buf)?;
        crate::bedrock::codec::U16LE(self.custom_permission_flags).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_player_ids_raw_id =
            <crate::bedrock::codec::I64LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let player_permission_level =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let custom_permission_flags =
            <crate::bedrock::codec::U16LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            target_player_ids_raw_id,
            player_permission_level,
            custom_permission_flags,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ToastRequestPacket {
    pub title: String,
    pub content: String,
}
impl crate::bedrock::codec::BedrockSized for ToastRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.title).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.content).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ToastRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.title).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.content).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let title = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let content = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self { title, content })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateAbilitiesPacket {
    pub data: SerializedAbilitiesData,
}
impl crate::bedrock::codec::BedrockSized for UpdateAbilitiesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateAbilitiesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let data =
            <SerializedAbilitiesData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateAdventureSettingsPacket {
    pub adventure_settings: AdventureSettings,
}
impl crate::bedrock::codec::BedrockSized for UpdateAdventureSettingsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.adventure_settings);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateAdventureSettingsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.adventure_settings.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let adventure_settings =
            <AdventureSettings as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { adventure_settings })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct DeathInfoPacket {
    pub death_cause_attack_name: String,
    pub death_cause_message_list: Vec<String>,
}
impl crate::bedrock::codec::BedrockSized for DeathInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.death_cause_attack_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.death_cause_message_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.death_cause_message_list)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for DeathInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.death_cause_attack_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let len = self.death_cause_message_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.death_cause_message_list {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let death_cause_attack_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let death_cause_message_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        Ok(Self {
            death_cause_attack_name,
            death_cause_message_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct EditorNetworkPacket {
    pub route_to_manager: bool,
    pub payload: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for EditorNetworkPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.payload);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for EditorNetworkPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.route_to_manager.encode(buf)?;
        self.payload.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let route_to_manager = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let payload =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            route_to_manager,
            payload,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct FeatureRegistryPacket {
    pub features_data_list: Vec<FeatureRegistryFeatureBinaryJsonFormat>,
}
impl crate::bedrock::codec::BedrockSized for FeatureRegistryPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.features_data_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.features_data_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for FeatureRegistryPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.features_data_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.features_data_list {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let features_data_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <FeatureRegistryFeatureBinaryJsonFormat as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self { features_data_list })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerStatsPacket {
    pub server_time: f32,
    pub network_time: f32,
}
impl crate::bedrock::codec::BedrockSized for ServerStatsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerStatsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::F32LE(self.server_time).encode(buf)?;
        crate::bedrock::codec::F32LE(self.network_time).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let server_time =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let network_time =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            server_time,
            network_time,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RequestNetworkSettingsPacket {
    pub client_network_version: i32,
}
impl crate::bedrock::codec::BedrockSized for RequestNetworkSettingsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for RequestNetworkSettingsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.client_network_version.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let client_network_version = <i32 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            client_network_version,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct GameTestRequestPacket {
    pub max_tests_per_batch: i32,
    pub repeat_count: i32,
    pub rotation: EnumsRotation,
    pub stop_on_failure: bool,
    pub test_pos: BlockPos,
    pub tests_per_row: i32,
    pub test_name: String,
}
impl crate::bedrock::codec::BedrockSized for GameTestRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.max_tests_per_batch),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.repeat_count),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.rotation);
        size += 1usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.test_pos);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.tests_per_row),
        );
        size += {
            let _len = (&self.test_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for GameTestRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.max_tests_per_batch).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.repeat_count).encode(buf)?;
        self.rotation.encode(buf)?;
        self.stop_on_failure.encode(buf)?;
        self.test_pos.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.tests_per_row).encode(buf)?;
        let bytes = (&self.test_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let max_tests_per_batch =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let repeat_count =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let rotation = <EnumsRotation as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let stop_on_failure = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let test_pos = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let tests_per_row =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let test_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            max_tests_per_batch,
            repeat_count,
            rotation,
            stop_on_failure,
            test_pos,
            tests_per_row,
            test_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct GameTestResultsPacket {
    pub succeeded: bool,
    pub error: String,
    pub test_name: String,
}
impl crate::bedrock::codec::BedrockSized for GameTestResultsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 1usize;
        size += {
            let _len = (&self.error).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.test_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for GameTestResultsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.succeeded.encode(buf)?;
        let bytes = (&self.error).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.test_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let succeeded = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let error = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let test_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            succeeded,
            error,
            test_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateClientInputLocksPacket {
    pub input_lock_component_data: u32,
}
impl crate::bedrock::codec::BedrockSized for UpdateClientInputLocksPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.input_lock_component_data,
        ));
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateClientInputLocksPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarUInt(self.input_lock_component_data).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let input_lock_component_data =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self {
            input_lock_component_data,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraPresetsPacket {
    pub camera_presets: CameraPresets,
}
impl crate::bedrock::codec::BedrockSized for CameraPresetsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.camera_presets);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraPresetsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.camera_presets.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_presets =
            <CameraPresets as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { camera_presets })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UnlockedRecipesPacket {
    pub packet_type: EnumsUnlockedRecipesPacketPayloadPacketType,
    pub unlocked_recipes_list: Vec<String>,
}
impl crate::bedrock::codec::BedrockSized for UnlockedRecipesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.packet_type);
        size += {
            let _len = (&self.unlocked_recipes_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.unlocked_recipes_list)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UnlockedRecipesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.packet_type.encode(buf)?;
        let len = self.unlocked_recipes_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.unlocked_recipes_list {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let packet_type = <EnumsUnlockedRecipesPacketPayloadPacketType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let unlocked_recipes_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        Ok(Self {
            packet_type,
            unlocked_recipes_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraInstructionPacket {
    pub camera_instruction: CameraInstruction,
}
impl crate::bedrock::codec::BedrockSized for CameraInstructionPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.camera_instruction);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraInstructionPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.camera_instruction.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_instruction =
            <CameraInstruction as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { camera_instruction })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct TrimDataPacket {
    pub trim_pattern_list: Vec<TrimPattern>,
    pub trim_material_list: Vec<TrimMaterial>,
}
impl crate::bedrock::codec::BedrockSized for TrimDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.trim_pattern_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.trim_pattern_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.trim_material_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.trim_material_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for TrimDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.trim_pattern_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.trim_pattern_list {
            item.encode(buf)?;
        }
        let len = self.trim_material_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.trim_material_list {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let trim_pattern_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(<TrimPattern as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?);
            }
            tmp_vec
        };
        let trim_material_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(<TrimMaterial as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?);
            }
            tmp_vec
        };
        Ok(Self {
            trim_pattern_list,
            trim_material_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct OpenSignPacket {
    pub pos: BlockPos,
    pub is_front_side: bool,
}
impl crate::bedrock::codec::BedrockSized for OpenSignPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pos);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for OpenSignPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.pos.encode(buf)?;
        self.is_front_side.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let pos = <BlockPos as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_front_side = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { pos, is_front_side })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AgentAnimationPacket {
    pub agent_animation: EnumsAgentAnimation,
    pub runtime_id: ActorRuntimeId,
}
impl crate::bedrock::codec::BedrockSized for AgentAnimationPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.agent_animation);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.runtime_id);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AgentAnimationPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.agent_animation.encode(buf)?;
        self.runtime_id.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let agent_animation =
            <EnumsAgentAnimation as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let runtime_id = <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            agent_animation,
            runtime_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct RefreshEntitlementsPacket {}
impl crate::bedrock::codec::BedrockSized for RefreshEntitlementsPacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for RefreshEntitlementsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerToggleCrafterSlotRequestPacket {
    pub pos_x: i32,
    pub pos_y: i32,
    pub pos_z: i32,
    pub slot_index: u8,
    pub is_disabled: bool,
}
impl crate::bedrock::codec::BedrockSized for PlayerToggleCrafterSlotRequestPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 1usize;
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerToggleCrafterSlotRequestPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I32LE(self.pos_x).encode(buf)?;
        crate::bedrock::codec::I32LE(self.pos_y).encode(buf)?;
        crate::bedrock::codec::I32LE(self.pos_z).encode(buf)?;
        self.slot_index.encode(buf)?;
        self.is_disabled.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let pos_x =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let pos_y =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let pos_z =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let slot_index = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let is_disabled = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            pos_x,
            pos_y,
            pos_z,
            slot_index,
            is_disabled,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetPlayerInventoryOptionsPacket {
    pub inventory_options: InventoryOptions,
}
impl crate::bedrock::codec::BedrockSized for SetPlayerInventoryOptionsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.inventory_options);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetPlayerInventoryOptionsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.inventory_options.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let inventory_options =
            <InventoryOptions as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { inventory_options })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SetHudPacket {
    pub hud_element: Vec<EnumsHudElement>,
    pub hud_visible: EnumsHudVisibility,
}
impl crate::bedrock::codec::BedrockSized for SetHudPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.hud_element).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.hud_element)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.hud_visible);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SetHudPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.hud_element.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.hud_element {
            item.encode(buf)?;
        }
        self.hud_visible.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let hud_element = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <EnumsHudElement as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        let hud_visible =
            <EnumsHudVisibility as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            hud_element,
            hud_visible,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct AwardAchievementPacket {
    pub achievement_id: i32,
}
impl crate::bedrock::codec::BedrockSized for AwardAchievementPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for AwardAchievementPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I32LE(self.achievement_id).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let achievement_id =
            <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self { achievement_id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundCloseFormPacket {}
impl crate::bedrock::codec::BedrockSized for ClientboundCloseFormPacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundCloseFormPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerboundLoadingScreenPacket {
    pub loading_screen_packet_type: EnumsServerboundLoadingScreenPacketType,
    pub loading_screen_id: Option<u32>,
}
impl crate::bedrock::codec::BedrockSized for ServerboundLoadingScreenPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.loading_screen_packet_type);
        size += {
            1usize
                + match &self.loading_screen_id {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerboundLoadingScreenPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.loading_screen_packet_type.encode(buf)?;
        match &self.loading_screen_id {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::U32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let loading_screen_packet_type = <EnumsServerboundLoadingScreenPacketType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let loading_screen_id = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        Ok(Self {
            loading_screen_packet_type,
            loading_screen_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct JigsawStructureDataPacket {
    pub jigsaw_structure_data_tag: crate::bedrock::codec::Nbt,
}
impl crate::bedrock::codec::BedrockSized for JigsawStructureDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.jigsaw_structure_data_tag);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for JigsawStructureDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.jigsaw_structure_data_tag.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let jigsaw_structure_data_tag =
            <crate::bedrock::codec::Nbt as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            jigsaw_structure_data_tag,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CurrentStructureFeaturePacket {
    pub current_structure_feature: String,
}
impl crate::bedrock::codec::BedrockSized for CurrentStructureFeaturePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.current_structure_feature).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CurrentStructureFeaturePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.current_structure_feature).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let current_structure_feature = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            current_structure_feature,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerboundDiagnosticsPacket {
    pub avg_fps: f32,
    pub avg_server_sim_tick_time_ms: f32,
    pub avg_client_sim_tick_time_ms: f32,
    pub avg_begin_frame_time_ms: f32,
    pub avg_input_time_ms: f32,
    pub avg_render_time_ms: f32,
    pub avg_end_frame_time_ms: f32,
    pub avg_remainder_time_percent: f32,
    pub avg_unaccounted_time_percent: f32,
    pub memory_category_values: Vec<MemoryMemoryCategoryCounter>,
    pub entity_diagnostics: Vec<EcsProfilingDiagnosticsEntityDiagnosticTimingInfo>,
    pub system_diagnostics: Vec<EcsProfilingDiagnosticsSystemDiagnosticTimingInfo>,
    pub system_categories: Vec<EcsProfilingDiagnosticsSystemCategory>,
    pub whisker_scopes: Vec<BedrockProfileWhiskerDiagnosticsScopeDataSummary>,
}
impl crate::bedrock::codec::BedrockSized for ServerboundDiagnosticsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += 4usize;
        size += {
            let _len = (&self.memory_category_values).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.memory_category_values)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.entity_diagnostics).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.entity_diagnostics)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.system_diagnostics).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.system_diagnostics)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.system_categories).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.system_categories)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.whisker_scopes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.whisker_scopes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerboundDiagnosticsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::F32LE(self.avg_fps).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_server_sim_tick_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_client_sim_tick_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_begin_frame_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_input_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_render_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_end_frame_time_ms).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_remainder_time_percent).encode(buf)?;
        crate::bedrock::codec::F32LE(self.avg_unaccounted_time_percent).encode(buf)?;
        let len = self.memory_category_values.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.memory_category_values {
            item.encode(buf)?;
        }
        let len = self.entity_diagnostics.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.entity_diagnostics {
            item.encode(buf)?;
        }
        let len = self.system_diagnostics.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.system_diagnostics {
            item.encode(buf)?;
        }
        let len = self.system_categories.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.system_categories {
            item.encode(buf)?;
        }
        let len = self.whisker_scopes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.whisker_scopes {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let avg_fps =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_server_sim_tick_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_client_sim_tick_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_begin_frame_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_input_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_render_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_end_frame_time_ms =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_remainder_time_percent =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let avg_unaccounted_time_percent =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let memory_category_values = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <MemoryMemoryCategoryCounter as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let entity_diagnostics = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <EcsProfilingDiagnosticsEntityDiagnosticTimingInfo as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let system_diagnostics = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <EcsProfilingDiagnosticsSystemDiagnosticTimingInfo as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let system_categories = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <EcsProfilingDiagnosticsSystemCategory as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let whisker_scopes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <BedrockProfileWhiskerDiagnosticsScopeDataSummary as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            avg_fps,
            avg_server_sim_tick_time_ms,
            avg_client_sim_tick_time_ms,
            avg_begin_frame_time_ms,
            avg_input_time_ms,
            avg_render_time_ms,
            avg_end_frame_time_ms,
            avg_remainder_time_percent,
            avg_unaccounted_time_percent,
            memory_category_values,
            entity_diagnostics,
            system_diagnostics,
            system_categories,
            whisker_scopes,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraAimAssistPacket {
    pub preset_id: String,
    pub view_angle: Vec2,
    pub distance: f32,
    pub target_mode: EnumsCameraAimAssistPacketPayloadTargetMode,
    pub action: EnumsCameraAimAssistPacketPayloadAction,
    pub show_debug_render: bool,
}
impl crate::bedrock::codec::BedrockSized for CameraAimAssistPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.preset_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.view_angle);
        size += 4usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_mode);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraAimAssistPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.preset_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.view_angle.encode(buf)?;
        crate::bedrock::codec::F32LE(self.distance).encode(buf)?;
        self.target_mode.encode(buf)?;
        self.action.encode(buf)?;
        self.show_debug_render.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let preset_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let view_angle = <Vec2 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let distance =
            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let target_mode = <EnumsCameraAimAssistPacketPayloadTargetMode as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let action = <EnumsCameraAimAssistPacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let show_debug_render = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            preset_id,
            view_angle,
            distance,
            target_mode,
            action,
            show_debug_render,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ContainerRegistryCleanupPacket {
    pub removed_containers: Vec<FullContainerName>,
}
impl crate::bedrock::codec::BedrockSized for ContainerRegistryCleanupPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.removed_containers).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.removed_containers)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ContainerRegistryCleanupPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.removed_containers.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.removed_containers {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let removed_containers = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <FullContainerName as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                );
            }
            tmp_vec
        };
        Ok(Self { removed_containers })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct MovementEffectPacket {
    pub target_runtime_id: ActorRuntimeId,
    pub effect_id: EnumsMovementEffectType,
    pub effect_duration: i32,
    pub tick: PlayerInputTick,
}
impl crate::bedrock::codec::BedrockSized for MovementEffectPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_runtime_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.effect_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::ZigZag32(self.effect_duration),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.tick);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for MovementEffectPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_runtime_id.encode(buf)?;
        self.effect_id.encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.effect_duration).encode(buf)?;
        self.tick.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_runtime_id =
            <ActorRuntimeId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let effect_id =
            <EnumsMovementEffectType as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let effect_duration =
            <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let tick = <PlayerInputTick as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_runtime_id,
            effect_id,
            effect_duration,
            tick,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraAimAssistPresetsPacket {
    pub camera_aim_assist_presets: Vec<SharedTypesv12150CameraAimAssistCategoryDefinition>,
    pub camera_aim_assist_categories: Vec<SharedTypesv121120CameraAimAssistPresetDefinition>,
    pub operation: EnumsCameraAimAssistPresetsPacketOperation,
}
impl crate::bedrock::codec::BedrockSized for CameraAimAssistPresetsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.camera_aim_assist_presets).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.camera_aim_assist_presets)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.camera_aim_assist_categories).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.camera_aim_assist_categories)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.operation);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraAimAssistPresetsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.camera_aim_assist_presets.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.camera_aim_assist_presets {
            item.encode(buf)?;
        }
        let len = self.camera_aim_assist_categories.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.camera_aim_assist_categories {
            item.encode(buf)?;
        }
        self.operation.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_aim_assist_presets = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SharedTypesv12150CameraAimAssistCategoryDefinition as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let camera_aim_assist_categories = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SharedTypesv121120CameraAimAssistPresetDefinition as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let operation = <EnumsCameraAimAssistPresetsPacketOperation as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            camera_aim_assist_presets,
            camera_aim_assist_categories,
            operation,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientCameraAimAssistPacket {
    pub camera_preset_id: String,
    pub action: EnumsClientCameraAimAssistPacketAction,
    pub allowaimassist: bool,
}
impl crate::bedrock::codec::BedrockSized for ClientCameraAimAssistPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.camera_preset_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientCameraAimAssistPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.camera_preset_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.action.encode(buf)?;
        self.allowaimassist.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_preset_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let action = <EnumsClientCameraAimAssistPacketAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let allowaimassist = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            camera_preset_id,
            action,
            allowaimassist,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientMovementPredictionSyncPacket {
    pub actor_data_flag: ActorDataFlagComponent,
    pub actor_bounding_box: ActorDataBoundingBoxComponent,
    pub movement_attributes: MovementAttributesComponentjson,
    pub actor_unique_id: ActorUniqueId,
    pub actor_flying_state: bool,
}
impl crate::bedrock::codec::BedrockSized for ClientMovementPredictionSyncPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_data_flag);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_bounding_box);
        size += (&self.movement_attributes)
            .iter()
            .map(|_item| 4usize)
            .sum::<usize>();
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.actor_unique_id);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientMovementPredictionSyncPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.actor_data_flag.encode(buf)?;
        self.actor_bounding_box.encode(buf)?;
        for item in &self.movement_attributes {
            crate::bedrock::codec::F32LE(*item).encode(buf)?;
        }
        self.actor_unique_id.encode(buf)?;
        self.actor_flying_state.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let actor_data_flag =
            <ActorDataFlagComponent as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_bounding_box =
            <ActorDataBoundingBoxComponent as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let movement_attributes = {
            let res: MovementAttributesComponentjson = {
                let mut values = Vec::with_capacity(9);
                for _ in 0..9 {
                    values
                        .push(
                            <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(
                                    buf,
                                    (),
                                )?
                                .0,
                        );
                }
                match values.try_into() {
                    Ok(array) => array,
                    Err(_) => {
                        unreachable!("fixed-array decoder produced the wrong length")
                    }
                }
            };
            res
        };
        let actor_unique_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let actor_flying_state = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            actor_data_flag,
            actor_bounding_box,
            movement_attributes,
            actor_unique_id,
            actor_flying_state,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct UpdateClientOptionsPacket {
    pub graphics_mode_change: Option<EnumsGraphicsMode>,
    pub filter_profanity_change: Option<bool>,
}
impl crate::bedrock::codec::BedrockSized for UpdateClientOptionsPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            1usize
                + match &self.graphics_mode_change {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.filter_profanity_change {
                    Some(_v) => 1usize,
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for UpdateClientOptionsPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        match &self.graphics_mode_change {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.filter_profanity_change {
            Some(v) => {
                buf.put_u8(1);
                (*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let graphics_mode_change = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<EnumsGraphicsMode as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?)
            } else {
                None
            }
        };
        let filter_profanity_change = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<bool as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        Ok(Self {
            graphics_mode_change,
            filter_profanity_change,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerVideoCapturePacket {
    pub action: PlayerVideoCapturePacketAction,
}
impl crate::bedrock::codec::BedrockSized for PlayerVideoCapturePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerVideoCapturePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action =
            <PlayerVideoCapturePacketAction as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        Ok(Self { action })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerUpdateEntityOverridesPacket {
    pub target_id: ActorUniqueId,
    pub property_index: u32,
    pub update: PlayerUpdateEntityOverridesPacketUpdate,
}
impl crate::bedrock::codec::BedrockSized for PlayerUpdateEntityOverridesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
            self.property_index,
        ));
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.update);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerUpdateEntityOverridesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_id.encode(buf)?;
        crate::bedrock::codec::VarUInt(self.property_index).encode(buf)?;
        self.update.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_id = <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let property_index =
            <crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let update = <PlayerUpdateEntityOverridesPacketUpdate as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            target_id,
            property_index,
            update,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PlayerLocationPacket {
    pub target_actor_id: ActorUniqueId,
    pub location: PlayerLocationPacketLocation,
}
impl crate::bedrock::codec::BedrockSized for PlayerLocationPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.target_actor_id);
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.location);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PlayerLocationPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.target_actor_id.encode(buf)?;
        self.location.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let target_actor_id =
            <ActorUniqueId as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let location =
            <PlayerLocationPacketLocation as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            target_actor_id,
            location,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundControlSchemeSetPacket {
    pub control_scheme: EnumsControlSchemeScheme,
}
impl crate::bedrock::codec::BedrockSized for ClientboundControlSchemeSetPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.control_scheme);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundControlSchemeSetPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.control_scheme.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let control_scheme =
            <EnumsControlSchemeScheme as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { control_scheme })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PrimitiveShapesPacket {
    pub arrayofprimitiveshapescanbeamixofnewupdatedorremoved: Vec<PrimitiveShapeDataPayload>,
}
impl crate::bedrock::codec::BedrockSized for PrimitiveShapesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.arrayofprimitiveshapescanbeamixofnewupdatedorremoved).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.arrayofprimitiveshapescanbeamixofnewupdatedorremoved)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PrimitiveShapesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self
            .arrayofprimitiveshapescanbeamixofnewupdatedorremoved
            .len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.arrayofprimitiveshapescanbeamixofnewupdatedorremoved {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let arrayofprimitiveshapescanbeamixofnewupdatedorremoved = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <PrimitiveShapeDataPayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self {
            arrayofprimitiveshapescanbeamixofnewupdatedorremoved,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerboundPackSettingChangePacket {
    pub pack_id: MceUuiDjson,
    pub pack_setting_name: String,
    pub pack_setting_value: ServerboundPackSettingChangePacketPackSettingValue,
}
impl crate::bedrock::codec::BedrockSized for ServerboundPackSettingChangePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pack_id);
        size += {
            let _len = (&self.pack_setting_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.pack_setting_value);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerboundPackSettingChangePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.pack_id.encode(buf)?;
        let bytes = (&self.pack_setting_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.pack_setting_value.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let pack_id = <MceUuiDjson as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let pack_setting_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let pack_setting_value = <ServerboundPackSettingChangePacketPackSettingValue as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            pack_id,
            pack_setting_name,
            pack_setting_value,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundDataStorePacket {
    pub updates: Vec<ClientboundDataStorePacketUpdatesItem>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundDataStorePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.updates).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.updates)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundDataStorePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.updates.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.updates {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let updates = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <ClientboundDataStorePacketUpdatesItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self { updates })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct GraphicsOverrideParameterPacket {
    pub parameter_keyframe_values: Vec<GraphicsOverrideParameterPacketParameterKeyframeValuesItem>,
    pub float_value: Option<f32>,
    pub vec_3_value: Option<Vec3>,
    pub biome_identifier: String,
    pub player_identifier: Option<String>,
    pub identifierfor_parameter: EnumsGraphicsOverrideParameterType,
    pub reset_parameter: bool,
}
impl crate::bedrock::codec::BedrockSized for GraphicsOverrideParameterPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.parameter_keyframe_values).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.parameter_keyframe_values)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            1usize
                + match &self.float_value {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.vec_3_value {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            let _len = (&self.biome_identifier).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            1usize
                + match &self.player_identifier {
                    Some(_v) => {
                        let _len = (_v).as_bytes().len();
                        crate::bedrock::codec::BedrockSized::encoded_size(
                            &crate::bedrock::codec::VarUInt(_len as u32),
                        ) + _len
                    }
                    None => 0usize,
                }
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.identifierfor_parameter);
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for GraphicsOverrideParameterPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.parameter_keyframe_values.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.parameter_keyframe_values {
            item.encode(buf)?;
        }
        match &self.float_value {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::F32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.vec_3_value {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        let bytes = (&self.biome_identifier).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        match &self.player_identifier {
            Some(v) => {
                buf.put_u8(1);
                let bytes = (v).as_bytes();
                let len = bytes.len();
                crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
                buf.put_slice(bytes);
            }
            None => buf.put_u8(0),
        }
        self.identifierfor_parameter.encode(buf)?;
        self.reset_parameter.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let parameter_keyframe_values = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <GraphicsOverrideParameterPacketParameterKeyframeValuesItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let float_value = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        let vec_3_value = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<Vec3 as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?)
            } else {
                None
            }
        };
        let biome_identifier = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let player_identifier = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some({
                    let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0) as usize;
                    if buf.remaining() < len {
                        return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                            declared: len,
                            available: buf.remaining(),
                        });
                    }
                    let mut bytes = vec![0u8; len];
                    buf.copy_to_slice(&mut bytes);
                    crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                })
            } else {
                None
            }
        };
        let identifierfor_parameter =
            <EnumsGraphicsOverrideParameterType as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?;
        let reset_parameter = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            parameter_keyframe_values,
            float_value,
            vec_3_value,
            biome_identifier,
            player_identifier,
            identifierfor_parameter,
            reset_parameter,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerboundDataStorePacket {
    pub update: BedrockDduiDataStoreUpdate,
}
impl crate::bedrock::codec::BedrockSized for ServerboundDataStorePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.update);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerboundDataStorePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.update.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let update =
            <BedrockDduiDataStoreUpdate as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { update })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundDataDrivenUiShowScreenPacket {
    pub screen_id: String,
    pub form_id: u32,
    pub data_instance_id: Option<u32>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundDataDrivenUiShowScreenPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.screen_id).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 4usize;
        size += {
            1usize
                + match &self.data_instance_id {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundDataDrivenUiShowScreenPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.screen_id).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        crate::bedrock::codec::U32LE(self.form_id).encode(buf)?;
        match &self.data_instance_id {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::U32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let screen_id = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let form_id =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let data_instance_id = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        Ok(Self {
            screen_id,
            form_id,
            data_instance_id,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundDataDrivenUiCloseScreenPacket {
    pub form_id: Option<u32>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundDataDrivenUiCloseScreenPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            1usize
                + match &self.form_id {
                    Some(_v) => 4usize,
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundDataDrivenUiCloseScreenPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        match &self.form_id {
            Some(v) => {
                buf.put_u8(1);
                crate::bedrock::codec::U32LE(*v).encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let form_id = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0,
                )
            } else {
                None
            }
        };
        Ok(Self { form_id })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundDataDrivenUiReloadPacket {}
impl crate::bedrock::codec::BedrockSized for ClientboundDataDrivenUiReloadPacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundDataDrivenUiReloadPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundTextureShiftPacket {
    pub action_id: EnumsClientboundTextureShiftPacketPayloadAction,
    pub collection_name: String,
    pub from_step: String,
    pub to_step: String,
    pub all_steps: Vec<String>,
    pub current_length_in_ticks: u64,
    pub total_length_in_ticks: u64,
    pub enabled: bool,
}
impl crate::bedrock::codec::BedrockSized for ClientboundTextureShiftPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.action_id);
        size += {
            let _len = (&self.collection_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.from_step).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.to_step).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.all_steps).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.all_steps)
                .iter()
                .map(|_item| {
                    let _len = (_item).as_bytes().len();
                    crate::bedrock::codec::BedrockSized::encoded_size(
                        &crate::bedrock::codec::VarUInt(_len as u32),
                    ) + _len
                })
                .sum::<usize>()
        };
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::VarULong(self.current_length_in_ticks),
        );
        size += crate::bedrock::codec::BedrockSized::encoded_size(
            &crate::bedrock::codec::VarULong(self.total_length_in_ticks),
        );
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundTextureShiftPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action_id.encode(buf)?;
        let bytes = (&self.collection_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.from_step).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.to_step).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let len = self.all_steps.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.all_steps {
            let bytes = (item).as_bytes();
            let len = bytes.len();
            crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
            buf.put_slice(bytes);
        }
        crate::bedrock::codec::VarULong(self.current_length_in_ticks).encode(buf)?;
        crate::bedrock::codec::VarULong(self.total_length_in_ticks).encode(buf)?;
        self.enabled.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let action_id = <EnumsClientboundTextureShiftPacketPayloadAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let collection_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let from_step = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let to_step = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let all_steps = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push({
                        let len = (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0) as usize;
                        if buf.remaining() < len {
                            return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                                declared: len,
                                available: buf.remaining(),
                            });
                        }
                        let mut bytes = vec![0u8; len];
                        buf.copy_to_slice(&mut bytes);
                        crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
                    });
            }
            tmp_vec
        };
        let current_length_in_ticks =
            <crate::bedrock::codec::VarULong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let total_length_in_ticks =
            <crate::bedrock::codec::VarULong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let enabled = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            action_id,
            collection_name,
            from_step,
            to_step,
            all_steps,
            current_length_in_ticks,
            total_length_in_ticks,
            enabled,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct VoxelShapesPacket {
    pub shapes: Vec<VoxelShapesSerializableVoxelShape>,
    pub name_map: Vec<VoxelShapesPacketNameMapItem>,
    pub custom_shape_count: u16,
}
impl crate::bedrock::codec::BedrockSized for VoxelShapesPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.shapes).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.shapes)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += {
            let _len = (&self.name_map).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.name_map)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size += 2usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for VoxelShapesPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.shapes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.shapes {
            item.encode(buf)?;
        }
        let len = self.name_map.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.name_map {
            item.encode(buf)?;
        }
        crate::bedrock::codec::U16LE(self.custom_shape_count).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let shapes = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <VoxelShapesSerializableVoxelShape as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let name_map = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <VoxelShapesPacketNameMapItem as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        let custom_shape_count =
            <crate::bedrock::codec::U16LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        Ok(Self {
            shapes,
            name_map,
            custom_shape_count,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraSplinePacket {
    pub camera_data_splines: Vec<SharedTypesv1260CameraSplineDefinition>,
}
impl crate::bedrock::codec::BedrockSized for CameraSplinePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.camera_data_splines).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.camera_data_splines)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraSplinePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.camera_data_splines.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.camera_data_splines {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_data_splines = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <SharedTypesv1260CameraSplineDefinition as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            camera_data_splines,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct CameraAimAssistActorPriorityPacket {
    pub camera_aim_assist_actor_priority_list: Vec<CameraAimAssistActorPriorityPriorityData>,
}
impl crate::bedrock::codec::BedrockSized for CameraAimAssistActorPriorityPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.camera_aim_assist_actor_priority_list).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.camera_aim_assist_actor_priority_list)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for CameraAimAssistActorPriorityPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.camera_aim_assist_actor_priority_list.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.camera_aim_assist_actor_priority_list {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let camera_aim_assist_actor_priority_list = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec
                    .push(
                        <CameraAimAssistActorPriorityPriorityData as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self {
            camera_aim_assist_actor_priority_list,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResourcePacksReadyForValidationPacket {}
impl crate::bedrock::codec::BedrockSized for ResourcePacksReadyForValidationPacket {
    fn encoded_size(&self) -> usize {
        0usize
    }
}
impl crate::bedrock::codec::BedrockCodec for ResourcePacksReadyForValidationPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        Ok(Self {})
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct LocatorBarPacket {
    pub waypoints: Vec<LocatorBarWaypointPayload>,
}
impl crate::bedrock::codec::BedrockSized for LocatorBarPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.waypoints).len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + (&self.waypoints)
                .iter()
                .map(|_item| crate::bedrock::codec::BedrockSized::encoded_size(_item))
                .sum::<usize>()
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for LocatorBarPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.waypoints.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        for item in &self.waypoints {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let waypoints = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            let mut tmp_vec =
                crate::bedrock::codec::prepare_decode_vec(len, buf.remaining(), None)?;
            for _ in 0..len {
                crate::bedrock::codec::reserve_decode_item(&mut tmp_vec)?;
                tmp_vec.push(
                    <LocatorBarWaypointPayload as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                );
            }
            tmp_vec
        };
        Ok(Self { waypoints })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PartyChangedPacket {
    pub party_info: Option<PlayerPartyInfo>,
}
impl crate::bedrock::codec::BedrockSized for PartyChangedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            1usize
                + match &self.party_info {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PartyChangedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        match &self.party_info {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let party_info = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(<PlayerPartyInfo as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?)
            } else {
                None
            }
        };
        Ok(Self { party_info })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerboundDataDrivenScreenClosedPacket {
    pub form_id: u32,
    pub close_reason: String,
}
impl crate::bedrock::codec::BedrockSized for ServerboundDataDrivenScreenClosedPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4usize;
        size += {
            let _len = (&self.close_reason).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerboundDataDrivenScreenClosedPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::U32LE(self.form_id).encode(buf)?;
        let bytes = (&self.close_reason).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let form_id =
            <crate::bedrock::codec::U32LE as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?
                .0;
        let close_reason = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            form_id,
            close_reason,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SyncWorldClocksPacket {
    pub data: SyncWorldClocksPacketData,
}
impl crate::bedrock::codec::BedrockSized for SyncWorldClocksPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SyncWorldClocksPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let data =
            <SyncWorldClocksPacketData as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundAttributeLayerSyncPacket {
    pub data: ClientboundAttributeLayerSyncPacketData,
}
impl crate::bedrock::codec::BedrockSized for ClientboundAttributeLayerSyncPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.data);
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundAttributeLayerSyncPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.data.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let data = <ClientboundAttributeLayerSyncPacketData as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self { data })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerStoreInfoPacket {
    pub client_store_entry_point_configuration:
        Option<ServerConfigurationClientStoreEntryPointConfiguration>,
}
impl crate::bedrock::codec::BedrockSized for ServerStoreInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            1usize
                + match &self.client_store_entry_point_configuration {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerStoreInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        match &self.client_store_entry_point_configuration {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let client_store_entry_point_configuration = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ServerConfigurationClientStoreEntryPointConfiguration as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            client_store_entry_point_configuration,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ServerPresenceInfoPacket {
    pub presence_configuration: Option<ServerConfigurationPresenceConfiguration>,
}
impl crate::bedrock::codec::BedrockSized for ServerPresenceInfoPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            1usize
                + match &self.presence_configuration {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ServerPresenceInfoPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        match &self.presence_configuration {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let presence_configuration = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ServerConfigurationPresenceConfiguration as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            presence_configuration,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ClientboundUpdateSoundDataPacket {
    pub server_sound_handle: ServerSoundHandle,
    pub stop: Option<ClientboundUpdateSoundDataPacketStop>,
    pub set_volume: Option<ClientboundUpdateSoundDataPacketSetVolume>,
    pub set_pitch: Option<ClientboundUpdateSoundDataPacketSetPitch>,
    pub fade: Option<ClientboundUpdateSoundDataPacketFade>,
    pub seek_to: Option<ClientboundUpdateSoundDataPacketSeekTo>,
    pub pause: Option<ClientboundUpdateSoundDataPacketPause>,
    pub resume: Option<ClientboundUpdateSoundDataPacketResume>,
}
impl crate::bedrock::codec::BedrockSized for ClientboundUpdateSoundDataPacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += crate::bedrock::codec::BedrockSized::encoded_size(&self.server_sound_handle);
        size += {
            1usize
                + match &self.stop {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.set_volume {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.set_pitch {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.fade {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.seek_to {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.pause {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size += {
            1usize
                + match &self.resume {
                    Some(_v) => crate::bedrock::codec::BedrockSized::encoded_size(_v),
                    None => 0usize,
                }
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for ClientboundUpdateSoundDataPacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.server_sound_handle.encode(buf)?;
        match &self.stop {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.set_volume {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.set_pitch {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.fade {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.seek_to {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.pause {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        match &self.resume {
            Some(v) => {
                buf.put_u8(1);
                v.encode(buf)?;
            }
            None => buf.put_u8(0),
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let server_sound_handle =
            <ServerSoundHandle as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let stop = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketStop as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let set_volume = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketSetVolume as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let set_pitch = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketSetPitch as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let fade = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketFade as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let seek_to = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketSeekTo as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let pause = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketPause as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        let resume = {
            let present = u8::decode(buf, ())?;
            if present != 0 {
                Some(
                    <ClientboundUpdateSoundDataPacketResume as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            } else {
                None
            }
        };
        Ok(Self {
            server_sound_handle,
            stop,
            set_volume,
            set_pitch,
            fade,
            seek_to,
            pause,
            resume,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct SendPartyDestinationCookiePacket {
    pub cookie: String,
    pub intent: String,
    pub destination_name: String,
}
impl crate::bedrock::codec::BedrockSized for SendPartyDestinationCookiePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.cookie).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.intent).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += {
            let _len = (&self.destination_name).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for SendPartyDestinationCookiePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.cookie).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.intent).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        let bytes = (&self.destination_name).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let cookie = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let intent = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let destination_name = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        Ok(Self {
            cookie,
            intent,
            destination_name,
        })
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct PartyDestinationCookieResponsePacket {
    pub cookie: String,
    pub accepted: bool,
}
impl crate::bedrock::codec::BedrockSized for PartyDestinationCookieResponsePacket {
    fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += {
            let _len = (&self.cookie).as_bytes().len();
            crate::bedrock::codec::BedrockSized::encoded_size(&crate::bedrock::codec::VarUInt(
                _len as u32,
            )) + _len
        };
        size += 1usize;
        size
    }
}
impl crate::bedrock::codec::BedrockCodec for PartyDestinationCookieResponsePacket {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes = (&self.cookie).as_bytes();
        let len = bytes.len();
        crate::bedrock::codec::VarUInt(len as u32).encode(buf)?;
        buf.put_slice(bytes);
        self.accepted.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, crate::bedrock::error::DecodeError> {
        let _ = buf;
        let cookie = {
            let len =
                (<crate::bedrock::codec::VarUInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as usize;
            if buf.remaining() < len {
                return Err(crate::bedrock::error::DecodeError::StringLengthExceeded {
                    declared: len,
                    available: buf.remaining(),
                });
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            crate::bedrock::codec::decode_utf8_lossy_owned(bytes)
        };
        let accepted = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self { cookie, accepted })
    }
}
