# Protocol deviations

No protocol deviations were found in the five Task 0.4 fixtures.

The byte corpus was generated with gophertunnel commit
`9948b1729395d2e819fce28e079d4a7bfc67716c` and decoded/re-encoded with
Valentine/Jolyne from axolotl-stack commit
`6f6806e821a579c183c44d786f76d9b358a2b825`. `NetworkSettings`, `StartGame`,
`LevelChunk`, `MovePlayer`, and `AddActor`/Valentine `AddEntity` decoded to the
expected fields and re-encoded byte-for-byte.
