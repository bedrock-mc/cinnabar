# Vanilla Texture Vertical Slice Progress

Plan: `docs/superpowers/plans/2026-07-10-vanilla-texture-vertical-slice.md`
Branch base: `d7711e0`

Task 1: complete (commits 6e951ae..ee1b1ce, review clean; offline PowerShell/Git Bash contracts green; no Mojang payload tracked)

Task 2: complete (code integrated at e024dc4; exporter tests/vet green; 16,913 records and deterministic SHA-256 f3b8689d7be5189d503a26f47224939d83a441a05d9259806c1fd08d1753e07c independently verified; no PNG or asset JSON payload markers)

Task 8 diagnostic checkpoint: complete (commit ba0b39d; binding `teleport_settle_ms` remains independent from secondary `forced_full_view_remesh_ms`; stage diagnostics and deferred resource sampling reviewed clean; exact cohort/GPU fence still open)

Task 8A exact cohort: complete (commit 33889d9; 117 tests + strict clippy/fmt green; independent review clean; exact 1,089-column target/source exclusion and blocker progress evidence; GPU/present fence still open)

Task 8B presented-frame gate: complete (render commit 2f27eed, client commit d84ca15; 32+14+9 render and 102+17+10 client tests, strict clippy/fmt green; exact manifest, adjacent GPU-complete frames, and FIFO source capture independently reviewed clean; live Task 8C evidence still open)

Task 8C acceptance validator: complete (commit 734fa57; target-prefixed exact proof parsing, cohort/manifest continuity, independent 2s gates, interval frame cap, and recomputed schema-v2 30-s resource evidence; PowerShell suite and independent review clean; Rust proof emission remains open)

Task 8C Rust proof emission: complete (commit 16fc4bb; exact resident-plus-known-air forced manifest, independent teleport/remesh proof JSON and markers, adjacent GPU-complete frames, deterministic hashes/null stages, and interrupted-probe reset; client 116+19+10, render 32+14+9, acceptance, strict clippy/fmt/diff green; schema and runtime reviews clean; fresh live evidence remains open)

Cutout-leaves implementation plan: complete/integrated (commits 502b738, e729603, c81af2e; final independent review clean; implementation intentionally follows Task 8 acceptance closure)

Task 8E omitted-SubChunk lifecycle: complete (commit db24278; per-Y two-second deadlines, two exact-Y retries, terminal no-air completion, FIFO-preserving bounded direct/deferred scheduling, eviction/inline cleanup, exact progress stats; 110+17+10 client tests, acceptance dry-run, and strict clippy/fmt/diff green; first review FIFO Important plus explicit-transient equivalent fixed, focused final re-review clean)

Task 8E live retry correction: complete (commit a9cbb12; transport-confirmed deadline arming, inbound-first network fairness, submit-time reply protection, bounded overlapping-attempt correlation, and first-fatal preservation; client 151, render 55, protocol 94, acceptance, strict clippy/fmt/diff green; independent review clean). Live runs 20260711T102936Z-8188 and 20260711T103334Z-39712 proved the pre-fix retry storm and local batch >1600 failure; a defensive Go producer cap remains open before rerun.

Task 8E local relay batch cap: complete (commit 1434bd6; upstream-BDS to local-Rust batches are flushed at an exact 1,600-packet ceiling without changing the reverse Lunar relay path; full Go tests, vet, gofmt, diff checks, and independent review clean).

Task 8E live network round-robin: complete (commit 02d9e4b; connection-local deterministic inbound/command alternation prevents both inbound and outbound starvation while preserving strict shutdown priority and command FIFO; focused network tests, full 151-test client suite, strict clippy/fmt/diff, live world readiness, and independent review clean).

Task 8G bounded network control liveness: complete (commits 180ea07 and f4e214c; controls use an independent exact-64 FIFO drained before admission-gated world data, and one bounded pending world event keeps the actual single worker able to consume commands and publish ACKs while the exact-4 world FIFO remains full; exact deadlock RED, focused 11-test network gate, full 156-test client suite, strict clippy/fmt/diff, and final independent rereview clean). Fresh capped live acceptance remains open.

Task 8G BDS readiness harness: complete (commit 7c35d71; primary log-marker readiness now has a BDS-only strict RakNet unconnected-pong fallback for child-side buffered stdout, with wrong-ID/wrong-magic regression coverage, preserved continuous log capture and cleanup, fresh full dry-run verification, and independent review clean).

Cutout leaves Task 1 independent block semantics: complete (commit f768cfa; BREG1002/MCBEAS02 version 2, exact independent air/cube/occluder/leaf flags with invalid and old-schema rejection, deterministic registry SHA 8a27e1389f5ffa2e2ab032563a45660dc31f5d708fdacb2225b344a49aa15bfc and exact 16913/713/669/44/1 counts; focused and broad Go/Rust tests, strict hygiene, no Mojang payload, and independent review clean).

Cutout leaves Task 2 materials and mips: complete (commit 4d23356; leaf-only bit-8 materials, exact 0x10f mask validation, deduplicated layers, raw RGB-preserving 21-step Q16 alpha coverage correction with global smaller-scale ties, deterministic cutout reporting, focused 32/full 57 asset tests, strict hygiene, and independent review clean). Ignored schema-2 blob SHA a626666c540c88b9457578e9f030bf4ee116f8b7b20891e5fce905b976f6c7c9 contains 405 materials, 11 cutout materials, and 372 layers.

Cutout leaves Task 3 palette-native meshing: complete (commit f33b71c; storage-palette facts, independent geometry/occluder/leaf u64 masks, exact ordered internal/all-boundary culling, diagnostic non-occlusion, classifier-authoritative air, non-occluder cave connectivity, world-graph integration, unchanged eight-byte quads; render 32+22+9 and client 128+19+10 tests, strict hygiene/flat-array scan, and final independent review clean).

Cutout leaves Task 4 opaque-shader cutout: complete (commit 8391a58; flat bit-8 material flags, exactly one texture sample, strict alpha-below-0.5 conditional discard, opaque alpha ignored, surviving alpha forced to one, unchanged eight-byte records and single depth-writing no-blend Opaque3d pipeline/bind group/MDI-direct architecture; render 32+22+10 and client 128+19+10 tests, strict hygiene, and independent review clean).

Phase 2.5 biome asset rules: complete (commits 4e7179b..578ec80; BIOREG01, MCBEAS03, eight tint maps, six resolved tint classes plus flags, fallback-zero live resolution, strict synthetic error/classification coverage; assets 69 tests + docs, strict clippy/fmt/workspace checks, independent re-review clean).

Phase 2.5 live biome runtime integration: complete (commits b55cdc5..6694383; lossless custom IDs, palette-native dense records, collision-free stream/revision identity, stale-work rejection, six-class WGSL routing, actual direct/MDI/upload/ack/bind-group tests; protocol/render/client suites and strict hygiene green, independent re-review clean). Real water-material production remains Phase 2.6.

Phase 2.6 Task 5 MCBEAS04 schema foundation: complete (commit 09f5831; range 2fd8b20..09f5831). The checked v4 codec/runtime now carries typed visual kind plus BREG1003 contributor role, render/tint/animation materials, fixed-point model templates/quads with UV and face/cull metadata, complete flipbook selector metadata and frame TextureRefs, one/two independently hashed texture pages, textures, and biomes. Every new section has canonical bounds/cross-reference/malformed decoder coverage; selected flipbooks and static strip aliases remain distinct by texture-key identity. Fresh evidence: assets 14+10+8+24+40+15 tests, strict assets Clippy, locked workspace all-target check, fmt/diff checks, and exact pinned-pack compile green (16,913 visuals, 377 materials, 397 layers, 88 biomes, 2,936,632 bytes, SHA-256 2e345f2993a5d68f5d1c8cdd06f32e270021e2c898b1a0686e2e3d7e3187fb). Independent final review reported no Critical/Important findings. Broader Phase 2.6 visual-family/template population remains open, so no plan checkbox is closed by this schema-only checkpoint.

Phase 2.6 Task 6 animation GPU resources: complete (commit a30a0ef; range 3e2e7db..a30a0ef, independent review clean). One shared bind group now carries bounded one/two-page textures plus immutable material/animation/frame tables and a stable 16-byte clock; current/next selection covers cross-page blend, wrap, nonblend, static materials, and a real page-one diagnostic fallback. Per-frame animation writes only the clock buffer; texture preparation remains revision-gated and atomically retains the previous prepared revision on failure. Fresh render all-target tests 82/82, strict render Clippy, fmt/diff, Naga validation, and independent spec/quality verdicts are green with zero findings.

Phase 2.6 Task 7 bounded multi-stream rendering: complete (commit 5734872; range 59655eb..5734872, independent re-review clean). ChunkMesh and the GPU handoff now carry named cube/model/model-lighting/liquid/liquid-lighting streams with exact combined byte accounting, the original PackedQuad fixed at eight bytes, PackedModelRef fixed at 16 bytes, transactional all-stream allocation/rollback/retry, exact generation/revision/tint gates, expected/drawn presentation masks, empty-stream behavior, and one direct/MDI address source. Review fixes consolidated four logical non-cube streams into one bounded u32-addressed physical arena (projected vertex storage bindings 7 <= common minimum 8 including a future template table) and replaced O(n^2) frame completion with keyed O(n log n) lookup. Fresh render all-target tests 89/89, strict Clippy/fmt/diff, and independent spec/quality verdicts are green with zero remaining findings.

Phase 2.6 Task 8 face-specific lighting sidecars: complete (commit 8b5c5a6; range 0fc4f96..8b5c5a6, independent re-review clean). A palette-native MeshNeighbourhood retains center plus all 26 bounded optional snapshots with explicit open fallback; worker-side sidecars pack block 0, sky 15, face-specific AO, and zero reserved bits into stable eight-byte per-quad records. Generation-scoped asset-aware diagonal AO/liquid masks now cover block updates, individual subchunks, inline full columns, removal/known-air replacement, and eviction; stale registration is rejected, exact current known-air is recorded empty, and unknown/new remains conservative. Fresh render 93/93, world 51/51, client 187/187, focused lifecycle/store/neighbourhood/lighting gates, strict combined Clippy/fmt/diff, and independent spec/quality verdicts are green with zero remaining findings.

Phase 2.6 Task 9 terrestrial crossed plants/crops: complete (commit a24370b; range ff739ac..a24370b, final controller re-review clean). The checked compiler maps all 443 terrestrial Cross/Crop states (279 Cross, 164 Crop) to reusable fixed-point two-quad templates with exact aliases, growth/facing variants, cutout/tint materials, and zero diagnostics while excluding aquatic seagrass/kelp. Palette-native meshing emits compact 16-byte model refs plus ordered Task8 lighting, no diagnostic cube faces, and open connectivity; zero templates are skipped and guarded before all WGSL reads. One shared 14-entry bind group stays at eight vertex storage buffers, the no-cull model pipeline preserves Task6 page/animation resources, and direct/MDI commands share exact addresses/presentation masks. The exhaustive 443-state gallery binds registry/blob/state hashes, measured zero diagnostics, a pose-independent complete relative layout, and proven front/back framing; fresh-source BDS identity handling is exact-not-found only and fail-closed for malformed/reparse/inaccessible paths. Fresh assets 112, render 102, world 51, client 187, acceptance, strict Clippy/fmt/diff, and final spec/quality verdicts are green with zero findings.

Phase 2.6 Task 14 vine deterministic acceptance: implementation complete (commits 489af26..eec96e2; review and corrected re-review clean). Five canonical poses bind all exact protocol-1001 masks 0..15 to BREG1003/MCBEAS04 identities, preserve mask 0 as zero-draw, construct isolated bit-exact supports, fence the committed camera, and require adjacent GPU-completed witnesses over the exact requested subchunks with stable nonzero total and zero contamination counters. Live run 20260714T023010Z-7488 passed adjacent sequences 461/462 with four keys and 94 stable refs; it failed only the shared 100 ms performance gate at 138.9454 ms. Acceptance/runtime-safety suites and diff hygiene are green; native evidence remains blocked by the RX 570 Bevy/wgpu presentation failure, exact drawing is complete, and GPU-stage measurement/performance closure remain open.

Phase 2.6 exact visible-quad model drawing: implementation complete (commits fcb1989..b07e194, report kept local; review/re-review clean). Exact eight-byte draw refs, strict lighting ownership, six-index WGSL pulling, one direct/MDI command per subchunk, and unchanged model-ref witnesses passed full render/client suites, strict Clippy/fmt/shader validation, and release build. Live VineGallery run 20260714T030538Z-22388 passed adjacent GPU witnesses at sequences 191/192 with four keys and 95 stable refs, but performance was neutral (p50 40.6 ms, p99 47.7 ms, mutation 142.6 ms, 8,345 resident); GPU-stage timestamps/workload counters are next.

Phase 2.6 model workload telemetry: implementation complete pending live evidence. A cross-world renderer bridge now publishes resident and frustum-visible model-ref counts, exact model-draw-ref counts, and the former fixed 32-quad slot invocations avoided into deterministic acceptance JSON. Tests prove exact aggregation, incomplete-stream exclusion, schema propagation, and serialization; fresh render/client all-target suites are green. Bevy's supported asynchronous opaque/transparent GPU pass timestamps are next.

Phase 2.6 GPU pass telemetry: implementation complete pending live evidence. Acceptance/profiling runs enable Bevy's existing asynchronous render diagnostics only when requested, pair opaque and transparent 3D GPU measurements strictly by completed-frame timestamp, reject stale transparent reuse, deduplicate delayed samples, and retain constant-memory p50/p95/p99/max histograms. CPU fallback paths are not mislabeled as GPU evidence. Fresh client all-target tests (195 main + 28 assets + 14 camera), strict Clippy, format, and diff checks are green.

Phase 2.6 workload/GPU live evidence: complete in run 20260714T032404Z-12360. The run captured 1,296 GPU samples at 4.9 ms combined chunk-containing p50 / 10.2 ms p99 while total frame time stayed 40.2 / 47.6 ms. Exact drawing issued 80,233 visible quad refs for 29,083 model refs and avoided 850,423/930,656 former fixed-slot invocations (91.38%), proving model shader work is not the remaining frame bottleneck. Adjacent witnesses 801/802 passed over four keys and 92 stable refs with zero contamination; RSS 638,537,728 bytes and mean CPU 2.82% stayed within budget. The sole gate failure remained mutation-to-visible at 142.5286 ms. Next performance work targets frame scheduling/presentation and mutation-to-frame latency, not a speculative model pipeline split.

Phase 2.6 DX12 pacing diagnosis: complete without a production workaround. Code tracing isolates FIFO surface acquisition plus pipelined-render synchronization as the dominant unmeasured cadence stall; 2,210 frames/60s with a 40.2 ms median is a burst/stall distribution, and the 1.6374 ms transparent sort's 40.4356 ms request-to-commit independently matches one app cadence interval. AutoNoVsync run 20260714T033621Z-14584 never reached world-ready, and identical FIFO VineGallery with the frame-latency object set to `dontwait` (20260714T034035Z-22400) reached the clean gallery/camera fence but never produced a GPU-completed witness and resisted graceful shutdown. Retain wgpu's default wait path for correctness; presentation remains an external Bevy/wgpu/RX570-driver blocker.

Phase 2.6 exact huge-mushroom tranche: complete (rebased commits fbb36cc..1e0d0c3; review/re-review clean). All 48 canonical tagged `huge_mushroom_bits` states now preserve exact existing face/material selection while duplicate, missing, unknown, mistyped, and out-of-range selector JSON fails closed. The production ratchet removes exactly 48 intended IDs with zero additions; combined with the already-landed 128-state glow-lichen/sculk-vein tranche, the integrated baseline leaves 7,722 diagnostics and blob SHA-256 `bd6b8ecb73c4032be51d00dda42d8e5e1b0b55333d276b5cbfa001cb46d0abba`. Focused and full assets/visualcoverage suites, explicit production ratchet, strict Clippy/fmt/diff, and independent final re-review are green.

Phase 2.6 exact stained-glass cubes Task 1: complete (commit 43b0cb0; review/re-review clean). The checked compiler admits only the 16 ordinary stateless stained-glass names as alpha-blended six-quad unit-cube models, clears runtime cube/occlusion facts, and exports one standalone transparent-cube template semantic. Checked encoder/runtime boundaries validate exact ordered unit-cube topology, canonical face flags, six non-diagnostic blend materials, and incompatible-flag rejection. Focused pinned compiler/blob/runtime tests, full assets, strict Clippy/fmt/diff, and independent final review are green; renderer culling and the 16-state production ratchet remain Task 2.

Phase 2.6 exact stained-glass cubes Task 2: complete (commit a603a94; independent review clean with the sole stale registry-SHA documentation Minor corrected before push). CPU meshing now derives each checked transparent cube's boundary from its canonical face field, suppresses a shared face only for equal six-face material identities, preserves both cross-colour faces, retains opaque/glass asymmetry, remains cave-open, and works across all six subchunk boundaries. The exact stateless `black`, `blue`, `brown`, `cyan`, `gray`, `green`, `light_blue`, `light_gray`, `lime`, `magenta`, `orange`, `pink`, `purple`, `red`, `white`, and `yellow` ordinary stained-glass records remove 16 diagnostics with zero additions; Education `hard_*` glass, panes, copper grates, slime, legacy flags-zero records, and `minecraft:invisible_bedrock` remain excluded. The refreshed baseline leaves 7,706 diagnostics, records 7,235 cumulative removals, and binds ignored integrated blob SHA-256 `61025bb3e8e1b9ca0d5e2ec1cd7847433333a20f99948c6193fbb370a0d4900f`; the preceding huge-mushroom checkpoint remains historical at 7,722.

Phase 2.6 exact copper grates: complete (commits fcf8489..c66f9b4; review/fix/re-review clean). The checked compiler admits only the eight canonical stateless non-air primary Cube records with exact drawable flags as homogeneous alpha-cutout six-quad transparent cubes, preserves all four waxed/unwaxed face-material aliases, and rejects AIR, flags-zero, and other mismatched admission facts. Encoder/runtime trust boundaries accept homogeneous blend or cutout templates and reject mixed, both-bit, opaque, diagnostic, incompatible, wrong-count, and malformed inputs. Meshing uses exact network identity in sequential and hashed modes, retaining wax/oxidation boundaries while identical states cull across every subchunk face; grates stay cave-open and never enter the transparent draw stream. The production ratchet removes exact IDs 1963, 2219, 5474, 6812, 9255, 9256, 13058, and 16113 with zero additions. Combined after static signs, the current baseline is 2,826 diagnostics and the regenerated ignored blob SHA-256 is `22037ddea89873f074a9940163857a66639a44f18bfd0cefa26f6f8e3e1c1065`; focused/full/ignored exact tests, strict Clippy/fmt/diff, and independent final re-review are green.

Finite clouds Task 1: complete (integration commit 03a8c3a; independent spec/quality review clean with one representability-only packing Minor deferred to final review). Deterministic 256x256 alpha occupancy, toroidal seam culling, greedy exposed faces, exact eight-byte records, explicit 196,608-record/1,572,864-byte ceilings, and canonical snapped 3x3 origins passed 10 focused tests, the full render suite, strict render Clippy, fmt, and diff checks. GPU rendering and live acceptance remain open.

Graphics metadata startup ordering: complete (integration commit a1c217e; independent spec/quality review clean). The primary-surface metadata probe now runs after Bevy's deferred `RenderSystems::ExtractCommands` barrier and before surface creation, preventing first-frame `VisibilityDiagnosticsInput` validation panics and the resulting dropped-command unwind warnings. Focused, full render, workspace, strict Clippy/fmt, and combined-tip verification are green.

World publication performance Task 1: complete (integration commit 39c44e8; independent spec/quality review clean). Exact no-op and direct-sky-provenance-only light completions preserve sampled identity/generation and avoid mesh invalidation, actual nibble changes retain the full 27-dependent contract, and exact saturating accepted/no-op/value/provenance/invalidation counters are covered. Fresh client 276/277 with one existing release-only ignore, world, strict affected Clippy, fmt, and diff checks are green. Stage-latency authority, the full release benchmark, and live gates remain open.
