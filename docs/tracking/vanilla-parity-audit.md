# Vanilla parity audit backlog

Audited baseline: `ddc68e39efee8b8ab643e383b3d81cea10e915f4` on 2026-08-21.

This is the durable repository-wide mismatch inventory. It complements `plan.md`; it does not
close any implementation, native, visual, timing, performance, or review gate. Gameplay HUD
appearance follows the Java Edition exception in `AGENTS.md`. Packets, world behavior, menus,
inventory, controls, movement, actors, viewmodels, audio, and reconciliation remain Bedrock
authority surfaces unless that exception says otherwise.

Classification:

- **Proven mismatch**: current code directly contradicts the stated product contract or drops a
  required state/feature.
- **Open contract**: current behavior is an explicit policy or approximation that still needs an
  exact-version native witness before it can be changed or accepted.
- **Missing surface**: the required runtime path does not exist.
- **Deliberate deviation**: owner-approved bring-up behavior that must remain labeled incomplete.
- **Implementation landed**: the bounded code fix is locally integrated and reviewed, while any
  wider native/live or surrounding parity gate remains open.

## Landed audit tranches

| IDs | Reviewed task head | Integrated commits | Evidence state |
| --- | --- | --- | --- |
| Branch audit closeout (VPA-003/107/122/127/131/138/139/209/210 slices) | `dcf780f1` (base `59f1f8e2`) | `9b7b94fd`..`dcf780f1` | Repository-wide branch review findings were closed with TDD regressions: terminal boundaries wake immediately while unusable transfers remain semantic skips; corrections and actor motion preserve authoritative ticks; player-list and inventory projections cannot go stale/cross-window; failed connection and session cleanup is identity-safe; saved-server and archive inputs are bounded; auth-cache protection/quarantine operates on opened file identities and fails closed; Phase 3 rejects early/kicked sessions; synthetic input rechecks PID+HWND per event; asset publication is atomic no-replace; registrygen root witnesses run in CI. Full locked Rust workspace, strict Clippy, formatting, architecture, Go core/registry tests and vet, Bash acceptance/publisher suites, Windows auth-cache cross-compile, and a fresh full-range Sol-high review passed. Native/live and visual acceptance gates remain open. |
| VPA-003 | `0bf8ca75553796c7428ead5d247d18b10e1b5984` (base `5ac941bd`) | fast-forward on `dev/ox-alpha` (`ba093b81`, `264ff1b8`, `0bf8ca75`) | Bounded one-shot `ServerTransferEvent`, terminal `Transferred` pump classification with durable marker, Failed-family teardown without the RemoteClosed latch, and launcher auto-rejoin through a fresh core with an 8-hop bounded chain. Fresh independent review APPROVE; five Minors recorded in the P0 row. Coordinator post-integration protocol/client-world/app suites, strict workspace Clippy, formatting, architecture gate, and diff-check green. No live evidence claimed; FastTransferWitness rerun and live transfer-rejoin evidence remain open. |
| VPA-210, VPA-209, VPA-127 | recovered interrupted lane head `a56dea41` + fix `a758342b`, `248daba6` | `18edaf52` (merge on `dev/ox-alpha` carrying `c1dc2a25`..`248daba6`) | Session runtime directories are now owned through an identity-checked grammar-bound guard with marker-proven reclaim (provisional 24 h/64-entry bounds), junction/symlink-shaped names never reclaimed through the link, and explicit `--socket-dir` directories left operator-owned; archive extraction gained entry/per-file/expanded-byte/ratio bounds, link-entry rejection before extraction, stale-staging reclamation, transactional publication with an immediate pre-move absence recheck plus a deterministic PATH-shim race witness on both script legs; canonical item identity now binds wire block runtime ID into equality/validation with fail-visible retained-block routing. First review NEEDS CHANGES (bash TOCTOU publication); fix verified with a discriminating witness and negative control. Second-round re-review's sole Important finding ($null collector poisoning) was refuted by direct PowerShell experiment (`AutomationNull` does not append) and a live full contract-suite run (`vanilla asset contract tests passed`, exit 0, deep bash leg live); its Minors are recorded: Unix backslash `Join-Path` leaves the overwrite sub-assertion dead cross-platform, fragile chmod quoting edge, residual check-to-mv window undocumented. Earlier recorded Minors: PS-side deterministic race injection unwitnessed, interruption coverage proxied via stale-staging simulation, non-atomic owner-marker writes, age-based reclaim can strike a genuinely live >24 h session, duplicate-directory rejection diverges across scripts. Post-integration workspace tests, strict workspace Clippy, formatting, architecture gate, and diff-check green at the merge. |
| VPA-004 | `469fe1bb3bfcab865bb6bb2e468ebb4df5d9414d` | `e21e99b6` | App-owned verified cache and semantic-skip isolation; coordinator protocol/client suites, formatting, strict Clippy, architecture, and fresh Sol-high review passed. Pushed on `dev`. |
| VPA-008 | `582ac0a099123a61588fca90e9d55648a23e99ae` | `a63431b0`, `29e6baaa`, `6c7e2672` | Verified/predicted stack encoding, latest-wins retry, session reset, and server-forced cancellation; coordinator protocol/client suites, formatting, strict Clippy, architecture, and fresh Sol-high re-review passed. Pushed on `dev`. |
| VPA-005, VPA-006 | `a9f24e96ef1a395acc2f0cdbb5f48b4f6f0d4a12` | `76f8c87c` | Failed request columns stay outside loaded/cohort readiness, and request-mode LevelChunks join the required cohort only after decode and admission; coordinator client-world suites, formatting, strict Clippy, architecture, and fresh Sol-high review passed. Pushed on `dev`. |
| VPA-106 | `eed6a4fc8b14e5a57fdb0cce3dab22bbf56f60f5` | `9dfecb80` | Non-finite remote player poses and unknown equipment containers are skipped at the semantic boundary without ending the session or overwriting usable actor state; coordinator protocol suites, formatting, strict Clippy, architecture, and fresh Sol-high review passed. Pushed on `dev`. |
| VPA-112 | `2ae01aed0e98b0fa8d23ca6e630f843183dd21ab` | `c4ccc81c`, `5cbfbcea` | The player preview no longer appears in ordinary gameplay or any storage lifecycle state, while personal inventory retains it; coordinator app suites, formatting, strict Clippy, architecture, and fresh Sol-high re-review passed. Pushed on `dev`. |
| VPA-013 | `f5e1343c4a349e4ffc26cba37f443f26f13a06aa` | `25d87058` | The active liquid oracle is now upgraded to BedSim v0.1.5; its four-record output remains byte-identical to the prior v0.1.4 slice, so the landed Rust behavior is unchanged. Open-water bobbing, water gravity, and clear/blocked/submerged ledge exits remain covered; v0.1.5 swimming, flow, pose, knockback, item-use, and provider APIs are explicit follow-up port work. |
| VPA-009 | `2a17459881ea5f586b8e1cb937c4ad7337882924` | `9d6e291b`, `d90d8764` | One selected-stack snapshot now feeds packet construction and presentation, preserves unknown/empty/present prediction, restores rejected authority, and bounds the equipment bootstrap fallback. The full app suite, strict Clippy, formatting, architecture, and fresh Sol-high review passed. Integrated on `dev`. |
| VPA-103 | `14e2ceedf3fdc58a60141410731605cef03e4f81` | `2e6feae1`, `83198075`, `e0277132`, `1322fb49`, `8e647d01`, `eb017fac` | Malformed inner actor/item/inventory/chunk/NBT wire now stays fatal through semantic and stale gates, while complete semantic oddities remain survivable. Fatal decode is FIFO-attributed and terminal across decode/light/mesh/request/publication work. World, protocol, client-world, focused app, strict Clippy, formatting, architecture, and repeated fresh Sol-high review passed. Integrated on `dev`. |
| VPA-120 | local commit `c3ff43c5` (unpushed) | `c3ff43c5` | Core shutdown now closes the core's piped stdin (its own stdin-EOF cancellation path), waits bounded 1.5 s inside the shutdown-watchdog envelope, and kills only as fallback. Guard/spawn helpers extracted to `app/src/menu/core_process.rs`. Four process fixtures cover graceful EOF exit, kill fallback, idempotence, and replace-stops-previous; app lib suite 578 passed, Go suites green, strict workspace Clippy/formatting/architecture clean, fresh independent review passed after one Important test-assertion finding was fixed. Live telemetry/cache-sync verification on an orderly exit remains open. |
| VPA-139 | local commit `7f702af8` (unpushed) | `7f702af8` | Saved-server storage is now atomic with schema validation, quarantine, visible recovery, stale-temp clearing, and owner-only Unix modes; seven fixtures cover roundtrip, quarantine/recovery, failed publish, stale temps, and re-quarantine. App lib suite 585 passed, strict workspace Clippy/formatting/architecture clean, fresh independent review returned one review cycle (two Important findings fixed and re-approved). Windows ACL hardening stays under VPA-121/VPA-138; native multi-instance/power-loss evidence remains open. |
| VPA-132 | local commit `170ee5a9` (unpushed) | `170ee5a9` | Toasts now expire through an explicitly provisional 5 s duration (labeled provisional pending native Bedrock timing), prune in receive order with retained-byte accounting, and stop rendering at exact expiry. UI crate suites green including four toast witnesses; strict workspace Clippy/formatting clean; fresh independent review APPROVE with three Minors dispositioned (byte-budget assertion added, comment wording fixed, clock-domain hazard recorded as VPA-213). Native duration/transition measurement and a matching visual gate remain open. |
| VPA-134 | local commit `44472948` (unpushed) | `44472948` | Boss darken-sky/world-fog flags now reach the atmosphere frame through a bounded provisional air-only response with identity when unset; five render tests plus two environment derivation/application witnesses. Strict Clippy/formatting clean; fresh independent review APPROVE with Minors dispositioned (extra channel assertions and boss-over-profile witness added, night-mix semantics documented for calibration). Native capture/calibration and a visual gate remain open. |
| VPA-131 | local commit `b7a6b165` (unpushed) | `b7a6b165` | Player-list bindings now exist for keyboard Tab, gamepad RightThumb (provisional), and a reserved touch control; five router witnesses cover per-device press/hold/release, context disjointness, and focus-loss release reasons, and one end-to-end app witness drives Tab through the real input chain into the overlay. The atmosphere derivation extraction to `environment/atmosphere.rs` (cap repair) is reviewed in the same tranche. Input/app suites, strict Clippy/formatting/architecture clean, fresh independent review APPROVE. Native hold/timing acceptance and the touch layout remain open under VPA-118. |
| VPA-010 (slice) | `a37b2527` | `27f8ad70` (merge on `dev/ox-alpha`) | Device-class move-vector carriers are preserved through `PlayerAuthInput`: raw carries the controlling device's pre-normalization sample, analogue carries post-deadzone analog axes when the device provides them (device sample for digital devices), and processed stays the simulation vector byte-identical to prior behavior; non-finite carriers fail closed at the authority boundary and encoder. Independent review APPROVE; recorded Minors: touch raw is post-bounding, no mixed-device carrier witness, gamepad raw inherits the codebase-wide post-deadzone definition, correction-replay carrier retention unwitnessed. What vanilla Bedrock puts in each field per device class remains unmeasured â€” no vanilla claim is made. |
| disconnect observability (VPA-109 adjacent) | `d10daecc` | `1e4610db`, `02f8f78e` (merges on `dev/ox-alpha`) | Server `DisconnectPacket` reasons now normalize into a bounded vendor-neutral event instead of being dropped, ride session teardown into `NetworkControlEvent::Failed`, drive the menu "Disconnected: <reason>" message with transport fallback, and emit through the registered terminal marker; app metrics gain outbound PlayerAuthInput sent/drop counts, outbox depth high-water mark, and an authority-halt flag. Truncated wire stays fatal. Independent review APPROVE; recorded Minors: sent-count high-water semantics across session replacement untested, end-to-end oversized-reason bounding witness gap, pre-spawn kicks still surface transport-only, `absorb_boundary_packet` awaits nothing. Kick attribution for future live reruns is therefore observable; no live gate closed by this alone. |
| VPA-129 | `303811a4` + fix `aabfaef7` | `c2f56027` (merge on `dev/ox-alpha`) | Camera instruction families 300/159/73 admit into bounded vendor-neutral FIFO events and a capped ordered app resource with eager per-drain identity refresh; center-offset non-finite validation and zero-event-drain clearing landed in the fix cycle after one NEEDS CHANGES review, then fresh full-range re-review APPROVE with three documentation Minors recorded (session-replacement window wording, streamless-interval persistence note, epilogue file placement). Same-batch-as-dimension-switch window is accepted and documented at the identity site. Renderer wiring and native/live camera evidence remain open. |
| VPA-110 (fix) | `8766a39d` | `7bbdb6ef` (merge on `dev/ox-alpha`) | The NEEDS CHANGES review finding is closed on the integrated line: unstated stack-response fields now stay genuinely absent (`Option` overlays), a count-only correction no longer fabricates a zero durability fact that hid locally derived bars, rejected responses carry an overlay-survival witness, and selected-slot label/durability follow the traveling predicted half during in-flight gestures. Focused inventory suites green at the merge. Provisional damage-domain and omit-retains labels remain pending native measurement. |
| VPA-130 | `cac0305c` + fix `4e8c69ab` | `7e4c34ba` (merge on `dev/ox-alpha`) | Server forms admit through structural validation into a bounded id-keyed store, and the tested menu-form answer lifecycle stages latest-wins ModalFormResponse frames with restore-on-failure retry; the NEEDS CHANGES review's Important modal-boolean finding was fixed fail-closed and the full range received fresh re-review APPROVE with four Minors recorded (menu index range unchecked against unparsed elements, stale pending answer on same-id reissue, tracker wording owned by integrator, dormant seam has no production caller yet). Renderer/focus/transport wiring and live form acceptance remain open. |
| VPA-124 | local commit `ad39a22f` (unpushed) | `ad39a22f` | A local storage close with an admitted prediction now enters a bounded closing state that retains the window generation, container identity, and response journal; all new gestures are blocked while accepted/rejected responses keep reconciling against the retained generation. The last resolution, an authoritative close, or the existing 1.5 s timeout/recovery path settles it to today's exact teardown, and session reset or window replacement clears it immediately. Seven regression witnesses cover retention/blocking, accepted/rejected settlement, timeout boundedness, session reset, authoritative close, and replacement; app lib 606 passed, inventory_ledger 16 passed, generic_storage_container 21 passed, strict Clippy/formatting/architecture clean. Fresh independent review APPROVE with three Minors recorded (unmatched-request-id batch during Closing unpinned, close checks prediction generation but not identity, closing lacks a presentation accessor); live container-close acceptance remains open under Phase 5.5. |
| VPA-133 | `24b9cd04` + docs `160f06fc` (integrated on `dev/ox-alpha`) | `24b9cd04`, `160f06fc` | Scoreboard render-type classification and raw-id owner fallback landed with ui 13/13 plus retained_hud witnesses, strict Clippy/formatting/architecture green. Fresh independent review APPROVE; four Minors dispositioned in follow-up commits (provisional spacing label, carrier-absent degradation doc line, witness-sequence wording plus hearts re-display assertion, criteria-vocabulary authority recorded for the native gate). Native hearts/sidebar calibration and visual gates remain open in the P2 row. |
| VPA-018 | `35987782` + fix `6a9ec143` (base `f48cbed5`) | `525ee438` (merge on `dev/ox-alpha`) | MCBEAS07 world-carrier provenance identity (manifest + block/light/biome registry hashes), fail-closed startup substitution gates for world and atmosphere carriers, typed path-and-rebuild errors, deterministic compile binding through the CLI, direct absent/tampered identity decode witnesses, and startup-level mixed/stale substitution fixtures. Fresh independent review APPROVE; three Minors dispositioned (direct decode witness added, canonicalizer doc reworded to the true mismatch mechanism, diagnostic-path atmosphere-fatal behavior recorded as an intentional change). Post-integration assets/asset-compiler/app suites, strict Clippy/formatting, architecture check green. Old `.local` world carriers fail decode until rebuilt by design. |
| VPA-107 | `9138b9d6` + fix `352a5ddf` (base `58a44aa1`) | `0e48ebb2` (merge on `dev/ox-alpha`) | Packet-specific correction semantics: the vendored 2168 wire proves no mode field (only a Player/Vehicle rewind subject), so shapes derive from field combinations — exact-agreement Confirmed mutates nothing, retained-tick Replay keeps the proven velocity-retention contract, beyond-16-block displacement snaps like the teleport anchor; server yaw/pitch now reach the view pose for every admitted shape (provisional policy pending live observation of correction-heavy servers); non-finite delta/rotation skip at the semantic boundary while non-finite position keeps sentinel recovery. Protocol/client-world/app suites, strict Clippy/formatting, architecture green. Fresh independent review APPROVE; four Minors dispositioned (rotation-application camera-yank exposure labeled provisional, InvalidAnchor comment mechanism corrected, vehicle_angular_velocity drop documented at the event, plus two new witnesses: Confirmed leaves the settle window intact and TeleportSnap clears queued knockback overlays). Live rotation/correction behavior observation remains open under VPA-109's live-gate work. |
| Venity sustained-session rerun | reviewed head `352a5ddf` (live run, no code change) | — | Authenticated Venity CandidatePhysics run `phase3-3664fb796c044671bf9e8912182c39f0` at the reviewed correction head with `RUST_MCBE_MOVEMENT_TRACE=1`: 110 s session, zero decode errors, 1,687 transmitted physics samples with a complete matching PAI trace, and NO authority-fault violation (the prior `ox-alpha-venity-physics-e` run had one). The settle gate engaged at spawn, lifted twice by reanchors, and its third window timed out fail-open after exactly 200 suppressed ticks as designed. The session ended only when Venity's upstream RakNet read timed out against a stationary client (core log: `Network timed out`), reproducing the known idle-kick profile without any movement-cheat rejection text; the terminal evidence reported `terminal_outbox_not_drained` because the transport died mid-session while outbox reconciliation was non-authoritative. That terminal classification gap is now closed by the remote-close outbox tranche below. Remaining live-gate work is unchanged: organic-movement drivers so the client is not stationary and sustained multi-minute survival on both targets. |
| Remote-close outbox classification | `94d39415` + fix `0b08c802` (base `0fca215f`) | `8237f4cd` (merge on `dev/ox-alpha`) | A receive-origin `NetworkControlEvent::Failed` now latches a bounded `RemoteClosed` movement reconciliation classification that survives teardown and same-frame flushes, so a remote-initiated transport death of an active CandidatePhysics session no longer emits the production-evidence violation `terminal_outbox_not_drained` at pending depth zero; local stops, send-side failures, authority faults, pre-session tickers, FreeCamera, and every nonzero depth keep today's exact strict predicate, and the FastTransferWitness lane still requires exactly `Drained`. App lib 683 passed, strict Clippy/formatting/architecture/diff-check green, Phase3 Pester 93→94/94 green post-integration. Fresh independent review APPROVE; three Minors recorded (network.rs sits exactly at its 1000-line cap, `RemoteClosed` covers server kicks so it must never be read as client exoneration — clarified in code, final-batch fault-ordering nuance verified safe). Deterministic witnesses only; no native run claimed. |
| VPA-017 (slice) | recovered interrupted lane head `a7030c6d` (base `667650e9`) | fast-forward on `dev/ox-alpha` (`a7030c6d`) | Session-owned named play/stop outcome resolution through the optional MCBEAUD1 carrier as described in the P0 row; carrier absence is a labeled incomplete fallback while malformed/oversized/stale carriers fail startup closed. Implementer verification at the exact head: bedrock-client lib 743 passed including all new session-audio witnesses, assets 47, audio_assets 11, fmt/clippy(-D warnings)/architecture/diff-check green; one pre-existing environmental failure in `physics_assets::make_physics_assets_repairs_a_newer_corrupt_registry_once` (absent `sh.exe` makes ezwinports make fall back to cmd.exe writing `"repaired  \r\n"` instead of LF bytes) was reproduced bare at base `667650e9` and is unrelated to this commit — tracked for its own bounded fix. Fresh independent review APPROVE with four Minors dispositioned: (1) duplicated carrier identity constants between `asset_startup.rs` and `asset_startup/audio_carrier.rs` — consolidate on next touch of that family; (2) skip outcomes share the 256-entry outcome queue with plays/stops so a flood of unknown-name plays can evict resolvable outcomes — accepted bounded consequence, counters preserve evidence, revisit when a playback consumer lands; (3) tracking/Makefile help text updated by this integration commit; (4) `SessionAudio::clear()` is a dead forward-seam API — wire it as the session-reset owner or drop it when the consumer lands. No audible output claimed; no native gate closed. |
| physics-assets repair witness | `6323a988` (base `464eaf01`, test-only) | fast-forward on `dev/ox-alpha` (`6323a988`) | The environmental `physics_assets::make_physics_assets_repairs_a_newer_corrupt_registry_once` failure above is fixed by making the synthetic fixture recipe shell-semantics deterministic: the repair truncates via `cd . > <registry>` (silent zero-byte under both POSIX sh and cmd.exe) and pins sha256(""), keeping the real Go hashcheck gate exercised twice per cycle and the `[1, 1]` repair-once witness plus a new final-bytes assertion. Full bedrock-client suite now passes 0 failures including physics_assets 2/2. Fresh independent review APPROVE with three informational Minors (empty-byte name reads misleading until the comment is read; emptiness assertion and recipe are two encodings of one contract that must change together; pre-existing cmd.exe `%VAR%` redirect expansion exposure shared by sibling fixtures). No production behavior changed; CI matrix predicted deterministic per OS. |
| publication starvation (recovered unrecorded tranche) | `90eb6cbc` + `f3279875` (merge `f58db9a5`; landed by a prior session without record) | already on `dev/ox-alpha`; review recorded 2026-08-24 | Fresh independent review of the previously unreviewed range returned APPROVE: upload-pressure hysteresis latch (set >=512 items or >=64 MiB, clear <256 items and <32 MiB), drain-proven collapse pause, per-frame operation caps floored at 8 instead of 1, bounded 4-job starved mesh-dispatch floor, plus three cadence-bounded `ADAPTIVE_PUBLICATION` telemetry fields with no acceptance-script consumers; nearest-first ordering, cohort identities, 256-op zero-byte cap, burst ceilings, and transactional arena growth unchanged. Four Minors recorded; the top one (no discriminating witness for the starved-dispatch floor) is closed by `0d63f7d6` below, which adds poll-path exact-floor (6 pending -> exactly 4 dispatched) plus empty-pending zero-work controls with a floor-constant mutation check; reviewer correction noted: reverting the floor entirely was already caught by an existing exhausted-window test, the closed gap was value discrimination. Remaining Minors: zero-byte-cap recovery stays slow at low FPS (~120 healthy frames per +1, revisit with live evidence before tuning), drain detection ignores bytes, world.rs at 983/1000 lines must extract on next touch. Live <=2 s teleport/full-view remesh gate remains open and unaffected. |
| VPA-138 file-permission slice | `23242dfe` + `0eb4227a` (merge `bb0b03b8`; landed by a prior session without record) | already on `dev/ox-alpha`; review recorded 2026-08-24 | Fresh independent review APPROVE: cached-token files are owner-only at creation (temp 0600, protected DACL before any token bytes on Windows, explicit Chmod pre-write) and revalidated at load/open on both platforms (path stat plus opened-handle fstat/descriptor check closing the stat-to-open window; SameFile plus parent-chain revalidation bracketing every phase); foreign-owner/unverifiable descriptors fail closed with typed secret-free codes; quarantined `.invalid` siblings restamped private; pre-existing broad caches quarantine (never delete) and re-authenticate with code+path-only notices. Seven Minors recorded: ancestor-directory privacy outside the enforced contract, `-auth-events` lane swallows the quarantine notice (Config.Writer is io.Discard), quarantine path less sandboxed than save/load, ambient-%TEMP%-dependent Windows acceptance fixture, ACL-less filesystems fail fatal on persist and deny-ACE trustees count as allow-equivalent (both safe directions, undocumented), per-commit hunks not independently attributable in the review sandbox. Windows ACL hardening beyond token-cache files stays open under VPA-121/VPA-139; native/live multi-platform evidence remains open. |
| VPA-011 processed jump-flag slice + replay reconciliation | `98e231ad` + `aa5f3ef0` (landed by a prior session without record) + fix `7c9cd4c0` | already on `dev/ox-alpha` via `da9f7343`; fix fast-forwarded (`7c9cd4c0`); review recorded 2026-08-24 | Outbound `Jumping` now derives from a simulator-consumed jump arc folded per fixed tick (`app/src/movement/state.rs`), raw button carriers stay physical, sprint forward-input gating preserved, sneak routes a single provisional seam with bytes unchanged pending VPA-012 pose authority. First review NEEDS CHANGES on one Important finding: correction replay treated retained `jump_initiated` as immutable input fact, so grounded-flip corrections could phantom-assert or suppress the wire flag. Fix `7c9cd4c0` recomputes initiations through a bounded pure `ReplayJumpArcFold` mirroring the simulator's exact clear -> ground-gated consumption -> end-of-tick decrement ordering from the corrected anchor (read-only `PredictionHistory::input_at`, exported `JUMP_DELAY_TICKS`; no new storage); both holes carry discriminating red-first end-to-end witnesses plus a rule-pinning unit test. Fresh re-review APPROVE; remaining Minors: movement.rs 999/1000 and physics.rs 997/1000 must extract on next touch, O(k^2) input_at scan is fine at capacity 32, fold duplicates the simulator rule by construction (optional shared-helper follow-up). All flag lifecycles remain explicitly provisional pending version-matched native measurement. Remaining VPA-011 strata: swim/climb/flight/item-use flags, consumable-slowdown wiring (VPA-108), current physics registry (VPA-014), pose/mode authority (VPA-012), live/native witnesses. |
| organic-movement input driver | `be2a915d` (base `7c9cd4c0`, tooling-only) | fast-forward on `dev/ox-alpha` (`be2a915d`) | Committed bounded synthetic input driver `scripts/organic-movement-input-driver.ps1` (SendInput scan codes into the focused window, foreground-gated, hard 120 s cap, guaranteed key release in try/finally, `-DryRun` never loads native types) mapping every keyboard-reachable Phase3Aggregate CandidatePhysics witness: flat walk, diagonal walk, single tap jump, exact F5 perspective cycle (default 3 taps per this client's verified three-state cycle, overriding the runbook's four-tap guess with the in-repo camera-cycle test as authority), KeyboardMouse input-mode frames; prints an honest GamePad coverage gap every run because GamePad-frame validation remains an open blocker. Pester suite 17/17 green including byte-deterministic dry-run plans and down/up balance assertions; architecture checker green. No live injection exercised yet — the first real driven CandidatePhysics session remains its own witness. |
| driver window activation | `5573eb18` (tooling-only) | fast-forward on `dev/ox-alpha` (`5573eb18`) | The driver's foreground gate refused correctly during the first live driven attempt (the spawned client window never took OS foreground unattended), so `-Activate` now attempts bounded per-process activation (ShowWindow restore, SetForegroundWindow, AttachThreadInput unlock, harmless Alt unlock, WScript AppActivate) while the existing GetForegroundWindow membership check stays the sole injection authority and failure still refuses with today's exact semantics; default off keeps prior behavior byte-identical and DryRun untouched. Verified by a real notepad smoke reproducing the refusal-then-attach-thread-input recovery path plus honest-failure runs; Pester 22/22; candidate resolution extracted verbatim to `scripts/organic-movement-input-focus.ps1` under the PS cap. The same session proved the first real driven injection (73 events over 100 s) into the live client window. |
| launcher core-args passthrough + cap repair | `3265a45e` + `cfabfed5` (tooling-only) | fast-forward on `dev/ox-alpha`; review recorded 2026-08-25 | Phase3Launcher gains optional `-CoreExtraArgs` (string[], conservative token allowlist, pre-build rejection, verbatim append after standard core args, additive manifest/metadata surfacing, four validators extended in lockstep) so acceptance lanes can exercise opt-in core capabilities such as upstream client cache without further harness edits. Fresh independent review APPROVE with Minors recorded (last-wins override semantics underdocumented, hyphen-leading value tokens end Go flag parsing early, suite-count bookkeeping discrepancy between lanes). Its test growth breached the 800-line PowerShell cap; mechanical verbatim split into `Phase3.LaunchCases.ps1`/`Phase3.TestHelpers.ps1` restored compliance with identical contract totals, and the coordinator's integration run settles the count question directly: Phase3.Tests.ps1 101/101, FastTransferWitness 14/14, architecture exit 0 at `cfabfed5`. |
| NSL echo provisional scaling | `1ef3e2a8` | fast-forward on `dev/ox-alpha` (`1ef3e2a8`); review recorded 2026-08-25 | Outbound NetworkStackLatency echoes now carry `creation_time.saturating_mul(1_000_000)` through one shared helper at both ingress sites, explicitly labeled PROVISIONAL pending authoritative retail-client measurement. Basis: four authenticated sessions on one authorized target were deterministically torn down by its anti-cheat ("Error: Network timed out") after exactly its spawn-region stream regardless of organic movement; fresh independent source verification of that anti-cheat project at its pinned version confirmed all three cited mechanisms line-by-line (per-tick NeedsResponse probes with raw random timestamps, echoed-id normalization dividing twice for non-PlayStation devices against stored batches so exact echoes never resolve, a PAI-gated responsiveness counter whose expiry disconnects, default 60 s budget matching observed lifetimes); its license requires descriptive attribution rather than code copying, which is what this record does. TDD stage-8 fixture moved 777 -> 777_000_000 with old-vs-new documented; saturation witness added; wire encoding is fixed-width u64 so magnitudes are lossless. Review APPROVE with Minors (red-first attested not independently reproducible; prior exact-echo live evidence no longer describes current bytes until BDS/LBSG re-runs land — the eventual gate updates must say so). Live A/B on the affected target and BDS/LBSG tolerance re-verification remain open and are the tranche's own gates. |
| Venity sustained-session live A/B | live run at `cfabfed5` (no code change; run `phase3-4194caa0`, local-only artifacts) | evidence recorded 2026-08-25 | First full-duration authenticated driven CandidatePhysics session on the affected target after the NSL scaling: 603.2 s wall (the complete 480 s acceptance window plus join/shutdown), 1,356 transmitted physics packets from source=Physics, `outbox_reconciliation=Drained` at depth 0, zero decode errors, zero outbound drops, zero authority faults, organic driver injected across four 100 s rounds via `-Activate`; core log ends in normal shutdown telemetry with NO timeout text, versus the prior four-session ceiling of 56-148 s ending in the anti-cheat teardown text. This empirically confirms the provisional scaling against its motivating target; it does NOT yet re-verify BDS/LBSG tolerance of scaled echoes, does not close the settle/overflow provisional constants' native measurement gates, and the strict aggregate verdict stays open on three named defects: (1) `world_ready=false` because the required cohort is populated only by request-mode admissions while this server streams fully inline columns (structural client defect, own tranche); (2) exactly one `non_monotonic_frame` violation marker in the debug run; (3) the known spawn-geometry embedment keeps re-engaging the provisional settle gate (dozens of engage/lift cycles, several 200-tick fail-open lifts), which remains the open movement root cure. GamePad-frame validation remains unavailable (no physical gamepad), so aggregate validity is blocked independently of the above. |
| inline-column cohort admission | `317a3de1` | merge `377015ec` on `dev/ox-alpha`; review recorded 2026-08-25 | Closes the structural world_ready defect found by the Venity diagnosis: one production insertion records admitted inline LevelChunks into `required_columns` through the exact request-mode gate, extending VPA-005/006 decode/admission ordering to inline delivery with shared dedup/eviction/publisher/epoch machinery. Four red-first witnesses (inline-only exactness incl. empty-hash-constant disappearance, failed-decode exclusion, replacement invalidate/restore via shared eviction, mixed-mode single counting). Fresh independent review APPROVE with Minors recorded (inactive-inline exclusion unwitnessed but defensively guarded inside the shared helper; reverse-order cross-path duplicate unwitnessed; mixed-mode stale-request priority classifies Visible instead of Prefetch — bandwidth-only observation). Coordinator fresh post-merge client-world suite green at the merge. Deterministic closure only: live confirmation on an authorized inline-only target (required_cohort_count>0, stable non-empty hash, world_ready emission) remains open as this tranche's own gate. |
| VPA-123 hotbar fan-out | `d0bb7ccc` | fast-forward on `dev/ox-alpha` (`d0bb7ccc`) | Presentation derives every hotbar cell — selected or not — from one ledger-snapshot authority revision via `displayed_stack` plus the travelling presented-slot overlay: accepted sparse responses refresh all consumers atomically, rejection restores exact pre-gesture cells, session reset clears both stores. Two red-first witnesses (nonselected refresh, mid-gesture presentation). Fresh independent review APPROVE with five Minors recorded: authority rule duplicated between `publish.rs` and the witness helper (pin together on next touch), `GameplayHudState` mirror ingestion still unguarded against cursor-window events (closed later the same session by VPA-122's projection), no dedicated mid-flight durability-overlay pairing witness, one guard test not red-first by design, `hud_layout.rs` at 996/1000. Native/live hotbar acceptance remains open under Phase 5.5. |
| non-monotonic-frame violation enrichment | `b6d689eb` | fast-forward on `dev/ox-alpha` (`b6d689eb`) | PHASE3_VIOLATION schema bumps to v2; reason `non_monotonic_frame` now carries a bounded ten-key previous/current identity payload (session/tick/dimension/fifo/pose) so future one-off live occurrences are self-diagnosing; every other reason stays exactly `{schema, reason}`; detection, latch, and emitter lifetime byte-identical. Root finding from the accompanying investigation: the most likely trigger is the settle-suppression tick gap (suppressed ticks burn tick numbers without evidence frames), so known settle cycles will still raise it — the lifted-settle exemption question is deliberately left open as its own decision. Fresh review APPROVE with four Minors (schema literal appears twice inside `take_violation_marker`, serde-derived wire keys pinned by a hand-written oracle test, red-first characterization precision, pin-case failure-mode ambiguity). |
| VPA-122 container-address projection | `9782ff02` + fix `8155bf1a` (recovered interrupted lane completed) | merge `57b59b06` on `dev/ox-alpha` | One canonical container-address projection routes every ledger admission, lookup, accepted-response correction, and HUD ingestion through typed cells (hotbar 0-8, main 9-35, armor, offhand, cursor, generic storage): Content, Slot, and response paths converge on one cell per physical cell; cursor slot-type events can no longer reach hotbar cells through any admission path; partial window-0 content preserves unseen mirror cells; unknown containers are typed counted lenient skips while malformed wire stays fatal; gesture wire bytes/ordering/rollback/recovery identical. First fresh review NEEDS CHANGES caught a silent narrowing of prior admissions (name-29 InventoryContainer on window 0, which the pinned fixture corpus itself encodes, and bare-window storage slots); the fix round restored both through the projection with fixture-shape witnesses; re-review APPROVE over the cumulative range with Minors N1-N3 recorded: storage Content with no open window stays silent while equivalent Slot events count (align on next touch), bare window-119 offhand-vs-storage precedence is a documented behavioral delta, fixer line-count report drifted from actuals. Armor/offhand cells resolve canonically but are retained nowhere (counted skips) — retention stays open; native/live inventory acceptance open under Phase 5.5. |
| spawn-anchor integrity probe (VPA-109 slice) | `b3b14bc5` | fast-forward on `dev/ox-alpha` (`b3b14bc5`) | Every hard anchor (StartGame, surface-spawn resolve, correction snaps) arms a bounded depenetration probe — PROVISIONAL 8 iterations / 1.5 net-displacement bounds — through a pure `sim::depenetrate_player` helper before the anchor's first simulated tick; cleared anchors adjust position pre-tick with no launch impulse. Unresolvable embedment freezes simulation advancement in `SpawnSettleGate::EmbeddedHold`, withholding transmission with `MOVEMENT_TX_GATE` reason `embedded_anchor`; exits are server-snap reanchors or the unchanged 200-tick fail-open cap, degrading to today's streaming after three failed probes per re-anchor epoch. Surface-spawn feet now rest exactly on the surface (`block_y + 1 + PLAYER_NETWORK_OFFSET`). Review APPROVE with six Minors: hold retains sub-tick remainder unlike the zeroing transient path (comment drift), settle-admission-during-hold invariant documented not enforced, epoch-degrade transition emits no distinct marker yet, runtime_system glue has no direct witness, deterministic `mutation_coordinate` Y shifts −1 so historical comparisons must expect it, "epoch" names two unrelated concepts in the subsystem. Live LBSG capture below shows the probe refusing to invent motion before the caps fail open. |
| HandledTeleport acknowledgement slice | `bc838c83` + fix `f9cbe242` | fast-forward on `dev/ox-alpha` (`f9cbe242`) | Opt-in (`RUST_MCBE_TELEPORT_ACK=1`) single-shot HandledTeleport flag assertion on the first transmitted PAI after TeleportSnap corrections, `teleported` MovePlayers, and committed Respawns — armed inside authorized world-stream reconciliation, applied at flush time so restore/retry resends and settle-suppression spans carry it, consumed strictly after send success; PROVISIONAL 40-admitted-tick expiry emits a `TELEPORT_ACK` marker; default-off reproduces today's bytes exactly (equality witnessed). First review NEEDS CHANGES (Critical: the Respawn arm was never wired although docs claimed it; Important: zero call-site-bound witnesses); the fix round wired production reconciliation and added five real-system integration witnesses that fail when individual call sites are removed; re-review APPROVE with Minors recorded (unwitnessed unauthorized-MovePlayer negative case, `movement.rs` 998/1000, read-only-reviewer execution caveat). No vanilla claim: carrier choice, persistence, and qualification breadth stay provisional pending version-matched native measurement; the PlayerAction nested-action route remains out of scope. |
| LBSG anti-cheat kick capture | live run at `b3b14bc5` (`phase3-b5a1f5faee194d8684688007dbccb625`, local-only artifacts) | evidence recorded 2026-08-25 | First authenticated current-binary CandidatePhysics session on `play.lbsg.net:19132` after fixing a launcher defect this session exposed: a machine-wide `CARGO_TARGET_DIR` redirected the harness build to the shared target directory while the launcher executed a days-old project-local exe (observed as a stale MCBEAS06 reader rejecting the rebuilt MCBEAS07 carrier); the coordinator orchestrator now clears the variable for the launcher child. Session: join succeeded, 17/17 required cohort streamed inline, 504 resident meshes presented, 44 physics packets transmitted, zero transport drops — then the core relay captured the exact upstream rejection `"We've detected movement cheats"` at 49.85 s. PAI trace: every packet claims zero input with pure-gravity `pos_delta` while position X advances exactly +1.00 block/tick under persistent HorizontalCollision+VerticalCollision flags — the predicted depenetration limit cycle; the gate trail shows three spawn_settle engage/reanchor lifts followed by two `embedded_anchor` holds each failing open at exactly 200 ticks before the doomed stream resumed, plus an initial 724.7-block snap correction. This empirically confirms the embedment mechanism live and proves the provisional caps merely delay the kick while client collision truth disagrees with the lobby; the next cure tranche is collision-truth adjudication instrumentation (name the sealing colliders during failed probes). Scaled NSL echoes were answered throughout without a latency teardown — weak positive signal only; BDS/LBSG echo tolerance re-verification stays open. Run metrics show `decode_error_count` 262 against terminal decode_error_count 0; subsequently reconciled: 262 = 232 `inactive_inline_chunks` + 30 `inactive_block_updates` — benign residency-policy skips of provably well-formed packets (each skipped inline chunk decoded successfully before rejection) with zero genuine wire decode errors at either layer, and the spawn region was demonstrably cohort-complete (25/25 then 17/17, player column fully resident, queues drained) when the anchor probe ran, so the embedment conclusion stands on complete world data. Residual open question: collider SHAPE provenance (pinned v1001 PREG/BREG versus current 2168 content, VPA-001/VPA-014) — the 2.32M-quadrant `birch_stairs` diagnostic attribution is circumstantial evidence of identity friction that could mis-shape colliders without any decode error; the landed opt-in `RUST_MCBE_ANCHOR_PROBE=1` collider-naming instrumentation (see landed tranches) is the smallest decisive addition for the next authorized capture. |
| collider-naming anchor-probe evidence | `1b709785` + `bdc6a8bf` (provenance threading) | fast-forward/merge `317781ba` on `dev/ox-alpha` | Opt-in `RUST_MCBE_ANCHOR_PROBE=1` single-line marker names up to eight sealing colliders ({min,max} quantized to 1/64, containment block when single-cell, merged flag otherwise) with total count and truncation flag under a 2048-byte strict-prefix cap, emitted once per failed probe plus once per epoch degrade; provenance threading adds exact per-collider wire runtime ids through a new provided `CollisionWorld::collision_boxes_with_provenance` surface projected from one shared private emitter with a dual-surface parity pin. Zero transmission/gating behavior change witnessed by enabled-vs-disabled decision-sequence equality across all five probe transitions. Fresh reviews APPROVE on both tranches with Minors recorded (feet f32 cast hardening, sub-epsilon intersects label, conservative cut margin, dead float arm, `movement.rs` 999/1000 pressure, per-layer provenance witness, malformed-combination witness, halo-assertion coupling). Consumed by the live capture below. |
| LBSG collider-geometry capture | live run at `60e0121c` (`phase3-ae1093ec3b64443ab7c52a73234b7233`, local-only artifacts) | evidence recorded 2026-08-25 | Second authenticated current-binary CandidatePhysics session with the marker enabled (pre-provenance build, so geometry only): same remote-close profile (~75 s, zero decode errors, stationary client — driver rounds never fired because the launcher exited before injection), and the new instrumentation captured the embedment pocket directly: feet [-511.0, 42.1, -511.0], 25 sealing colliders (truncated to 8) whose y-extents are half-height stair profiles ([42.0,42.5] / [43.0,43.5]) at cells x=-512..-511, y=41..44, z=-512..-510, plus a merged [-512,-511.25]x[41,42.5] slab; epoch degrade emitted once after three failures. The geometry is exactly aliased birch_stairs collision standing where the server streams air (wire id 13629 = minecraft:air in v2168 space), closing the loop between VPA-001 aliasing and the VPA-109 kick mechanism on live wire data. |
| carrier-cutover T0: v2168 physics registry | `1df2fa3c` | fast-forward (`1df2fa3c`) | tools/registrygen gains a mutually-exclusive mode generating deterministic `block-physics-v2168.bin` (+sha256, 799,460 bytes, digest e7631b3c...) from the checked-in v2168 BREG: reserved detected BY NAME (969 neutralized passable), identical fact pipeline (CollisionSeed boxes verbatim, PMMP friction by name Q1E8, liquid heights, 19 reviewed override families cross-checked), fail-closed sorted listing of non-reserved names missing PMMP rows, parameterized encoder stamping protocol 2168/count 17499 while the legacy path stays byte-reproducible (full v1001 four-source rebuild reproduced checked-in sha 2590d486 byte-exactly). Red-first Go tests incl. spot facts at new ids and legacy-seed transplant equality for all 16,530 states. Review APPROVE with Minors N1-N5 (CI runs only ./cmd/... so root-package witnesses are developer-machine-only until a bounded CI flip; no regeneration-equality CI witness yet; checksum sibling format divergence; ladder histogram {empty:2,one-box:4} is inherited v1001 semantics surfaced for movement owners). Artifact inert until consumption flips. |
| carrier-cutover T1: parameterized PREG reader | `09b2ba41` | merge `c06b943d` | `read_physics_registry_for_protocol` accepts exactly {1001,2168} with typed pre-structural rejection otherwise; legacy alias behavior-provably unchanged (sole internal change routes the element-wise BREG re-decode through the parameterized reader because v2168-bound PREGs carry v2168-stamped BREGs); `PhysicsRegistry.protocol()` replaces the app identity literal with production bytes unchanged; cross-version bindings rejected both directions; committed artifact sidecar digest pinned as drift guard. Review APPROVE with Minors (report said five new witnesses, file adds six; MAGIC constant name now gates both protocols; inline protocol-set duplication mirrors sibling style; thin 2168 structural-negative coverage and sha-equality-based alias equivalence noted as hardening). A pre-existing fmt violation on three files from the earlier merged tranche was fixed mechanically in `9f69101e`. |
| carrier-cutover T2a: versioned registry triples + canonical air | `bdaa0fee` + `cdefece7` (docs `84a9b33c`) | merge `067541e1` on `dev/ox-alpha` | assetc consumes header-versioned BREG/LREG triples with typed, file-attributed fail-closed mismatches (`registry_version.rs`), derives canonical air from validated registry content for both sequential and hashed sessions (v1001 byte-compat pinned to 13094/0xdbf44120; arbitrary-identity v2168 end-to-end discriminator), and confines gated real-pack ratchet tolerance to one explicitly asserted appended air identity through one shared fixture helper without weakening any other ratchet assertion. Fresh independent review APPROVE over the full `c06b943d..067541e1` range; reviewer shell was unavailable, so the coordinator executed the required verification green: full `-p asset-compiler --locked` suite (all binaries passed, 10 environment-gated ignored) and architecture gate exit 0; policy delta confirmed confined to pre-range T0/T1 baselines with no cap relaxation. Minors recorded: pin the real-v2168 canonical-air identity (unique `minecraft:air`, sequential 13629) in `block_registry_v2168.rs`; count/log the silent bootstrap-air fallback in `construction.rs`; `InvalidCompiledAssets` attribution cosmetic; optional air-less-registry CLI test. Production defaults remain v1001; VPA-001 stays open. |
| carrier-cutover T2b: fallback rekey continuity witness | `6a6da17b` + `8510808c` + `c4377f78` | merge `48718159` + `b24e91a1` on `dev/ox-alpha` | registrygen gains a mutually exclusive fallback-rekey mode joining every CVFB1001 entry through checked-in v1001→v2168 BREGs by exact canonical name+state key with dual fingerprint checks, collision/duplicate/unmatched fail-closed listings, reserved-exclusion accounting, sorted output + sha sidecar + provenance manifest bound to both registries and the input inventory. Empirical result, now executed-witnessed: network hashes are content-derived so all 2,031 fallback identities carry identical hashes across protocols — output byte-identical to input today (`31cfbf7b…`, drift-guard test pins sidecar AND regeneration; EOL-tolerant after an autocrlf checkout proved the original exact-string comparison spuriously failing). Fresh independent review APPROVE on `6a6da17b` (coordinator executed Go/vet/hash/range debt) and on `8510808c`; four residual Minors closed by `c4377f78` (EOL-tolerant comparison + LF `.gitattributes` for data sidecars, honest input/excluded/output stats fields, stdout/stderr-separated CLI witnesses, precise failure-class doc comments). Recorded Minors remaining: root-package registrygen tests stay developer-machine-only until a bounded CI flip (inherited T0 N1); out-of-repo consumers of the old `entries_joined` stdout key would need the renamed fields (none exist in-repo). |
| carrier-cutover T3: protocol-aware fallback selection | `fb7f1b9b` | merge `48718159` on `dev/ox-alpha` | The compiler's provisional vanilla-fallback inventory is selected by the active registry's header-derived protocol from stamped embedded tables (v1001 default byte-identical; any other protocol fails closed naming active protocol and both stamps), threaded through every fallback consumer via one resolution point with no setter access; pinned coverage gates prove both tables' full 2,031-entry inventories resolve against their own checked-in BREGs; closes the T2a Minor by pinning the unique real-v2168 canonical air at sequential 13,629 with state/flags/role/family exactness plus cross-protocol hash continuity. Red-mutation attestations verified by reading: stamp-ignore collapses the dual-static assertion; global-table hardcode breaks the synthetic-hash consumption witness. Fresh independent review APPROVE; recorded Minors: library-level records/protocol coherence enforced only at the assetc boundary (documented misuse surface), corrupt-embedded-table panic inherited verbatim from prior code, swap-class mutations undetectable until table content diverges (bounded by the Go join; becomes self-disclosing on first divergence), `compiler.rs` 882/1000 and `assetc.rs` 918/1000 cap pressure — extract on next touch. Production defaults remain v1001; VPA-001 stays open. |
| T2a air-contract fixture repair | `1f6b0b5d` | merge `0c2eb54e` on `dev/ox-alpha` | First milestone workspace run exposed that T2a's fail-closed canonical-air derivation broke crates/render's synthetic FlowerBed fixture (3-record wildflowers registry without air); sweep found exactly ONE offender across all compile-path fixtures (meshing fixtures already embed genuine air; asset-compiler suites route through `with_canonical_air`). Fix clones the genuine committed v1001 air record into the fixture without touching any production gate. Test-only non-material correction: red-first observed by coordinator (52/3 fail), focused rerun 55/55, full `--workspace --no-fail-fast` green (167 binaries), fmt/clippy(render)/architecture green — recorded disposition in lieu of a fresh review cycle per workflow rule 4. Follow-ups noted: shared air-injection helper if a third copy of the pattern appears; legacy `AIR = 12_530` classifier constant in plugin.rs is cosmetic-only. |

## P0 â€” session, state, and player-visible blockers

| ID | Classification | Current mismatch | Primary evidence | Smallest independent closure |
| --- | --- | --- | --- | --- |
| VPA-001 | Proven mismatch | Protocol 2168 runs against protocol-1001 world, light, physics, visual, and coverage carriers. Live-movement impact established 2026-08-25: sequential-id aliasing is bidirectional and movement-blocking — decoding the pinned v2168 registry (`read_registry_for_protocol(.., 2168)` over `crates/assets/data/block-registry-v2168.bin`) proves index 13629 = `minecraft:air` where v1001 names `minecraft:birch_stairs`, and v1001's air 13094 = acacia_stairs in v2168 space; current-content servers therefore present solid collision on true open air (and vice versa) with zero decode errors, which is the direct mechanism behind the live LBSG embedment/kick capture (see landed tranches). | `app/src/asset_startup.rs`, `app/src/app.rs`, `tools/visualcoverage`, `plan.md` | Build and atomically bind one version-coherent 2168 carrier set; reject mixed sets before gameplay; the movement-blocking aliasing makes this row the prerequisite cure for the VPA-109 embedment family rather than a visuals-only concern. |
| VPA-002 | Deliberate deviation | Server-required packs are downgraded on the private hop; selected packs are retained but never extracted, merged, decrypted, applied, or presented. | `core/proxy/resource_pack_admission.go`, `crates/resource-pack`, `app/src/runtime/network` | Truthfully reject required packs until bounded stack application and revision-safe renderer/UI swap exist. |
| VPA-003 | Implementation landed | Post-login `Transfer` now decodes into a bounded vendor-neutral one-shot event (well-formedness floor: trim, ≤255-byte host, no control/interior whitespace, nonzero port; truncated wire fatal, nonsense-but-well-formed counted skips), the network pump classifies it as a FIFO-preserving terminal `Transferred` with a durable `outcome=transferred` marker outranking later transport errors, the Failed-family teardown runs without latching `RemoteClosed`, and launcher sessions auto-rejoin the validated target through a fresh core with an 8-hop bounded chain and explicit cannot-follow states (`--address` runs end with a named reason). Zero Go changes. Recorded Minors: send-error and capacity-path pump legs unwitnessed, capacity-branch permit held during the pending-event resend, transfer consumption waits on next ingress completion, superseded error text dropped from the marker, semantic-skip counter has no production log line. Live rejoin evidence and a FastTransferWitness lane rerun remain open; hostile servers can chain up to 8 fresh cores per user join (bounded, vanilla-correct). | `crates/protocol/src/transfer.rs`, `crates/protocol/src/login.rs`, `app/src/runtime/network/session/pump_runtime.rs`, `app/src/menu/input.rs` | Preserve this boundary; capture live transfer-rejoin evidence and rerun the FastTransferWitness lane under the new semantics. |
| VPA-004 | Implementation landed | Verified blob entries now survive app network-worker replacement through one process-owned cache; unrelated semantic skips leave pending cached terrain intact. | `app/src/app.rs`, `app/src/menu/input.rs`, `crates/protocol/src/login.rs` | Preserve this boundary while the separate cache ordering, miss timing, and persistent-storage gates remain open. |
| VPA-005 | Implementation landed | Request-mode failures drain their bounded transport state but no longer mark the collision-incomplete column loaded or complete its required cohort. | `crates/client-world/src/stream/retries.rs`, `cohort.rs`, `sequencing.rs` | Preserve this boundary while exact retention, timeout, and retry policies remain open. |
| VPA-006 | Implementation landed | A request-mode `LevelChunk` now joins the required cohort only after successful decode, active-column admission, and supported-dimension admission. | `crates/client-world/src/stream/sequencing.rs` | Preserve this ordering while exact recovery behavior for rejected announcements remains open. |
| VPA-007 | Proven mismatch | The compiler promotes atlas aliases to item identifiers while live stacks use canonical registry identifiers, blanking mismatched sprite names; block-item routes are also omitted from the flat icon carrier and have no 3-D item renderer. | `crates/asset-compiler/src/entity/item.rs`, `icon.rs`, `app/src/ui_runtime/presentation.rs` | Compile an authoritative canonical-identifier-to-sprite crosswalk, then add a separate renderer-owned block-item geometry path. Never infer either route from filename similarity. |
| VPA-008 | Implementation landed | Hotbar selection sends the checked current ledger prediction, preserves unknown/empty/present, retries a full channel, and cancels stale pending sends on a valid server-forced selection. | `crates/protocol/src/item.rs`, `app/src/hotbar.rs`, `app/src/ui_runtime/inventory_ledger.rs` | Preserve this boundary while selected-store unification and native/live equipment acceptance remain open. |
| VPA-009 | Implementation landed | One tri-state selected snapshot now drives packet construction and selected presentation; prediction, rejection restoration, correction, known-empty state, and bounded matching-equipment bootstrap share the same authority. | `app/src/hotbar.rs`, `app/src/ui_runtime/gameplay_authority.rs`, `presentation/publish.rs` | Preserve this boundary while nonselected response fan-out, canonical item visuals, offhand/viewmodel rendering, and native equipment acceptance remain open. |
| VPA-010 | Implementation landed (slice) | Device-class raw, analogue, and processed `PlayerAuthInput` vectors are now preserved through packet construction with fail-closed non-finite handling; what vanilla Bedrock places in each field per device class remains unmeasured, so no per-device convention is claimed. | `crates/input/src/router.rs`, `crates/input/src/axes.rs`, `app/src/movement.rs` | Capture version-matched native per-device vector conventions before claiming parity; add a mixed-device carrier witness. |
| VPA-011 | Proven mismatch | Swim, climb, flight, item-use, and related processed states remain absent; sneak bytes still mirror held input pending the VPA-012 pose/mode authority. Landed slices: processed sprint gates active sprint on forward movement input, and outbound `Jumping` now derives from a simulator-consumed jump arc (with correction-replay recomputation through `ReplayJumpArcFold`) instead of raw held input — see the landed-tranches rows for review evidence. All flag lifecycles remain explicitly provisional pending version-matched native measurement. | `app/src/movement/encoding.rs`, `app/src/movement/physics.rs`, `app/src/movement/state.rs` | Add shared pose/mode authority (VPA-012), then exact per-mode witnesses for each remaining flag before changing encoder bytes; wire consumable slowdown (VPA-108) and current physics registry facts (VPA-014). |
| VPA-012 | Proven mismatch | Sneak/swim/crawl/flight have no shared pose/mode authority; body box, eye height, play mode, gravity, and collision stay standing/normal. | `crates/sim/src/aabb.rs`, `app/src/movement/physics.rs`, `crates/protocol/src/movement.rs` | Add one pose/mode state shared by simulation, camera, rendering, and packet production. |
| VPA-013 | Implementation landed | Active BedSim v0.1.5 provenance drives the same bounded non-swimming water-gravity and raised-ledge probe; its four-record output is byte-identical to v0.1.4. Open-water held ascent bobs; clear ledges boost while blocked, submerged, partial-liquid, unavailable, or identity-incompatible probes do not. Swimming pose, immersion depth, pitch steering, currents, flow, and camera state remain absent from the Rust port. | `crates/sim/src/simulator.rs`, `crates/sim/src/simulator/environment.rs`, `crates/sim/fixtures/bedsim-v0.1.5-liquid.jsonl`, `tools/bedsimtrace-v0.1.5` | Port and independently test v0.1.5 swimming/flow/pose/knockback/item-use/provider strata in Rust before treating the newer oracle as runtime parity. |
| VPA-014 | Proven mismatch | Production movement consumes the old physics registry, so collision, fluid, friction, climb, and surface facts can disagree with decoded states. | `app/src/app.rs`, `app/src/movement/physics.rs` | Version-tag and fail closed on physics/carrier mismatch; generate current facts before enabling movement. |
| VPA-015 | Proven mismatch | Dropped-item packets, several actor-motion/event families, remote armor, and retained hand equipment do not reach world rendering. Non-player actors are no-draw. Local-player `SetActorMotion` impulses now normalize into a bounded world event, commit FIFO through the world stream, and enter prediction as tick-keyed velocity overlays that survive correction replay; remote-actor motion consumption and rendering remain open. | `crates/protocol/src/login.rs`, `client-world/actor_store`, `app/src/presentation/actors.rs` | Add one packet/lifetime/render family at a time with two-client and native gallery gates. |
| VPA-016 | Proven mismatch | First-person held items are UI-raster approximations with identity transforms and dormant bob/action state, not the renderer-owned Bedrock viewmodel. | `app/src/ui_runtime/presentation/item_viewmodel.rs`, `viewmodel_bob.rs`, `crates/asset-compiler/src/entity/item.rs` | Route the selected item through renderer-owned sprite/block geometry, display transforms, and action timelines. |
| VPA-017 | Implementation landed (slice) | Named PlaySound/StopSound traffic now drains into a bounded session-owned `SessionAudio` outcome store (256 ordered outcomes under one `(session_generation, dimension)` identity with clear-on-mismatch, reset, oldest-drop accounting) whose plays resolve through the optional compiled MCBEAUD1 catalog into finite-checked playback records carrying raw wire position without spatial math; stops resolve catalog-free; unknown names/empty alternatives/unavailable catalog/non-finite combinations are typed counted skips; LevelSoundEvents stay transport-only counts because numeric/string level routing is unverified; weighted alternative selection is an FNV-1a function of `(sequence, name bytes)` explicitly labeled provisional pending native observation. The optional carrier joins the assets umbrella; absence selects the empty-catalog fallback with notice while present-but-malformed/oversized/stale carriers fail startup closed naming path and rebuild command. Still absent: any audible output, audio backend/mixer/device dependency, listener math, positional state, looping playback, stop handles, category settings, pack routing, and a session reset owner for the queue. | `app/src/session_audio.rs`, `app/src/session_audio_tests.rs`, `app/src/asset_startup/audio_carrier.rs`, `crates/assets/src/audio.rs`, `crates/protocol/src/audio.rs`, `Makefile` | Add one independently reviewable family at a time: audible play/stop consumer behind an audio backend decision, then positional/listener math, pack routing, category settings, and session-reset wiring. |
| VPA-018 | Implementation landed | The world carrier now embeds a four-slot source identity (canonical `vanilla-source.json` SHA-256 plus block/light/biome registry input hashes) in a version-bumped MCBEAS07 header, and startup fails closed on stale, tampered, absent, or mismatched world/atmosphere carrier identity with typed errors naming the exact path and rebuild command; the diagnostic no-world-blob path still selects diagnostic textures but a present-but-stale atmosphere carrier is now fatal there too (recorded behavior change). Old local carriers fail decode until rebuilt. Entity/font/lang/icon carriers already validated provenance before this tranche. | `crates/assets/src/provenance.rs`, `crates/assets/src/runtime/decode.rs`, `app/src/asset_startup/world_provenance.rs`, `app/src/asset_startup.rs` | Preserve this boundary; the remaining protocol-version coherence gap across all carriers stays under VPA-001. |

## P1 â€” correctness and lifecycle gaps

| ID | Classification | Current mismatch | Primary evidence | Smallest independent closure |
| --- | --- | --- | --- | --- |
| VPA-101 | Open contract | Cache publication/status/recovery ordering is per-column and permits unrelated work to overtake unresolved cached terrain; exact retail scope is unproven. | `crates/protocol/src/blob_cache/resolver.rs`, `crates/protocol/tests/blob_cache` | Capture concurrent-column misses plus actor/movement/radius traffic; then encode the observed dependency scope. |
| VPA-102 | Open contract | Empty cache-miss responses retain transactions indefinitely; cross-transaction missing-hash suppression is an implementation policy. | `crates/protocol/src/blob_cache/resolver.rs`, `crates/protocol/tests/blob_cache` | Establish miss timeout/retry/duplicate behavior with an exact-version transcript. |
| VPA-103 | Implementation landed | Malformed inner world wire is distinguished from semantic oddity, complete packets are preclassified before stale/scope/mutation gates, later truncation wins over deferred semantic policy where bounded proof is possible, and the FIFO fatal latch clears or drains every downstream work family without resurrection. | `crates/protocol/src/login.rs`, `crates/protocol/src/inventory.rs`, `crates/world/src/block_entity.rs`, `crates/client-world/src/stream` | Preserve this malformed-wire/semantic boundary as new packet families are admitted. |
| VPA-104 | Open contract | Radius/publisher values are clamped to 16; SubChunk requests use a one-column/128-entry builder and locally invented two-attempt/two-second recovery policy, while native batching and timing are broader or unmeasured. | `crates/client-world/src/stream`, `crates/protocol/src/world/requests.rs` | Measure radius above 16, multi-column batching, result-class retries, deadlines, publisher movement, and edge retention before changing safety ceilings. |
| VPA-105 | Proven mismatch | Publisher updates change admission/cohort state but retention follows the player; server-built chunk metadata is discarded. Partial-column omission, saved-chunk precedence, and exact publisher/player interaction remain unmeasured. | `crates/protocol/src/world.rs`, `crates/client-world/src/stream/residency.rs`, `crates/world/src/chunk_grid.rs` | Retain bounded metadata and capture far publisher moves, exact edge columns, stale request cancellation, partial replacements, saved positions, and negative/unaligned centers before selecting retention semantics. |
| VPA-106 | Implementation landed | Non-finite remote player poses and unknown equipment containers now produce skippable semantic errors, preserving the prior usable actor/equipment state and session. | `crates/protocol/src/world.rs`, `crates/protocol/src/item.rs`, `crates/protocol/src/login.rs` | Preserve this boundary while wider actor packet, movement, lifetime, and rendering gaps remain open. |
| VPA-107 | Implementation landed | Packet-specific correction semantics: exact-agreement Confirmed mutates nothing, retained-tick Replay keeps the proven velocity-retention contract, and beyond-16-block displacement snaps like the teleport anchor; server yaw/pitch reach the view pose for every admitted shape under a provisional policy pending live observation. The vendored 2168 wire has no mode field, so shapes derive from field combinations; vehicle-subject records are retained at protocol and skipped downstream until riding rewind exists. | `crates/protocol/src/world/events.rs`, `app/src/movement/correction_shape.rs`, `app/src/runtime/world/control_apply.rs` | Preserve this boundary; live rotation/correction behavior observation remains open under VPA-109's live-gate work. |
| VPA-108 | Proven mismatch | Consumable slowdown exists in `sim` but production always supplies false. Active sprint no longer tracks the raw held request: processed sprint requires forward movement input, so prediction and wire flags stay consistent; consumable wiring remains open. | `crates/sim/src/simulator.rs`, `app/src/movement/physics.rs` | Connect authoritative use lifecycle and remaining processed movement state to simulation and input flags. |
| VPA-109 | Implementation landed (slice) | Initial prediction tick/ground state, packet bursts, third-person camera orientation, and input/DPI sensitivity need exact measurement. Stall tick dropping no longer revokes movement authority: a render-frame catch-up overflow keeps the retained samples contiguous and monotonic, so the outbound 20 Hz sequence stays valid and the server can still reconcile it; a live Venity candidate session previously died here when join-time streaming stalls dropped seven of fifteen due ticks. The overflow is now bounded evidence logging rather than a permanent `FreeCamera` fault, labeled provisional pending native stall-behavior measurement. Server disconnect reasons are now observable end to end (see the landed observability tranche), so future kick reruns attribute their cause. An opt-in `RUST_MCBE_MOVEMENT_TRACE=1` stdout trace (`rust-mcbe-pai-trace-v1`, one JSON line per PlayerAuthInput actually handed to the transport) now records the exact outbound movement stream for live diagnosis; it closes no measurement gate by itself. A bounded, explicitly provisional post-spawn transmission-settle gate now withholds the outbound PlayerAuthInput transport hand-off after each spawn anchor (StartGame reset, surface-spawn resolve, correction snap — never a correction replay) until 20 consecutive admitted samples report grounded movement without horizontal collision, failing open after 200 suppressed admitted ticks so an odd spawn cannot silence the stream into an idle timeout; suppressed ticks stay simulated and contiguously numbered but are never replayed, transitions emit the single-line `MOVEMENT_TX_GATE` stdout marker, and both constants are labeled provisional pending version-matched native Bedrock measurement. Live LBSG diagnosis (2026-08-22, run `run-lbsg-trace-374e095e`): the captured rejected stream shows the server-provided spawn embedding the prediction in lobby geometry so position oscillates up to ±0.6 blocks per tick while every packet claims zero input and gravity-only delta (`rust-mcbe-pai-trace-v1`, 194 samples); the anti-cheat rejected exactly that inputless displacement ("We've detected movement cheats", reproduced twice). With the gate active, a follow-up authenticated LBSG session with OS-level driven input survived 602 s and 11,535 transmitted samples with zero decode errors and no kick (ungated repro: kick within ~40 s), with `MOVEMENT_TX_GATE` engaged/timeout-lifted markers recorded; spawn-embedment root cure, native stall measurement, and sustained multi-target survival remain open. | `app/src/runtime/network.rs`, `app/src/movement.rs`, `app/src/movement/settle.rs`, `app/src/movement/outbox.rs`, `movement/physics.rs`, `movement/runtime_system.rs` | Rerun the live LBSG/Venity targets with the trace and gate markers; add discriminating startup/stall/perspective/DPI captures before changing clocks or scaling or the provisional settle constants. Two further slices landed 2026-08-25 (see the landed-tranches table): the bounded spawn-anchor depenetration probe with `EmbeddedHold`, and opt-in HandledTeleport acknowledgement. An authenticated current-binary LBSG capture then reproduced the exact `"We've detected movement cheats"` rejection after two 200-tick embedded-anchor fail-open lifts with a constant +1.00 X-block/tick inputless slide, making collision-truth adjudication instrumentation (name the sealing colliders during failed probes) the next cure tranche. |
| VPA-110 | Implementation landed | Accepted stack responses now retain custom names, filtered names, and durability corrections as per-cell overlays across player, cursor, and generic-storage cells (travelling with predicted stacks, merging affirmative restatements, cleared by any other authority replacement or recovery), and presentation consumes them for hotbar durability bars plus the authoritative selected-item name. The provisional damage-domain reading (maximum minus remaining) and the omit-retains merge policy are explicitly provisional pending version-matched native Bedrock measurement. | `app/src/ui_runtime/inventory_ledger/response.rs`, `inventory_ledger.rs`, `item_facts.rs`, `presentation/publish.rs` | Preserve this boundary while native measurement of the correction domain and omit-retains policy, personal/storage-screen cell bars, filtered-name presentation policy, and native/live equipment acceptance remain open. |
| VPA-111 | Missing surface | Inventory supports only server-authoritative full-stack left-click Take/Place/Swap and generic 27/54 storage. | `app/src/ui_runtime/interaction.rs`, `inventory_ledger.rs`, `crates/protocol/src/inventory/request.rs` | Add typed cells/actions and then survival, creative, crafting, furnace, controller, and touch slices independently. |
| VPA-112 | Implementation landed | The software player preview is now confined to personal inventory and stays absent from gameplay plus Open-before-Content and populated storage screens. Its inventory geometry, pose, equipment, and animation still do not share authoritative actor rendering. | `app/src/ui_runtime/presentation/player_preview.rs`, `publish.rs`, `hud_layout.rs` | Replace the inventory approximation with the same actor rig/equipment/action snapshot as world rendering and close a native visual/animation gate. |
| VPA-113 | Proven mismatch | Legacy 64x32, persona/custom geometry, outer layers, and duplicate entity-definition selection are incomplete or silently defaulted. | `crates/render/src/actor.rs`, `crates/client-world/src/actor_animation.rs` | Add explicit layout/candidate identity and fail-visible bounded fallbacks with native player galleries. |
| VPA-114 | Proven mismatch | Every block-entity renderer remains deferred; backing blocks render without chest/sign/banner/bed/skull/beacon-specific presentation. | `assets/block-entity-renderers-v1001.json`, `crates/client-world/src/block_entity_visuals.rs` | Implement one manifest-owned renderer family with NBT/live-update/native gates. |
| VPA-115 | Proven mismatch | Authentication has no sign-out/account switch and malformed cache recovery has no product flow. | `core/authflow`, `core/authcache`, `app/src/menu`, UI menu screens | Add secure quarantine/sign-out plus bounded supervised recovery and native UX evidence. |
| VPA-116 | Proven mismatch | The normal app does not subscribe to the planned control/status surface; lifecycle, pack, transfer, disconnect, and refresh state stay fragmented. | `core/control`, `app/src/menu.rs`, `crates/bridge` | Define a versioned control protocol and replace one-shot helper polling with one owned subscription. |
| VPA-117 | Proven mismatch | Menus are a custom Java-like shell even though the Java exception applies only to gameplay HUD. Settings selection is transient and often inert. | `app/src/ui_runtime/presentation/menu.rs`, `app/src/menu.rs`, `app/src/settings_runtime.rs` | Establish measured Bedrock/resource-pack menu contracts and one persisted settings authority. |
| VPA-118 | Missing surface | Gameplay touch is disabled; menu/controller input bypasses the shared semantic binding authority. | `app/src/ui_runtime/gameplay_touch.rs`, `app/src/menu/input.rs` | Measure and implement touch layout, then route menu/gameplay through semantic controls. |
| VPA-119 | Missing surface | Local world creation, selection, persistence, reload, pause/resume, and Dragonfly lifecycle are absent. | `app/src/menu`, `core/go.mod`, `plan.md` | Add a lifecycle-managed local Dragonfly target and world CRUD before gameplay parity. |
| VPA-120 | Implementation landed | Core shutdown now closes the child's piped stdin (bedrock-core's own stdin-EOF cancellation path), waits bounded 1.5 s inside the shutdown-watchdog envelope, and kills only as fallback. | `app/src/menu/core_process.rs`, `core/cmd/bedrock-core/main.go` | Preserve this boundary; verify final telemetry/cache sync on an orderly live exit. |
| VPA-121 | Proven mismatch | Windows endpoint ownership is not enforced; packaging is local-development staging without platform-native metadata/signing/install/update coverage. | `core/internal/streamnet/endpoint_windows.go`, `tools/dist` | Add owner-only Windows transport/ACL tests and platform-native installed-launch pipelines. |
| VPA-122 | Implementation landed | One canonical container-address projection (see the landed-tranches table) now routes every admission, lookup, response correction, and HUD ingestion through typed cells; cursor events cannot reach hotbar cells through any path and unknown containers are typed counted skips. Armor/offhand resolve canonically but are retained nowhere; native/live inventory acceptance remains open. | `crates/protocol/src/inventory/address.rs`, `app/src/ui_runtime/inventory_ledger/admission.rs`, `gameplay_hud.rs` | Preserve this boundary; add armor/offhand retention and live container acceptance as independent slices. |
| VPA-123 | Implementation landed | Every hotbar cell — selected or not — now derives from one ledger-snapshot authority revision (see the landed-tranches table), so accepted sparse responses refresh all consumers atomically. Native/live hotbar acceptance remains open. | `app/src/ui_runtime/presentation/publish.rs`, `gameplay_authority.rs` | Preserve this boundary; native/live hotbar acceptance under Phase 5.5. |
| VPA-124 | Implementation landed | A local storage close with an admitted prediction now enters a bounded closing state that retains the generation, identity, and journal, blocks new gestures, and settles on the last resolution, authoritative close, existing timeout/recovery, session reset, or window replacement. | `app/src/ui_runtime/inventory_ledger.rs`, `app/src/ui_runtime/inventory_ledger/response.rs` | Preserve this boundary; live container-close acceptance remains open under Phase 5.5. |
| VPA-125 | Missing surface | `storage_item` associations are decoded but ignored, so embedded item-container contents and bundle-style actions cannot remain authoritative. | `crates/protocol/src/inventory.rs`, `app/src/ui_runtime/inventory_ledger.rs` | Retain a bounded storage-item association and add typed item-container request/reconciliation slices. |
| VPA-126 | Missing surface | Gameplay block break/place/use, actor attack/interact, item release, consumable, and durability-prediction timelines have no app producer; nested `PlayerAuthInput` action payloads are always absent. | `crates/protocol/src/interaction.rs`, `crates/protocol/src/movement.rs`, `app/src/ui_runtime/interaction.rs` | Add one immutable interaction/selected-stack snapshot and one independently tested action family at a time. |
| VPA-127 | Implementation landed | Canonical retained item identity now binds the wire block runtime ID into equality and validation, and stacks that retained block identity route fail-visibly (`ItemVisualRoute::RetainedBlock`) instead of laundering through sprite aliases; compiled `BlockItem` routes stay authoritative and classification reads only wire fields. Renderer consumption of `RetainedBlock` and the separate 3-D block-item path remain open under VPA-007/VPA-016. | `crates/assets/src/item.rs`, `crates/client-world/src/item.rs` | Preserve this boundary; close VPA-007's canonical-identifier-to-sprite crosswalk and the block-item renderer without filename inference. |
| VPA-128 | Proven mismatch | Component/data-driven item facts are discarded and durability/armor facts fall back to incomplete hardcoded tables. | `app/src/ui_runtime/item_facts.rs`, `crates/protocol/src/item.rs`, `crates/client-world/src/item.rs` | Retain bounded component definitions and resolve them through the active pack stack before enabling custom-item behavior. |
| VPA-129 | Implementation landed | Wire camera instruction families 300/159/73 now decode into bounded vendor-neutral events, commit FIFO through client-world with honest backpressure accounting, and drain into a capped ordered `ServerCameraInstructions` resource with eager per-drain session/dimension identity refresh; non-finite fields, oversized ease identifiers, and splines are counted semantic skips while truncated wire stays fatal. Nothing is wired into view transforms yet; presets/aim-assist registries remain pre-decode drops. One accepted bounded window: camera committed in the same poll batch as a dimension switch may carry prior-dimension context until the next drain (documented at the identity site); streamless intervals defer clearing until the next drain. | `crates/protocol/src/camera.rs`, `app/src/server_camera.rs`, `crates/client-world/src/stream` | Wire the retained state into renderer-owned view/camera transforms behind a visual acceptance gate before claiming application parity. |
| VPA-130 | Implementation landed (slice) | Server forms now admit through structural validation into a bounded id-keyed store (cap with replacement/oldest-eviction accounting, session/dimension clears incl. the documented same-batch window), and a tested menu-form answer lifecycle stages latest-wins `ModalFormResponse` frames with restore-on-failure retry; modal/custom answers fail closed pending an explicit boolean action. No renderer, focus owner, or production transport caller exists yet; custom-element state capture is explicitly out of scope; `"modal"`/`"custom_form"` kind strings are provisional pending a version-matched reference. | `crates/protocol/src/ui/forms.rs`, `app/src/ui_runtime/forms.rs` | Wire the staged seam to the network transport behind one owned lane, then add the boolean modal action, element state capture, UI rendering, focus/navigation, and live form acceptance independently. |
| VPA-131 | Implementation landed | The gameplay player-list action now has default keyboard (Tab), gamepad (RightThumb, provisional), and reserved touch bindings; held semantics, context disjointness with UI Tab, and focus-loss release reasons are witnessed per device, plus one end-to-end app witness driving Tab into the overlay. | `crates/input/src/binding.rs`, `crates/input/tests/router/player_list.rs`, `app/src/ui_runtime/presentation/tests/tab_binding_tests.rs` | Preserve this boundary; native hold/timing acceptance and the touch layout remain open under VPA-118. |
| VPA-132 | Implementation landed | Toasts now expire through an explicitly provisional bounded duration, prune in receive order with retained-byte accounting, and stop rendering at exact expiry; the duration is labeled provisional because no version-matched native Bedrock toast timing has been measured. | `crates/ui/src/hud.rs`, `app/src/ui_runtime/event_apply.rs` | Measure native Bedrock toast duration/transition timing and replace the provisional constant; then close a matching visual gate. |
| VPA-133 | Implementation landed | Displayed objectives now classify the wire criteria name into an explicit `ScoreRenderType`, health-style criteria present a bounded provisional ten-heart sprite row instead of decimal text when the required HUD carrier is loaded, and unresolvable player/entity sidebar owners fall back to their raw retained numeric identity instead of disappearing. The hearts mapping is explicitly provisional pending native evidence; list/below-name surfaces still present integer values behind their own open native gates; no XUID-keyed name source exists yet (normalization drops the PlayerList xuid field), so XUID-keyed owners resolve only through the unique-id-keyed authority map and otherwise show raw ids. | `crates/ui/src/scoreboard.rs`, `crates/ui/tests/scoreboard.rs`, `app/src/ui_runtime/presentation/retained_hud.rs`, `app/src/ui_runtime/presentation/tests/retained_hud_tests.rs` | Fresh independent review returned APPROVE with four Minors, all dispositioned: the hearts-advance comment is now labeled provisional pending native measurement, the carrier-absent hearts degradation is documented at the cell builder (unreachable in production because startup fails closed), the retention-witness comment now matches the exact witnessed sequence plus a hearts re-display assertion, and the lowercase criteria-vocabulary authority is recorded for the future native-evidence gate. Native hearts/sidebar calibration, a real owner-name source for unloaded/XUID-keyed owners, and matching visual gates remain open. |
| VPA-134 | Implementation landed | Active boss bars with explicit `darken_sky`/`create_world_fog` requests now drive a bounded, explicitly provisional air-medium atmosphere response (sky mix toward storm targets, fog pinned to provisional distances); unset flags reproduce prior frames byte-identically. The pinned protocol-2168 wire never populates these flags, so live traffic stays inert today. | `crates/render/src/atmosphere.rs`, `app/src/environment/atmosphere.rs` | Capture version-matched native boss darkening/fog behavior, calibrate the provisional constants and dim-vs-mix semantics, then close a matching visual gate. |
| VPA-135 | Proven mismatch | Personal/storage inventory screens use fixed Java-proportioned hand-drawn geometry even though the Java exception covers only the gameplay HUD. | `app/src/ui_runtime/presentation/hud_layout/inventory.rs`, `plan.md` | Replace non-HUD screens with versioned Bedrock/resource-pack layout carriers and native screen galleries. |
| VPA-136 | Implementation landed (slice) | The deterministic half of this row is closed: the Go core now exposes an explicit opt-in `-upstream-client-cache` / `Config.UpstreamClientCache` capability whose only effect is flipping the outbound upstream `ClientCacheStatus` enabled byte to 1 through an observe-then-flip `Dialer.PacketFunc` before telemetry observation, so `PHASE2_CACHE_BOUNDARY upstream_status_enabled` reports the effective wire value; default false reproduces today's exact bytes under renamed ratchet tests (`TestNewUpstreamDialerDefaultsUpstreamClientCacheOff`, scripted-server default-disabled witness) plus new wire-level opt-in/pass-through/propagation tests, and both production app spawn paths pass the flag exactly when the session owns its process-lifetime `ClientBlobCacheOwner` cache (the same fact that makes the vendored client advertise support downstream). Still open: any live cached-chunk streaming evidence (real MissResponse round-trips on Venity/LBSG/BDS, RSS bounds under blob retention, anti-cheat deltas), runtime dynamic negotiation (deliberately out of scope; documented operator constraint), and the recorded Minors: `-auth-events` mode does not reject the new serve-only flag like its siblings, the enables-upstream predicate is a documented comment-enforced seam over today's always-owned cache, telemetry remains direction-blind (pre-existing), and the `run()` disabled-direction plumbing rests on the parse-default test alone. | `core/proxy/proxy.go`, `core/proxy/cache_boundary.go`, `core/cmd/bedrock-core/main.go`, `app/src/menu/core_process.rs`, `app/src/menu/input.rs`, `app/src/app.rs` | Preserve default-off; then run the live cached-streaming tranche on authorized targets with RSS/anti-cheat observation before claiming the row's parity closure. |
| VPA-137 | Proven mismatch | The verified blob cache is process-memory-only with synchronous local LRU thresholds; native persistence, timestamp flush, background trimming, and restart behavior are absent or unmeasured. | `crates/protocol/src/blob_cache.rs`, `app/src/app.rs` | Add a version-bound atomic persistent backend after restart/suspend/trim behavior is measured. |
| VPA-138 | Implementation landed (file-permission slice) | Cached-token files are now owner-only at creation and revalidated at load/open on both platforms; foreign-owner/unverifiable descriptors fail closed with typed secret-free codes; quarantined `.invalid` siblings are restamped private; pre-existing broad caches quarantine and re-authenticate. Remaining open: ancestor-directory privacy policy, `-auth-events` observability of the quarantine reason, ACL-less-filesystem documentation, Windows ACL hardening beyond token-cache files, native/live multi-platform evidence. | `core/authcache/cache.go`, `core/authcache/permissions_unix.go`, `core/authcache/permissions_windows.go`, `core/authcache/quarantine.go` | Preserve this boundary; then close the recorded Minors and add platform-native installed-launch ACL coverage under VPA-121/VPA-139. |
| VPA-139 | Implementation landed | Saved-server storage is now atomic (flushed temp sibling + rename), refuses out-of-schema entries, clears stale temp files from crashed runs, and quarantines unreadable files to a `.invalid` sibling with a visible startup recovery message; non-NotFound read failures are reported without touching the file. Unix files are mode 0600; Windows ACL hardening remains under the VPA-121/VPA-138 families. Swallowed save errors at the favorite/remove call sites remain a recorded minor. | `app/src/menu/servers.rs` | Preserve this boundary; native multi-instance and power-loss durability evidence remains open. |

## P2 â€” visual calibration and bounded policy work

| ID | Classification | Current mismatch | Primary evidence | Closure gate |
| --- | --- | --- | --- | --- |
| VPA-201 | Proven mismatch | 2,031 block states use labeled provisional fallback geometry/materials. | `crates/asset-compiler/src/compiler/visuals/fallback.rs`, `tools/visualcoverage` | Zero-fallback, version-current strict gallery with per-family native comparison. |
| VPA-202 | Open contract | Biome tint uses a provisional equal-weight radius-one kernel. | `crates/meshing/src/biome.rs`, `crates/render/src/biome_tint.wgsl` | Abrupt/diagonal biome boundary native measurements plus cross-chunk GPU witness. |
| VPA-203 | Open contract | Lighting transfer, night/ambient floors, AO, and light-channel combination are approximations. | `crates/render/src/lighting.wgsl`, `crates/meshing/src/lighting.rs` | Native day/night/cave/emissive/Nether/End calibration and performance pass. |
| VPA-204 | Open contract | Atmosphere, celestial size/filtering, weather, medium fog, and cloud geometry/configuration are not calibrated. | `crates/render/src/atmosphere*`, `cloud_config.rs`, `crates/meshing/src/cloud.rs` | Matching-view all-phase/horizon/weather/underwater/lava/cloud galleries. |
| VPA-205 | Open contract | Fluid surface weighting, falling heights, UV flow/scroll, lava behavior, and flipbook replication are unverified or dead. | `crates/meshing/src/liquid.rs`, `crates/render/src/liquid.wgsl`, texture upload code | Frame-by-frame water/lava state gallery across seams, flow, falls, and waterlogging. |
| VPA-206 | Proven mismatch | GUI scale settings do not drive live presentation; Auto mixes physical and logical size; platform safe-area acquisition is a zero stub. | `app/src/menu.rs`, `app/src/ui_runtime/presentation`, `publish.rs` | Java HUD GUI 2/3/4/Auto matrix across DPI/Retina plus platform inset propagation. |
| VPA-207 | Proven mismatch | Ordinary actor name tags are absent unless a below-name scoreboard objective exists. | `app/src/ui_runtime/presentation/publish.rs`, actor queries | Bounded distance/occlusion/mode name-tag path with live two-client evidence. |
| VPA-208 | Open contract | Persistent pack cache omits server digest; asset fetch freshness is presence-based; multi-file generators publish non-transactionally. | `core/packcache`, fetch scripts, generator tools | Digest-bound identity and stale/interrupted publication tests. |
| VPA-209 | Implementation landed | Both fetch scripts now enforce entry-count, declared/actual per-file and total expanded-byte, and compression-ratio bounds with tighten-only CLI overrides, reject Unix link entries before extraction, reclaim stale `.extracting*` staging, and publish transactionally through an immediate pre-move absence recheck plus a deterministic PATH-shim race witness; CI exercises the bash leg on Linux/macOS. Recorded Minors: the PS-side extractor has no deterministic race-injection seam (guard only), interruption coverage is proxied via stale-staging simulation, duplicate-directory rejection diverges across scripts, residual check-to-mv window undocumented in comments. | `scripts/fetch-vanilla-assets.ps1`, `scripts/fetch-vanilla-assets.sh`, `scripts/tests/vanilla-assets.ps1` | Preserve this boundary; direct kill-mid-extraction fixtures and a PS-side race seam remain optional hardening. |
| VPA-210 | Implementation landed | Per-session runtime directories are now bound by an identity-checked grammar-bound guard whose marker-proven reclamation (provisional 24 h age / 64-entry bounds) never removes explicit `--socket-dir` operator directories and never deletes through junction/symlink-shaped names; disconnect/exit/failure teardown releases app-owned state after the core child stops. Recorded Minors: owner-marker writes are non-atomic (crash residue is skipped, not reclaimed), age-based reclaim can strike a genuinely live >24 h session, staging keep-4 can delete a live sibling's staging under ≥6 concurrent fetches. | `app/src/session_cleanup.rs`, `app/src/app.rs`, `app/src/menu/input.rs` | Preserve this boundary; OS liveness probes and atomic marker writes are the eventual cure for the recorded Minors. |
| VPA-211 | Open contract | Windows defaults to DX12 with only a manual environment override; capability-driven backend fallback and unsupported-hardware UX are undefined. | `app/src/app.rs`, `docs/handoffs/2026-07-18-dx12-present-mode.md` | Define adapter/backend negotiation from a native hardware matrix and add visible recovery. |
| VPA-212 | Open contract | Java-HUD damage flash, selected-label timing, effect flicker, boss tint, and related calibration values remain explicit approximations. | `app/src/ui_runtime/presentation/hud_layout/pinned.rs` | Capture lawful matching-version Java HUD timing/visual witnesses and calibrate without copying decompiled identifiers or constants. |
| VPA-213 | Implementation landed | Timed HUD families (`Text`, `CommandOutput`, `RawText`, `Title`, `Hud`) carrying a populated `server_tick` are now rejected by `apply` with a typed error before any mutation, the former tick-to-millis conversion is removed so timed stamping always uses local millis, the domain contract is documented on the field, and rejection-hygiene witnesses cover FIFO/tick-anchor non-advancement plus CommandOutput/RawText coverage. Non-timed families retain tick ordering authority unchanged; the sole production producer passes `None`, so live behavior is byte-identical today. | `app/src/ui_runtime.rs`, `app/src/ui_runtime/tests.rs` | Preserve this guard; a future producer needing server-tick-domain expiry must implement an explicit anchored conversion and drop to sub-variant classification deliberately. |

## Program rules

1. Fix P0 items as independently reviewable vertical slices. Do not combine unrelated protocol,
   movement, rendering, and product changes into one acceptance claim.
2. Every behavior change starts with an exact contract and a failing regression witness.
3. A deterministic test closes implementation only. Native, visual, timing, performance, and
   review gates remain separate.
4. Do not replace an open contract with a guessed constant. Label a bounded approximation in
   `plan.md` and user-facing status until independent measurement exists.
5. Keep captures, credentials, non-redistributable inputs, and Mojang payloads out of git. Commit
   only independently stated behavior, lawful compact rules, hashes, and tests.
