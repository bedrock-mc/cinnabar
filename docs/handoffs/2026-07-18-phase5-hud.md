# Phase 5 vanilla HUD handoff

Branch: `phase5-hud-vanilla-geometry`

Base: `origin/phase2-textures` at `d026417`

Status: incomplete work-in-progress checkpoint. Do not merge or describe as
visually accepted.

## Implemented in this checkpoint

- Adds a dedicated `MCBEHUD1` runtime carrier with strict schema, provenance,
  payload-hash, dimension, and role validation.
- Adds a compiler path that reads only pinned local resource-pack PNG inputs.
- Defines canonical 9x9 heart background, full-heart, and half-heart roles.
- Begins Makefile, startup, UI-model, and presentation wiring.

The real pinned `v1.26.30.32-preview` heart inputs compiled successfully. Focused
asset tests passed 3/3 and compiler tests passed 2/2 before interruption.

## Incomplete work

- Finish startup/Makefile wiring and make startup fail closed on stale or missing
  HUD carrier provenance.
- Add the textured-image retained visual and combine HUD texture pages with the
  font texture array without per-frame resource churn.
- Replace both numeric `20/20` health stand-ins with ten vanilla heart slots;
  preserve server-authoritative full/half/empty state.
- Add negative tests proving no duplicate numeric HUD survives.
- Run strict Clippy, formatting, focused/full tests, independent review, and an
  exact native Bedrock comparison at matching resolution/DPI.

The current dirty source is intentionally checkpointed for handoff; it was not
fully compiled or reviewed and may contain integration errors.

